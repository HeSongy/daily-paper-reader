---
title: "WorldComp2D: Spatio-semantic Representations of Object Identity and Location from Local Views"
title_zh: WorldComp2D：基于局部视图的物体身份与位置的时空语义表示
authors: "SeongMin Jin, Doo Seok Jeong"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11743v1"
tags: ["query:navwm"]
score: 9.0
evidence: 空间语义隐空间与物体身份表示
tldr: WorldComp2D提出一种轻量级框架，通过显式构造潜在空间的几何结构，将对象的语义身份与空间位置信息结合，用于高效的时空语义推理。通过多尺度局部感受野和邻近依赖编码器，实现参数与计算量的大幅减少，同时保持实时性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11743v1/fig-001.webp\", \"caption\": \"Figure 5. Analysis of spatio-semantic latent representation. (a) L2 distances between individual landmark representations and their corresponding class means. (b) L2 distances between the left pupil representation and other landmarks representations within the same image. L2 distances as a function of spatial distance from a given landmark obtained from (c) PdEnc and (d) from an encoder trained using proximity-unweighted contrastive loss.\", \"page\": 6, \"index\": 1, \"width\": 492, \"height\": 452}, {\"url\": \"assets/figures/arxiv/2605.11743v1/fig-002.webp\", \"caption\": \"Table 1. Comparison of our method with SoTA approaches on COFW, 300W and AFLW.\", \"page\": 7, \"index\": 2, \"width\": 995, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2605.11743v1/fig-003.webp\", \"caption\": \"Table 4. Localization accuracy under various degradations.\", \"page\": 8, \"index\": 3, \"width\": 488, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2605.11743v1/fig-004.webp\", \"caption\": \"Table 5. PdEnc with different model complexities.\", \"page\": 8, \"index\": 4, \"width\": 438, \"height\": 285}, {\"url\": \"assets/figures/arxiv/2605.11743v1/fig-005.webp\", \"caption\": \"Figure 2. Networks in WorldComp2D. (a) Proximity-dependent encoder (PdEnc), which maps fixation-centered observations to a normalized latent vector. (b) Localizer (Loc), which aggregates paired latent vectors and fixation coordinates to predict object locations. (c) Auxiliary localizer (AuxLoc), an optional refinement module that estimates a heatmap from a local patch and class-conditioned embedding.\", \"page\": 4, \"index\": 5, \"width\": 833, \"height\": 433}, {\"url\": \"assets/figures/arxiv/2605.11743v1/fig-006.webp\", \"caption\": \"Table 2. Network architectures.\", \"page\": 12, \"index\": 6, \"width\": 1071, \"height\": 324}]"
motivation: 现有方法潜变量结构隐式，计算效率与灵活性受限。
method: 提出了基于邻近依赖编码器和局部坐标推断模块的WorldComp2D框架。
result: 在面部关键点定位任务中，参数和计算量分别减少达4倍和2.2倍，并保持实时CPU性能。
conclusion: 显式结构化潜在空间能够为时空语义推理提供高效且通用的基础。
---

## 摘要
学习能够同时捕捉语义与空间信息的潜在表示是高效时空语义推理的核心。然而，许多现有方法依赖于隐式潜在结构，结合密集特征图或任务特定的头部，从而限制了计算效率和灵活性。我们提出 WorldComp2D，这是一种新颖的轻量化表示学习框架，通过多尺度局部感受野显式地以物体身份和空间邻近性来组织潜在空间几何结构。该框架包括：(i) 一个与邻近性相关的编码器，将给定观测映射到时空语义潜在空间；以及 (ii) 一个定位器，从生成的时空语义表示中推断输入中物体的坐标。以人脸关键点定位为概念验证实验，我们展示了与当前轻量级模型相比，WorldComp2D 能分别将参数量和浮点运算数（FLOPs）最多减少 4.0 倍和 2.2 倍，同时在 CPU 上保持实时性能。这些结果表明，显式结构化的潜在空间为时空语义推理提供了高效且通用的基础。该框架已开源，地址为：https://github.com/JinSeongmin/WorldComp2D。

## Abstract
Learning latent representations that capture both semantic and spatial information is central to efficient spatio-semantic reasoning. However, many existing approaches rely on implicit latent structures combined with dense feature maps or task-specific heads, limiting computational efficiency and flexibility. We propose WorldComp2D, a novel lightweight representation learning framework that explicitly structures latent space geometry according to object identity and spatial proximity using multiscale local receptive fields. This framework consists of (i) a proximity-dependent encoder that maps a given observation into a spatio-semantic latent space and (ii) a localizer that infers the coordinates of objects in the input from the resulting spatio-semantic representation. Using facial landmark localization as a proof-of-concept, we show that, compared to SoTA lightweight models, WorldComp2D reduces the numbers of parameters and FLOPs by up to 4.0X and 2.2X, respectively, while maintaining real-time performance on CPU. These results demonstrate that explicitly structured latent spaces provide an efficient and general foundation for spatio-semantic reasoning. This framework is open-sourced at https://github.com/JinSeongmin/WorldComp2D.

---

## 论文详细总结（自动生成）

# WorldComp2D：基于局部视图的物体身份与位置的时空语义表示 — 中文总结

---

## 一、研究背景与核心问题

* **研究动机**：  
  论文关注的是如何在**局部视角**下构建能够同时编码语义与空间信息的潜在表示。这在**具身智能体（Embodied AI）**中尤为关键，因为此类系统不能获得完整的全局图像，只能基于局部观测快速进行空间推理与决策。

* **现有问题**：  
  - 传统视觉模型往往假设对整个图像的访问（如全图卷积或热力图回归）。  
  - 隐式潜变量结构缺乏空间几何约束，导致计算代价高且泛化能力有限。  
  - 在真实时间和算力受限的场景中（如移动端或机器人视觉系统），这些方法难以高效运行。

* **总体目标**：  
  作者旨在提出一个**显式结构化的轻量表示学习框架**，从局部观测中学习潜在空间的空间语义结构，既保留物体身份，又反映空间距离，服务于高效的时空语义推理。

---

## 二、方法论：WorldComp2D 框架

### 核心思想
通过显式设计潜在空间的几何结构，使得：
- 同类（同语义身份）对象在隐空间中保持聚类；
- 不同对象之间的距离能够反映它们在真实空间中的相对距离；
- 仅依赖多个局部视图即可完成空间定位。

### 框架结构
WorldComp2D由三个主要组件组成：

1. **邻近依赖编码器（PdEnc）**  
   - 输入：两尺度的局部感受野 \( RF_1, RF_2 \)，都以同一注视点 \( F \) 为中心。  
   - 输出：单位球面上的时空语义潜向量 \( z \in \mathbb{R}^{256} \)，通过 L2Norm 约束。  
   - 网络结构：6 个卷积层 + 2 个全连接层，最终输出归一化向量。  
   - 设计目的：在潜空间中同时保留物体的语义身份和邻近的空间关系。

2. **定位器（Loc）**  
   - 输入：多个观测点对应的 \((z, F)\) 对，拼接后输入一个 MLP。  
   - 输出：所有对象的归一化坐标预测 \(\hat{x}\)。  
   - 结构：FC512–FC512–FCn–Tanh。  
   - 功能：在不访问整幅图像的前提下，根据少量局部表征推断目标位置。

3. **辅助定位器（AuxLoc，可选）**  
   - 输入：以预测坐标为中心提取的局部图像块及类别嵌入。  
   - 输出：细化的热力图，用于坐标微调。  
   - 特点：采用深度可分离卷积，实现轻量化 refinement。

### 学习机制：邻近加权对比学习（Proximity-Weighted Contrastive Learning）

* **创新点**：  
  不再采用传统的二元正负样本对，而是引入**连续空间距离权重** \( w_{ij} = 1 + \exp(-0.025d_{ij}) \)，其中 \( d_{ij} \) 为真实空间距离。
* **损失函数（PWConLoss）**：  
  通过加权的对比学习目标，使得：
  - 语义相同的对象在潜空间中靠近；
  - 与注视点空间邻近的对象在潜空间中相对接近；
  - 同时维持跨样本一致性。

整体训练流程依次优化 PdEnc → Loc → AuxLoc。

---

## 三、实验设计

### 实验任务
以**人脸关键点定位**作为验证任务（结构化定位的典型场景），展示方法能否从局部视图推理整体坐标。

### 使用的数据集
1. **COFW**：1,345训练 + 507测试，29个关键点，含遮挡。  
2. **300W**：3,148训练 + 689测试，68个关键点，姿态与光照变化大。  
3. **AFLW**：20,000训练 + 4,386测试，19个关键点，姿态变化大，但部分面部不可见。  

### 对比基线（SoTA）
与多种主流轻量级关键点定位方法比较：
- LAB, AWing, AVS, ODN, HRNet, PIP, ADNet, SLPT, STARLoss, RHT-R, D-ViT, PoPos 等。

### 评价指标
- **Normalized Mean Error (NME)**：使用不同归一距离（眼间距、瞳间距或人脸框对角线）评估定位误差。  
- **FLOPs 与参数量**：衡量模型复杂度与效率。  
- **FPS（帧率）**：测试实时性能。

---

## 四、算力与训练资源

* **训练平台**：  
  - GPU：RTX A6000（Xeon Gold CPU 2.9GHz，256GB RAM）。  
  - 框架：PyTorch。  
  - PdEnc训练2000轮（AFLW为600轮），Loc和AuxLoc训练1000轮。  
  - 批次大小 PdEnc:128，Loc/AuxLoc:50。  
  文中未提及多GPU或总训练时长，但提供了完整超参数表。

* **推理平台**：  
  - 仅CPU（i5-13400, 2.5GHz, 128GB RAM）即可实时运行，FPS可低至 78–163。  
  - GPU高精度(FP32/FP16/BF16)模式分别展示了万级FPS性能。

---

## 五、实验丰富性与客观性

* 共进行了**三大类实验 + 多项补充分析**：  
  1. 主实验：COFW、300W、AFLW 三个独立评估集。  
  2. **潜空间分析**：验证语义聚类与空间距离保持。  
  3. **效率实验**：FPS 与 FLOPs 对比。  
  4. **消融实验**：PdEnc复杂度、AuxLoc作用、注视点数量NF的影响。  
  5. **鲁棒性测试**：多种图像退化（模糊、压缩、遮挡等）。  
  6. **跨数据集验证**：在COFW-68上零样本迁移。

* 实验设计全面，既有定量评估也有定性可视化；对比基准涵盖主流强模型，结果公正且说明充分。

---

## 六、主要结论与发现

* **核心成果：**
  - 显式结构化潜在空间能有效融合“语义身份”与“空间邻近性”。  
  - 在不依赖完整图像的前提下，实现高效、可解释的空间定位。  
  - 模型极度轻量化（仅2.4M参数），FLOPs比SoTA降低至1/2~1/4。  
  - 在CPU端保持实时速度（>78 FPS），在GPU上可达万FPS级。  
  - 潜空间几何呈现出语义聚类与距离保持特性。

---

## 七、优点与创新亮点

1. **显式潜空间几何结构设计**：  
   将空间距离直接嵌入学习目标，具有良好的可解释性。

2. **轻量化与多尺度局部感受野结合**：  
   兼顾局部精度与上下文信息，适合嵌入式与低功耗场景。

3. **任意位置推理能力**：  
   不依赖全图输入，支持多视点融合推理。

4. **灵活可调的计算–精度权衡**：  
   通过NF或AuxLoc的开关可调整性能/速度。

5. **鲁棒性强**：  
   对模糊、压缩及遮挡场景的误差提升控制在10%以内。

6. **开源实现**：  
   代码公开（GitHub），便于复现与扩展。

---

## 八、不足与局限性

* **应用范围受限**：目前仅在2D人脸关键点任务中验证，尚未在复杂3D或多物体场景测试。  
* **观察策略固定**：使用周期性固定注视点（NF=9），未探索自适应或主动视觉策略。  
* **邻近性假设**：空间距离权重仅在局部感受野范围内有效，超范围推理性能下降。  
* **比较精度**：在部分数据集上（如300W）精度略低于强监督回归式SOTA模型。  
* **算力描述不完整**：未提供总训练时长与GPU数量，对实际训练成本的评估略不足。

---

（完）
