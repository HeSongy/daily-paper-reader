---
title: Do We Really Need Immediate Resets? Rethinking Collision Handling for Efficient Robot Navigation
title_zh: 我们真的需要立即重置吗？重新思考高效机器人导航中的碰撞处理
authors: "Shanze Wang, Xinming Zhang, Siwei Cheng, Xianghui Wang, Hailong Huang, Wei Zhang"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02192v1"
tags: ["query:navwm"]
score: 8.0
evidence: 重新思考碰撞处理以实现高效的机器人导航学习
tldr: 本文重新思考了机器人导航中的碰撞处理机制，提出多碰撞重置预算（MCB）框架，允许机器人在同一回合内多次尝试困难环境，从而提升早期探索效率和整体导航性能。研究表明，小碰撞预算下可获得最大收益。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02192v1/fig-001.webp\", \"caption\": \"Fig. 5. Trajectory comparison between SCR and MCB-K2 in Env 2.\", \"page\": 5, \"index\": 1, \"width\": 514, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2605.02192v1/fig-002.webp\", \"caption\": \"Fig. 3. Learning curves and navigation efficiency metrics of the compared methods on Env 1 and Env 2 in Stage. Solid lines and shaded regions represent the mean and standard deviation over ten random seeds.\", \"page\": 5, \"index\": 2, \"width\": 514, \"height\": 633}, {\"url\": \"assets/figures/arxiv/2605.02192v1/fig-003.webp\", \"caption\": \"Fig. 6. Learning curves of MCB with collision budget K ∈ {2, 3, 5, 10, 50} and the SCR baseline on Env 1 and Env 2 in Stage.\", \"page\": 6, \"index\": 3, \"width\": 514, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2605.02192v1/fig-004.webp\", \"caption\": \"Fig. 1. Overview of Single-Collision Reset and Multi-Collision Budget. The conventional single-collision reset protocol stops the episode and triggers an immediate global reset after the first collision, whereas the proposed multicollision budget allows continued exploration in the same scene and performs a global reset only after the collision count reaches K.\", \"page\": 1, \"index\": 4, \"width\": 514, \"height\": 519}]"
motivation: 传统强化学习导航中一次碰撞即终止回合限制了早期探索，减慢了学习进度。
method: 提出多碰撞重置预算（MCB）框架，将局部碰撞与全局重置解耦，允许在同回合内多次尝试。
result: 在多种仿真和真实机器人平台上的实验表明，MCB框架加快了早期探索速度，提高了成功率和导航效率。
conclusion: 多碰撞重置预算机制在保证学习效率的同时提高了任务完成率和导航质量。
---

## 摘要
一次碰撞是否一定要终止整个导航回合？在大多数用于机器人导航的深度强化学习（DRL）框架中，这仍然是标准做法：每次发生碰撞都会立即触发全局环境重置，并被惩罚为一次完整任务失败。虽然在部署过程中发生碰撞自然意味着任务失败，但在训练期间采用相同的处理方式会阻碍智能体探索复杂障碍配置，从而减缓早期训练阶段的学习进度。在本研究中，我们挑战这一惯例，提出了一种多碰撞重置预算（Multi-Collision reset Budget，MCB）框架，将局部碰撞终止与全局环境重置解耦，使智能体能够在同一回合中重试困难的场景配置。在多个仿真和现实机器人平台上的实验结果表明，该框架能够加速早期阶段的探索，相比于传统的单次碰撞重置基线方法，显著提高了成功率和导航效率，而较小的碰撞预算带来了最大的增益。

## Abstract
Should a single collision necessarily terminate an entire navigation episode? In most deep reinforcement learning (DRL) frameworks for robot navigation, this remains the standard practice: every collision immediately triggers a global environment reset and is penalized as a complete task failure. While a collision during deployment naturally indicates task failure, applying the same treatment during training prevents the agent from exploring challenging obstacle configurations, which slows learning progress in the early training phase. In this work, we challenge this convention and propose a Multi-Collision reset Budget (MCB) framework that decouples local collision termination from global environment resets, allowing the agent to retry difficult configurations within the same episode. Experiments on multiple simulated and real-world robotic platforms show that the framework accelerates early-stage exploration and improves both success rate and navigation efficiency over conventional single-collision reset baselines, with a small collision budget producing the largest gains.

---

## 论文详细总结（自动生成）

# 论文总结：我们真的需要立即重置吗？重新思考高效机器人导航中的碰撞处理  
---

## 一、研究背景与核心问题

- **研究动机**：  
  深度强化学习（DRL）在机器人无地图导航（mapless navigation）中广泛应用，可直接学习从传感器到控制命令的端到端策略。然而，现有的训练惯例通常在每次碰撞后立即终止整个回合（episode）并重置环境。  
  作者指出，这种“单次碰撞立即重置”策略虽然符合实际部署中的安全逻辑，但在训练阶段限制了智能体的探索范围，尤其是在早期阶段难以学习复杂障碍规避行为。

- **核心问题**：  
  是否必须在每次碰撞后立即重置环境？这种做法是否会影响学习效率和策略质量？论文正是对这一习惯性假设进行系统性反思。

- **研究目标**：  
  提出一种新的碰撞处理机制，使智能体在训练中能够保留探索困难场景的机会，从而提高学习速度、稳定性与导航性能。

---

## 二、方法论与关键技术

### 1. 核心思想

- **提出方法**：多碰撞重置预算（Multi-Collision reset Budget, MCB）框架。
- **主要概念**：
  - **局部终止（Local Termination）**：碰撞发生时仅触发本地失败信号与惩罚，不立即重置环境。
  - **全局重置（Global Reset）**：只有当累计碰撞次数达到预设预算 K 时或者任务成功/超时，才重新初始化环境。

### 2. 算法与流程

- **碰撞计数机制**：  
  为每个回合设置一个碰撞计数器 `c`，从 0 开始。  
  - 当发生碰撞时：`c ← c + 1`  
  - 若 `c < K`：继续在当前场景中探索。  
  - 若 `c ≥ K`：执行全局重置，开始新回合。

- **训练算法**：基于 Soft Actor-Critic (SAC) 算法的标准框架，结合 MCB 扩展实现。
- **核心流程（Algorithm 1 要点）**：
  1. 初始化策略网络与评论家网络（critic），重置计数器与环境；
  2. 在每一步执行动作，记录奖励与事件；
  3. 若发生碰撞，标记结束但不重置场景；
  4. 跳过“碰撞桥接”转移（防止不一致的 MDP 转换），保持价值回传一致；
  5. 循环更新 SAC 网络参数；
  6. 当达到成功、超时或碰撞预算上限时，全局重置。

### 3. 关键机制补充

- **碰撞感知的转移构建（Collision-Aware Transition Construction）**：  
  在经验回放中，碰撞步被视为终止状态（`d_t = 1`），以确保价值函数在碰撞处不做错误回传，维持清晰的信贷分配。

- **姿态变化过滤（Pose-Change-Based Filtering）**：  
  针对多次相似碰撞，设定角度差阈值 `τ_θ`（如 3°），过滤掉重复的失败经验，以提升样本多样性。

---

## 三、实验设计

- **仿真平台**：
  1. **Stage Simulator**（ROS 环境）；  
  2. **Isaac Lab**（GPU 加速仿真框架）。

- **场景设置**：  
  - 训练环境 Env 1 和未见测试环境 Env 2。  
  - 任务为点到点导航，不同障碍布局。

- **对比方法**：
  - **SCR**：传统单次碰撞重置基线（Single-Collision Reset）。  
  - **MCB-K2**：碰撞预算 K=2 的 MCB 模型。  
  - **MCB-K2-PF3**：加入姿态过滤（阈值 τθ=3°）的 MCB 变体。

- **评价指标**：
  - 成功率（SR）  
  - 平均速度（AV）  
  - 平均回合长度（AEL）  
  - 平均导航得分（ANS），衡量成功与耗时综合表现。

---

## 四、算力与资源说明

- **硬件配置**：  
  > 所有实验在单台配备 NVIDIA RTX 4090 GPU 的 PC 上完成。
- **训练设置**：
  - Stage 平台：每方法训练 50,000 步，最多 200 步/回合。  
  - Isaac Lab 平台：3,000 轮训练，每轮包含 512 并行环境 × 16 步。  
- **随机性控制**：  
  每个方法运行 10 个随机种子取平均结果。

---

## 五、实验数量与充分性

- **主实验**：2 个不同仿真平台 × 2 个环境（训练与测试）；
- **对比实验**：3 个方法 (SCR, MCB-K2, MCB-K2-PF3)；
- **消融实验**：
  1. **碰撞预算 K**：测试 K ∈ {2, 3, 5, 10, 50}。  
  2. **姿态过滤阈值 τθ**：测试 τθ ∈ {0.5°, 1°, 2°, 3°, 10°}。  
- **结果展示**：包括学习曲线、指标表格与轨迹可视化，数据平均自多个随机种子。  
  → 实验覆盖充分、对比公平，控制变量较严谨。

---

## 六、主要结论与发现

1. **突破传统假设**：单次碰撞重置非必要，适度的多碰撞预算可加速早期学习。  
2. **最优设置**：小预算（K=2）效果最佳，训练速度提升约 40%，收敛性能更高。  
3. **姿态过滤机制**：能增加导航速度且稳定成功率，对参数不敏感，提升鲁棒性。  
4. **跨平台验证**：在 Isaac Lab 中也表现出更快的收敛与更高的导航效率，说明方法具备良好的泛化性。

---

## 七、优点与亮点

- **概念创新**：首次系统性质疑“碰撞即重置”的默认假设。  
- **实现简洁**：易于嵌入现有 DRL 框架，属于“即插即用”模块。  
- **训练稳定性**：改进早期探索效率，强化困难场景的学习。  
- **消融全面**：通过参数扫描验证各组件的贡献及稳健性。  
- **广泛验证**：测试于多个仿真与真实机器人平台，说明方法的普适性。

---

## 八、不足与局限

- **实验范围**：虽包含两大仿真平台，但缺少更大规模真实环境测试。  
- **安全性讨论不足**：论文集中于训练效率，对部署时碰撞处理的安全约束未深入探讨。  
- **预算依赖性**：虽然提出了姿态过滤缓解，但不同任务下最佳预算 K 可能需调优。  
- **经验分布风险**：多次局部探索可能仍导致经验相关性偏高，对长期稳定性影响需进一步验证。

---

**（完）**
