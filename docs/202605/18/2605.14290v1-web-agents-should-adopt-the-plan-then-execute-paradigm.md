---
title: Web Agents Should Adopt the Plan-Then-Execute Paradigm
title_zh: Web 智能体应采用“计划–执行”范式
authors: "Julien Piet, Annabella Chow, Yiwei Hou, Muxi Lyu, Sylvie Venuto, Jinhao Zhu, Raluca Ada Popa, David Wagner"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14290v1"
tags: ["query:navwm"]
score: 6.0
evidence: 智能体控制流与决策的先规划后执行范式
tldr: 本文指出当前网页智能体普遍采用的ReAct架构存在安全风险，主张将默认范式转为计划后执行。研究通过对WebArena任务的分析发现，大部分任务可在无运行时模型依赖下完成，并提出改进基础设施以提供语义明确的网页交互接口，从而实现更安全和可控的Web代理。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.14290v1/fig-001.webp\", \"caption\": \"Figure 1: ReAct exposes action selection to untrusted content, creating a direct path for prompt injections to steer the agent’s control flow. Plan-then-execute expresses tasks as programs over website SDKs, optionally using LLM subroutines to process data, isolating planning from content.\", \"page\": 2, \"index\": 1, \"width\": 814, \"height\": 433}, {\"url\": \"assets/figures/arxiv/2605.14290v1/fig-002.webp\", \"caption\": \"Table 1: Scenario viability summary over six simulated websites in the WebArena benchmark.\", \"page\": 8, \"index\": 2, \"width\": 818, \"height\": 205}, {\"url\": \"assets/figures/arxiv/2605.14290v1/fig-003.webp\", \"caption\": \"Table 2: Examples of Safe-with-Influence task patterns and their security implications.\", \"page\": 14, \"index\": 3, \"width\": 818, \"height\": 684}]"
motivation: 作者认为ReAct架构在网页环境中易受提示注入攻击，应更关注计划执行的可靠性。
method: 论文分析了WebArena基准任务，并评估了在计划后执行框架下的可行性与工具接口需求。
result: "研究发现WebArena全部任务适用于计划后执行，其中80%可纯以程序化计划完成，无需运行时LLM介入。"
conclusion: Web任务应采用计划后执行范式，而非ReAct式的即时反应模型，以提升安全性和可控性。
---

## 摘要
ReAct 已成为 LLM agents 的默认架构，许多现有的网络智能体也遵循这一范式。我们认为，这一默认设置对网络智能体而言是不合适的。相反，网络智能体应当采用“计划–执行”（plan-then-execute）的默认策略：在观察运行时网页内容之前，先为特定任务确定一个程序，然后再执行。原因在于网页内容往往混合了多个来源的输入。例如，一个电子商务产品页面可能同时包含卖家的商品信息、用户评论和赞助广告。在 ReAct 框架下，所有这些内容都会在智能体决定下一步行动时输入到模型中，从而为提示注入（prompt injection）提供了直接途径，可能影响智能体的控制流。而计划–执行范式重新划定了这一边界：不受信任的数据可以影响预定义执行图中的变量或分支，但无法重新定义用户任务或在运行时促使模型生成新的行动。我们分析了流行的网络智能体基准任务 WebArena，发现其中所有任务都与计划–执行范式兼容，其中 80% 可以通过纯程序化的计划完成，无需任何运行时 LLM 子程序。我们指出，采用计划–执行范式的主要障碍在于：要使其运作良好，工具必须能够清晰映射到语义化动作，其执行效果在执行前即可知，这样智能体才能有效规划。然而，网络本身并未自然地提供这种接口。浏览器工具如点击（click）、输入（type）、滚动（scroll）等，其含义依赖于具体页面。在这一层面进行规划是短视的：智能体只能看到当前页面的可执行动作，而后续的动作要等到执行后才会显现。弥补这一差距需要类型化接口，将网站交互从点击与按键操作转化为任务级别的操作。这是一个基础设施问题，而非建模问题。网络任务默认不需要“反应性”；它们需要类型化、完整且可审计的网站 API。

## Abstract
ReAct has become the default architecture across LLM agents, and many existing web agents follow this paradigm. We argue that it is the wrong default for web agents. Instead, web agents should default to plan-then-execute: commit to a task-specific program before observing runtime web content, then execute it. The reason is that web content mixes inputs from many parties. An e-commerce product page may combine a seller's listing, customer reviews and sponsored advertisements. Under ReAct, all of this content flows into the model when deciding on the next action, creating a direct path for prompt injections to steer the agent's control flow. Plan-then-execute changes this boundary: untrusted data may influence values or branches inside a predefined execution graph, but it cannot redefine the user task or cause the model to synthesize new actions at runtime. We analyze WebArena, a popular web agent benchmark, and find that all tasks are compatible with plan-then-execute, while 80% can be completed with a purely programmatic plan, without any runtime LLM subroutine. We identify the main barrier to adopting plan-then-execute on the web: For it to work well, tools must map cleanly to semantic actions, with effects known before execution, so agents have enough information to plan. The web does not naturally expose that interface. Browser tools such as click, type, and scroll have page-dependent meanings. Planning at this layer is near-sighted: the agent can only see actions on the current page, and later actions appear only after it acts. Closing this gap requires typed interfaces that turn website interactions from clicks and keystrokes to task-level operations. This is an infrastructure problem, not a modeling problem. Web tasks do not need reactivity by default; they need typed, complete, auditable website APIs.