---
title: Proactive Instance Navigation with Comparative Judgment for Ambiguous User Queries
title_zh: 面向模糊用户查询的比较判断主动实例导航
authors: "Junhyuk Kwon, Seungjoon Lee, Hyejin Park, Kyle Min, Jungseul Ok"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06223v2"
tags: ["query:navwm"]
score: 9.0
evidence: 智能体的实例导航策略
tldr: 本文提出ProCompNav框架，用比较判断方式在模糊用户查询下主动导航目标实例。方法通过构建候选池并基于属性值提出二元问题逐步缩小候选范围，显著提高实例辨识成功率并减少用户响应长度，在多个基准上达到了最新性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.06223v2/fig-001.webp\", \"caption\": \"Figure 7: Effect of the candidate pool size threshold Nmin that triggers the Recursive Comparison Stage. (Left) SR and SPL under different Nmin values. A larger Nmin improves comparative judgment and thus increases SR, but leads to lower SPL. (Right) Detailed results for all evaluation metrics, including user burden and navigation cost. Based on this trade-off, we choose Nmin = 5 as the default setting.\", \"page\": 15, \"index\": 1, \"width\": 820, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2605.06223v2/fig-002.webp\", \"caption\": \"Table 2: Performance on CoIN-Bench. In the Judge column, Indep/Comp indicate whether each method uses independent matching or comparative judgment for target disambiguation. The Interact column indicates whether the method interacts with the user for target disambiguation. For fair comparison across models, we include 3D-Mem∗ and AIUTA∗, our reproductions using the same MLLM and LLM as ProCompNav. Results denoted by † are taken from [Taioli et al., 2025] and those denoted by ‡ are taken from [Jang and Kim, 2026].\", \"page\": 7, \"index\": 2, \"width\": 826, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2605.06223v2/fig-003.webp\", \"caption\": \"Figure 6: Examples of multi-view candidates produced by the Pool Construction Stage. For each candidate (e.g., the bed and the desk), multiple viewpoints captured during exploration are shown.\", \"page\": 14, \"index\": 3, \"width\": 823, \"height\": 413}, {\"url\": \"assets/figures/arxiv/2605.06223v2/fig-004.webp\", \"caption\": \"Figure 5: TextNav adaptation of the Recursive Comparison Stage. In TextNav, ProCompNav preextracts an attribute set A from the text goal before the Recursive Comparison Stage and, at each round, selects the discriminative attribute that best supports the current core set Gc. Since these attributes are derived from the goal, the active set is always updated to Gc. The agent terminates once the target-selection condition is met.\", \"page\": 13, \"index\": 4, \"width\": 826, \"height\": 555}, {\"url\": \"assets/figures/arxiv/2605.06223v2/fig-005.webp\", \"caption\": \"Figure 2: Recursive Comparative Judgment. At iteration t, ProCompNav splits the candidate pool Ut into a core set Gc and a remainder set Gr by similarity. It identifies a discriminative attribute a∗t , that is common in Gc but not in Gr. Finally, it asks whether the target has a∗t , and prunes the pool to obtain the next candidate pool Ut+1 based on the user’s response.\", \"page\": 4, \"index\": 5, \"width\": 826, \"height\": 270}, {\"url\": \"assets/figures/arxiv/2605.06223v2/fig-006.webp\", \"caption\": \"Figure 4: Qualitative comparison of Independent Matching and Comparative Judgment under a minimally specified query (“Find the dresser”). Independent Matching (left) asks the user to describe an attribute of the intended target without knowing whether that attribute distinguishes it from distractors. This burdens the user with a long response and can lead the robot to mistakenly match a distractor sharing the same attribute. In contrast, Comparative Judgment (right) asks about discriminative attributes in a way that elicits only short user responses, reducing verbal burden while correctly isolating the user-intended target.\", \"page\": 9, \"index\": 6, \"width\": 701, \"height\": 383}]"
motivation: 针对模糊用户请求下实例导航中目标不唯一的问题，现有方法不能高效区分相似实例。
method: 提出ProCompNav框架，通过候选池构建与基于属性-值划分的二元提问实现高效判别。
result: ProCompNav在CoIN-Bench和TextNav上取得更高成功率和更短响应长度，优于现有基线。
conclusion: 比较判断式主动实例导航能有效减少用户交互负担，提高目标辨识的成功率。
---

## 摘要
当用户最初的请求无法唯一确定目标实例时，自然语言实例导航会变得具有挑战性。一个实用的智能体应通过主动询问仅区分目标与相似干扰项所需的信息来减轻用户负担，而不是一开始就要求详细的描述。现有方法往往难以实现这一目标：它们可能在发现第一个似乎合理的候选项后就停止，而未充分探索替代选项；或者，即使收集了多个候选项，也会询问关于单个候选项属性的问题，而非根据候选集合选择区分性问题。其结果是，尽管进行了对话，智能体仍可能无法有效区分目标与干扰项，导致过早决策与冗长的用户回答。我们提出了一种名为 Proactive Instance Navigation with Comparative Judgment（ProCompNav）的两阶段框架：首先构建候选池，然后通过比较判断识别目标。在每一轮中，ProCompNav 提取一个可将当前候选池划分的属性-值对，提出一个是/否问题，并一次性剔除所有不一致的候选项。该方法将消歧任务从开放式目标描述重构为候选池层面的判别式提问，其中每一个问题都旨在缩小候选集合。在 CoIN-Bench 上，ProCompNav 在使用相同最小输入的交互式基线和基于详细描述的非交互式基线上均显著提高了成功率（Success Rate），同时大幅减少了响应长度（Response Length）。ProCompNav 还在 TextNav 上达到了最新的成功率水平，表明比较判断在具有相似干扰项的实例级导航任务中具有广泛适用性。

## Abstract
Natural-language instance navigation becomes challenging when the initial user request does not uniquely specify the target instance. A practical agent should reduce the user's burden by actively asking only the information needed to distinguish the target from similar distractors, rather than requiring a detailed description upfront. Existing approaches often fall short of this goal: they may stop at the first plausible candidate before sufficiently exploring alternatives, or, even after collecting multiple candidates, ask about the target's attributes derived from individual candidates rather than questions selected to distinguish candidates in the pool. As a result, despite the dialogue, the agent may still fail to distinguish the target from distractors, leading to premature decisions and lengthy user responses. We propose Proactive Instance Navigation with Comparative Judgment (ProCompNav), a two-stage framework that first constructs a candidate pool and then identifies the target through comparative judgment. At each round, ProCompNav extracts an attribute-value pair that splits the current pool, asks a binary yes/no question, and prunes all inconsistent candidates at once. This reframes disambiguation from open-ended target description to pool-level discriminative questioning, where each question is chosen to narrow the candidate set. On CoIN-Bench, ProCompNav improves Success Rate over interactive baselines with the same minimal input and non-interactive baselines with detailed descriptions, while substantially reducing Response Length. ProCompNav also achieves state-of-the-art Success Rate on TextNav, suggesting that comparative judgment is broadly useful for instance-level navigation among similar distractors.

---

## 论文详细总结（自动生成）

# 面向模糊用户查询的比较判断主动实例导航 — 论文总结

---

## 一、研究核心问题与背景

- **问题背景**  
  在自然语言驱动的实例导航任务中，智能体需要根据用户的语言指令（如“找到柜子”）在三维环境中找到目标实例。但现实中的用户请求通常**模糊或不唯一**，难以仅凭初始描述确定目标。

- **现有方法的不足**  
  - 多数工作假设用户能提供**详细且排他性**的描述；  
  - 独立匹配（Independent Matching）策略无法区分视觉上相似的干扰项，易导致**过早判断或错误选择**；  
  - 即使采用延迟决策策略，也倾向于询问非判别性问题，造成**用户回答冗长、识别仍不精确**。

- **研究目标**  
  本文旨在设计一个**主动、判别式的交互导航框架**，让智能体在最少用户输入下，通过高效提问**逐步排除干扰项、确认目标实例**，以减少用户负担并提升导航成功率。

---

## 二、方法论：ProCompNav 框架

### 1. 总体思路
ProCompNav（Proactive Instance Navigation with Comparative Judgment）是一种**两阶段的零样本框架**：

1. **候选池构建（Pool Construction Stage）**：探索环境获取同类别候选实例；
2. **递归比较判断（Recursive Comparative Judgment, RCJ）**：基于候选间对比提出判别性问题，逐轮缩小搜索空间。

### 2. 核心机制
- **阶段一：候选池构建**
  - 通过视觉检测与3D点云聚合收集同类候选；
  - 使用多视图拼图（multi-view collage）生成图像；
  - 调用多模态大模型（MLLM，Qwen3-VL-8B）生成每个候选的**多视图文字描述**；
  - 当候选数≥N_min（默认5）时，转入下阶段。

- **阶段二：递归比较判断（RCJ）**
  - 当前候选集为 \(U_t\)，被划分为核心集 \(G_c\) 与剩余集 \(G_r\)：
    - 使用文本与图像嵌入相似度 \(S(i,j)\)；
    - 采用改进的贪心“peeling”算法选择语义最一致的核心集；
  - 从核心集中提取属性候选集 A；
  - 利用自然语言推理模型（DeBERTa-v3-large）打分，计算属性的**蕴含得分**：
    \[
    a_t^* = \arg\max_a \big( \mathbb{E}_{i\in G_c}[s(d_i,a)] - \mathbb{E}_{j\in G_r}[s(d_j,a)] \big)
    \]
  - 选出区分度最大的属性值对 \(a_t^*\)，向用户提问是/否问题，例如：“目标是否在电视旁？”；
  - 根据回答更新候选集：若回答Yes则保留 \(G_c\)，否则保留 \(G_r\)；
  - 循环直到剩下单一候选或需要新探索。

- **特殊机制补充**
  - **属性阈值再分组（Refinement）**：修正残留候选；  
  - **重新探索机制**：当池中无有效候选时重新补充样本；  
  - **非交互适配（TextNav）**：直接从文本目标中提取判别属性，无需用户反馈。

---

## 三、实验设计

### 1. 基准与场景
- **CoIN-Bench**（Taioli et al., 2025）  
  - 模糊查询环境，仅给出类别名称；
  - 有三个划分：Val Seen, Val Seen Synonyms, Val Unseen；
  - 使用多实例3D场景与模拟用户交互的MLLM。
- **TextNav**（Sun et al., 2024）  
  - 提供详细目标描述，无需互动。

### 2. 对比方法
| 类型 | 基线方法 | 特性 |
|------|------------|------|
| 训练型 | Monolithic-GOAT, PSL | 端到端导航模型 |
| 训练无关型（有描述） | 3D-Mem, Context-Nav | 使用详细描述，较强先验 |
| 训练无关型（仅类别） | VLFM, AIUTA, Pooled Independent Matching | 交互/非交互模糊输入 |
| 本文方法 | ProCompNav | 比较判断 + 二元交互 |

### 3. 评价指标
- **成功率（SR）**、**SPL**（路径效率）；
- **交互场景**：统计平均响应长度（RL）与问题数（NQ）。

---

## 四、资源与算力

- 使用 **两张 NVIDIA RTX 3090 GPU（24GB）**；
- 每场景均在模拟环境中运行，无需额外训练；
- 样本推理过程中主要耗时部分为多模态模型推理（MLLM）。
  - AIUTA* 平均227s／episode；
  - ProCompNav 仅82.89s／episode（减少63.5%）。

---

## 五、实验数量与充分性

- **主实验：**  
  - CoIN-Bench 全部三个split + TextNav，涵盖交互与非交互任务。
- **消融实验：**  
  - 四项关键组件消融（多视图聚合、NLI验证、核心集选择、属性再分组）；  
  - 参数敏感性实验：最小候选池大小 N_min 与阈值 τ 的变化。
- **性能分析：**  
  - 终止步分布分析（与AIUTA对比）；  
  - 定性案例展示（如 “Find the dresser” 场景）。
- **实验充分性评价：**  
  - 覆盖全面、与强基线公平对比；  
  - 使用相同模型权重（Qwen3-VL-8B）确保可比性；
  - 未报告真实人类用户实验（仅MLLM模拟）。

---

## 六、主要结论与发现

- **性能**：  
  - CoIN-Bench “Val Seen Synonyms” 上 SR≈28.1%，较AIUTA*提升近13%；  
  - 平均用户响应长度 RL 从 109.5 降至 4.2 tokens；
  - TextNav 非交互任务上 SR=28.5%，达到SOTA。
- **结论要点：**
  - 比较判断能有效取代独立匹配；
  - 建立候选池并进行组间区分优于单实例推理；
  - 是/否二元提问极大减轻用户负担；
  - 框架具备从模糊到详细场景的**跨场景泛化能力**。

---

## 七、优点与创新点

- **方法创新**
  - 将目标判别视为**候选池级别的比较判断（pool-level comparative reasoning）**；
  - 引入自然语言推理模型量化属性对比；
  - 采用贪心相似度“核心集”提取增强判别稳定性。
- **交互效率**
  - 每轮问题可同时剔除多个干扰项；
  - 用户交互简短且结构化（是/否响应）。
- **通用性**
  - 框架可在“交互／非交互”、“模糊／详细描述”之间自然迁移；
  - 无需额外训练，具零样本泛化特性。
- **工程性亮点**
  - 相比AIUTA减少63%以上多模态推理时间；
  - 较好平衡成功率、导航效率与交互成本。

---

## 八、不足与局限

- **实验限制**
  - 全部在仿真环境中完成，**缺乏真实物理机器人验证**；
  - 用户交互由 MLLM 模拟，尚未证实在人类用户场景中的效果。
- **方法依赖**
  - 候选池需达到最小数量（N_min），当环境中同类对象稀少时效率下降；
  - 若属性提取或NLI判断出错，可能引发错误剪枝。
- **伸缩性问题**
  - 在更复杂多类别环境中可能计算开销上升；
  - 依赖LLM/MM模型调用，推理延迟仍高于纯学习策略。

---

**（完）**
