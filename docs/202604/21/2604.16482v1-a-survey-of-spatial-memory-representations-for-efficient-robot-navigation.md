---
title: A Survey of Spatial Memory Representations for Efficient Robot Navigation
title_zh: 空间记忆表示的综述：高效机器人导航的研究
authors: "Ma. Madecheen S. Pangaliman, Steven S. Sison, Erwin P. Quilloy, Rowel Atienza"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.16482v1"
tags: ["query:navwm"]
score: 9.0
evidence: 机器人导航空间记忆表示综述
tldr: 该论文综述了视觉导航机器人在大尺度环境中空间记忆的高效表示问题，系统评估了88篇研究与52个系统的内存效率，提出α比率衡量运行内存与地图存储的差距，并建立标准化评估协议，帮助研究者评估不同方法的部署可行性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.16482v1/fig-001.webp\", \"caption\": \"Figure 1. Evolution of spatial memory representations along two competing demands: geometric completeness and memory efficiency. Sparse features (1) traded completeness for efficiency; neural methods (2) recovered completeness via learned compression; scene graphs (3) added semantic abstraction. The scene graph box is dashed to indicate that the 48 MB figure reflects only the graph abstraction layer [26]; the required metric-semantic backend [54] adds scene-dependent cost (dashed arrow), shifting the true system footprint leftward. The scene graph’s vertical position reflects its semantic completeness (objects, rooms, places) rather than geometric fidelity.\", \"page\": 1, \"index\": 1, \"width\": 487, \"height\": 307}, {\"url\": \"assets/figures/arxiv/2604.16482v1/fig-002.webp\", \"caption\": \"Figure 3. Accuracy vs. map size (Mmap) by benchmark. Left: Sparse (EuRoC, linear scale). Right: Neural (Replica, log scale); dashed = Pareto front. Lower-left is better. Point-SLAM’s measured 2.9 GB checkpoint (vs. 80 MB survey estimate) moves it far right, off the Pareto front. Plotting Mpeak instead would further shift high-α systems rightward.\", \"page\": 6, \"index\": 2, \"width\": 489, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2604.16482v1/fig-003.webp\", \"caption\": \"Figure 2. Taxonomy of spatial memory representations with representative citations and typical efficiency metrics (gray). ATE = absolute trajectory error on standard benchmarks; map sizes are representative for building-scale environments. 3DGS = 3D Gaussian Splatting. Semantic maps include open-vocabulary approaches leveraging vision-language models such as CLIP.\", \"page\": 2, \"index\": 3, \"width\": 1036, \"height\": 356}, {\"url\": \"assets/figures/arxiv/2604.16482v1/fig-004.webp\", \"caption\": \"Figure 4. Runtime GPU memory on Replica/room0 (1 Hz sampling, A100-SXM4-80GB, baseline subtracted). All four systems independently measured (⋄). Co-SLAM stays flat at ∼1.3GB (bounded hash). NICE-SLAM oscillates between 3–7 GB due to periodic global mapping passes (absolute peak 10.1 GB, Table 3). Point-SLAM stabilizes at 5–6 GB after initialization. SplaTAM grows monotonically to ∼12.7GB as Gaussians accumulate (peak 14.0 GB).\", \"page\": 7, \"index\": 4, \"width\": 481, \"height\": 323}, {\"url\": \"assets/figures/arxiv/2604.16482v1/fig-005.webp\", \"caption\": \"Table 5. Memory dynamics taxonomy. Consol. = consolidation, Hier = hierarchical, Mono = monolithic. ∆M = reported memory reduction. α from Table 3 where available; note that forgetting reduces Mmap but not α.\", \"page\": 7, \"index\": 5, \"width\": 496, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2604.16482v1/fig-006.webp\", \"caption\": \"Table 2. Neural SLAM on Replica (synthetic). Eff. = 103/(ATE× Mmap), higher is better. †: CVPR/ECCV 2024 or later.\", \"page\": 4, \"index\": 6, \"width\": 496, \"height\": 421}, {\"url\": \"assets/figures/arxiv/2604.16482v1/fig-007.webp\", \"caption\": \"Table 3. Unified efficiency analysis across spatial memory paradigms. Overhead factor α = Mpeak/Mmap; αCPU denotes CPU RSS, αGPU denotes GPU allocation (see Section 4). Cross-benchmark caveat: sparse systems are evaluated on EuRoC (real-world, stereo-inertial) and neural systems on Replica (synthetic, RGB-D); ATE and memory values are not directly comparable across horizontal dividers. Within-benchmark comparisons are valid; cross-paradigm conclusions should be drawn from α and scaling behavior. Per-value provenance notes are in Tables 1 and 2.\", \"page\": 5, \"index\": 7, \"width\": 1036, \"height\": 689}]"
motivation: 视觉机器人在大规模环境中导航时会消耗大量内存资源，限制了其在嵌入式平台上的部署。
method: 作者建立了统一评估框架，包括内存增长率、查询延迟和性能退化等指标，并进行独立配置分析。
result: 研究通过分析52个系统（1989-2025）的空间记忆策略，提出了α比率指标来衡量运行期内存与存储地图的差距。
conclusion: 研究提供了首个独立测得的α参考值和α感知预算算法，为未来机器人地图存储设计提供量化标准。
---

## 摘要
随着基于视觉的机器人在更大环境中进行导航，其空间记忆会无限增长，最终耗尽计算资源，特别是在嵌入式平台（8–16GB 共享内存、功耗 <30W）上，无法通过增加硬件来解决。本综述针对空间记忆效率问题进行研究，涵盖自 1989 至 2025 年的 88 篇文献与 52 个系统，从占用栅格（occupancy grids）到神经隐式表示（neural implicit representations）。我们引入了 $α = M_{\text{peak}} / M_{\text{map}}$，即峰值运行时内存（操作期间使用的总 RAM 或 GPU 内存）与保存的地图大小（持久化写入磁盘的检查点）之比，用以揭示已发表地图大小与实际部署成本之间的差距。基于 NVIDIA A100 GPU 的独立性能分析显示，仅在神经方法内部，$α$ 可跨越两个数量级，从 2.3（Point-SLAM）到 215（NICE-SLAM，其 47MB 地图运行时需占用 10GB 内存），说明决定部署可行性的关键在于内存架构而非方法类别标签。我们提出了一个标准化评估协议，包括内存增长率、查询延迟、内存完备性曲线以及吞吐量退化，这些指标目前的基准测试均未涵盖。通过具有显式基准区分的帕累托前沿分析，我们表明不存在在其评估域内绝对占优的范式：3DGS 方法在 Replica 数据集上以 90–254MB 的地图大小实现了最佳的绝对精度，而场景图（scene graphs）则以可预测的成本提供语义抽象。我们首次提供了独立测得的 $α$ 参考值，并提出了一个基于 $α$ 的预算算法，使研究人员能够在实现前评估目标硬件上的部署可行性。

## Abstract
As vision-based robots navigate larger environments, their spatial memory grows without bound, eventually exhausting computational resources, particularly on embedded platforms (8-16GB shared memory, $<$30W) where adding hardware is not an option. This survey examines the spatial memory efficiency problem across 88 references spanning 52 systems (1989-2025), from occupancy grids to neural implicit representations. We introduce the $α= M_{\text{peak}} / M_{\text{map}}$, the ratio of peak runtime memory (the total RAM or GPU memory consumed during operation) to saved map size (the persistent checkpoint written to disk), exposing the gap between published map sizes and actual deployment cost. Independent profiling on an NVIDIA A100 GPU reveals that $α$ spans two orders of magnitude within neural methods alone, ranging from 2.3 (Point-SLAM) to 215 (NICE-SLAM, whose 47,MB map requires 10GB at runtime), showing that memory architecture, not paradigm label, determines deployment feasibility. We propose a standardized evaluation protocol comprising memory growth rate, query latency, memory-completeness curves, and throughput degradation, none of which current benchmarks capture. Through a Pareto frontier analysis with explicit benchmark separation, we show that no single paradigm dominates within its evaluation regime: 3DGS methods achieve the best absolute accuracy at 90-254,MB map size on Replica, while scene graphs provide semantic abstraction at predictable cost. We provide the first independently measured $α$ reference values and an $α$-aware budgeting algorithm enabling practitioners to assess deployment feasibility on target hardware prior to implementation.

---

## 论文详细总结（自动生成）

# 空间记忆表示的综述：高效机器人导航的研究 — 中文结构化总结

---

## 一、研究核心问题与整体含义

- **核心问题**：视觉导航机器人在大规模、长时间任务中面临空间记忆不断增长的问题，导致运行内存溢出、实时性下降和嵌入式平台（8–16GB RAM、功耗 <30W）无法部署。  
- **研究背景**：传统 SLAM（Simultaneous Localization and Mapping）系统虽取得显著进展，但随着环境规模扩展，系统的“空间记忆”（地图存储）成为瓶颈。不同的表示方式（如稠密栅格、稀疏特征、神经隐式或场景图）在**几何完整性**与**内存效率**之间存在显著权衡。  
- **论文目标**：建立统一评价框架，量化不同空间记忆表示的真实内存成本，提出能指导机器人在算力有限硬件上部署的可行性准则。  

---

## 二、方法论与核心思想

### 1. 引入关键指标：**内存开销因子 α**
- 定义公式：  
  \[
  α = \frac{M_{\text{peak}}}{M_{\text{map}}}
  \]
  其中：
  - \( M_{\text{peak}} \)：运行时峰值内存（RAM 或 GPU 显存占用）  
  - \( M_{\text{map}} \)：保存于磁盘的地图数据总大小  
- **核心思想**：通过 α 量化运行开销与地图存储的偏差，揭示实测部署成本与论文报告值的差距。

### 2. 标准化评估协议
作者提出四项互补指标，以扩展现有 benchmark 的单维度评价：
1. **内存增长率** \( dM/dt \)：区分受限与无限增长系统。  
2. **查询延迟**：比较从地图中进行空间查询的速度。  
3. **内存–完备性曲线**：将地图大小与几何/语义重建的 F1 分数关联。  
4. **吞吐量退化**：考察随地图增大时 FPS 的下降。

### 3. α 感知预算算法（Algorithm 1）
- 输入：硬件可用内存 \( M_{\text{budget}} \)，目标任务地图上限 \( M_{\text{target}} \)。  
- 计算：
  \[
  M_{\text{map}}^{\max} = \frac{M_{\text{budget}}}{α}
  \]
- 输出：推荐的地图大小与可用表示类别（参照 Table 4 指导不同硬件选型）。  
- 意义：在实现前即可预估可部署规模，避免硬件溢出。

### 4. 系统比较框架
- 将 52 个系统分为四大类：
  - 稠密映射（Occupancy / Octree / TSDF）
  - 稀疏特征 SLAM（ORB-SLAM、Basalt、VINS-Mono）
  - 神经映射（NeRF、3D Gaussian Splatting）
  - 分层与语义表示（Scene Graphs、CLIP 特征图）

---

## 三、实验设计与评估场景

- **数据集**：
  - *EuRoC*（真实场景，视觉–惯性）用于传统稀疏 SLAM。
  - *Replica*（合成室内场景，RGB-D）用于神经 SLAM 系列。
  - 其他如 *TUM RGB-D*、城市类序列仅用于拓展讨论（未做统一对比）。

- **主要对标方法**：  
  - 稀疏类：ORB-SLAM3、VINS-Mono、Basalt。  
  - 神经类：iMAP、Co-SLAM、NICE-SLAM、Point-SLAM、SplaTAM、GS-SLAM、SGS-SLAM。  
  - 分层类：Hydra、Kimera-Multi、SNI-SLAM 等。

- **结合对比指标**：
  - 绝对轨迹误差（ATE, cm）与峰值内存（MB）。  
  - PSNR 评价重建质量（Replica）。  
  - α 值的跨方法比较（见 Table 3、Fig. 3）。  

---

## 四、资源与算力配置

- **显式说明**：
  - 独立性能分析在 **NVIDIA A100-SXM4-80GB GPU** 上进行。
  - 内存采样频率：1 Hz，通过 `nvidia-smi` 测量峰值显存。
  - 所有神经实现仅单卡测试，未报道训练时长或批量规模。  

- **隐含信息**：
  - 未提供多 GPU 或训练时长细节；推测为线上实验的推理阶段开销。
  - 部分结果与文献报告存在检查点大小差异（8 vs. 32MB 等），说明测量过程有独立验证。

---

## 五、实验数量与充分性

- 共汇总 **88 篇文献、52 个系统**（其中 15 个具可比内存数据）。  
- 独立实测了五个神经 SLAM 系统（Co-SLAM、NICE-SLAM、Point-SLAM、SplaTAM、SGS-SLAM）。  
- 附加分析包括：
  - 内存时间序列（Fig. 4）
  - 多指标帕累托前沿比较（Fig. 3）  
- 实验覆盖丰富但：  
  - 使用单 GPU、单场景（Replica Room0），缺乏跨环境验证。  
  - 训练时与推理时未分离测量，影响公平性。  

---

## 六、主要结论与发现

1. **地图大小并非部署成本的可靠预测指标**。  
   α 在神经方法中跨度达两个数量级（2.3–215），差异可超过 100 倍运行内存。  
2. **系统类型区别显著**：
   - 稀疏 SLAM：低 α（≈4），内存可靠可控。  
   - 神经隐式法：低存储但高运行时内存（NICE-SLAM、Co-SLAM）。  
   - 稠密点法（Point-SLAM）：高地图成本但低 α。  
3. **帕累托分析结果**：无单一范式在精度与效率上全面占优，3DGS 方法在 90–254MB 范围内最具均衡性。  
4. **提出 α-aware 部署框架**：在硬件限制（如 Jetson Orin NX）下，预测最大可行地图大小（75–290MB）。  

---

## 七、优点与亮点

- **创新指标**：首次提出并实测跨范式内存开销因子 α，填补长期缺失的运行时评估维度。  
- **量化指导性强**：α-aware 预算算法可实际用于硬件规划。  
- **标准化建议**：建立四维评估协议，为未来长时任务 benchmark 提供方向。  
- **独立测量验证**：重新校正文献中错误或不一致的内存数据，提升可靠性。  
- **跨代视角完整**：从 1989 到 2025，覆盖稠密、稀疏、神经、语义四大演化阶段。

---

## 八、不足与局限

- **实验单一性**：独立分析仅覆盖单 Replica 场景与一块 GPU，泛化性有限。  
- **跨范式不完全公平**：EuRoC 与 Replica 数据集差异较大，ATE 与内存不可直接比较。  
- **动态环境与多机器人场景未充分实验**，如协作或变化环境下的 α 行为未量化。  
- **训练时开销未区分**：未测离线或推理专用配置，影响对部署阶段的真实预测。  
- **语义/语言模型部分**：CLIP 等特征引入的额外内存消耗仅理论讨论，缺乏实测数据。  

---

（完）
