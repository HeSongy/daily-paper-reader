---
title: "Efficient Rationale-based Retrieval: On-policy Distillation from Generative Rerankers based on JEPA"
title_zh: 高效的基于推理的检索：来自基于 JEPA 的生成式重排序器的在线策略蒸馏
authors: "Teng Chen, Sheng Xu, Feixiang Guo, Xiaoyu Wang, Qingqing Gu, Hongyan Li, Luo Ji"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23336v1"
tags: ["query:navwm"]
score: 6.0
evidence: 基于 JEPA（联合嵌入预测架构）的生成式重排序器蒸馏
tldr: 本文提出了高效的基于推理的检索模型Rabtriever，通过从生成式重排序器进行策略蒸馏并结合JEPA架构，实现独立编码查询与文档，同时保持跨语义理解能力，有效降低计算复杂度并在多种推理与传统检索任务中取得优异表现。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23336v1/fig-001.webp\", \"caption\": \"Table 2: Performances on three rationale-based retrieval tasks. ‘R’ denotes Recall. LaHoRe performs better RankLLaMA as reankers; Rabtriever is then distilled from LaHoRe and achieves the best accuracy among retrievers. Bold indicates the best result, while underline indicates the second best. Numbers are in percentages.\", \"page\": 6, \"index\": 1, \"width\": 872, \"height\": 361}, {\"url\": \"assets/figures/arxiv/2604.23336v1/fig-002.webp\", \"caption\": \"Figure 1: Top: By effective distillation, Rabtriever achieves a similar accuracy to Reranker and a similar speed to Retriever. Bottom: The teacher has two peaks (<T> and <F>) after finetuning; forward KL tends to learn averaged behaviors, while reverse KL helps the student seek the most typical mode [5].\", \"page\": 2, \"index\": 2, \"width\": 472, \"height\": 295}, {\"url\": \"assets/figures/arxiv/2604.23336v1/fig-003.webp\", \"caption\": \"Figure 4: Averaged Online RTs corresponding to different 𝐿𝐷 .\", \"page\": 7, \"index\": 3, \"width\": 488, \"height\": 285}, {\"url\": \"assets/figures/arxiv/2604.23336v1/fig-004.webp\", \"caption\": \"Table 4: Out of domain performance on BEIR (nDCG@10). Best value in the row is bolded. The best result is marked bolded and the second best is underlined. Compared to the best reranker LaHoRe, Rabtriever obtains averagely 58.2, and performs the best in 2/13 subtasks and second-best in 4/13 subtasks, which is close to the best retriever competitor (GritLM).\", \"page\": 8, \"index\": 4, \"width\": 960, \"height\": 446}, {\"url\": \"assets/figures/arxiv/2604.23336v1/fig-005.webp\", \"caption\": \"Figure 6: MRR comparison of models on MS MARCO passage retrieval.\", \"page\": 8, \"index\": 5, \"width\": 778, \"height\": 356}, {\"url\": \"assets/figures/arxiv/2604.23336v1/fig-006.webp\", \"caption\": \"Figure 3: Pipeline to develop Rabtriever. I: A teacher cross-encodes query (𝑄) and document (𝐷), then generates the retrieval score (𝑠). We train it by finetuning an LLM on question-answering samples using the cross-entropy (CE) loss (L). II: We initialize the student from teacher, with parameters frozen. A predictor is then inserted between LLM layers and heads, consisting of an MLP and a multiplication operator. III: The predictor converts the 𝑄 embedding into a new embedding, with the 𝐷 embedding as the latent control variable (𝑧). The predicted 𝑄 embedding is then fed into the head, which produces the relevance score for student. Both embedding and scoring differences between teacher and student are minimized (F ).\", \"page\": 4, \"index\": 6, \"width\": 1029, \"height\": 623}]"
motivation: 现有推理型检索方法需昂贵的交叉编码计算，亟需提升效率同时保留跨语义理解能力。
method: 采用基于LLM的生成式重排序器作为教师，通过JEPA范式与反向KL损失进行策略蒸馏，以构造高效的独立编码检索器。
result: 实验证明Rabtriever在共情对话、机器人操作及标准检索数据集上均优于现有检索基线，性能与重排序器相近。
conclusion: Rabtriever在保持高效性的同时，实现了与生成式重排序器近似的性能，是一种可泛化的推理型检索新方案。
---

## 摘要
与传统的基于事实的检索不同，基于推理的检索通常需要利用大型语言模型对查询–文档对进行跨编码，从而导致巨大的计算成本。为了解决这一限制，我们提出了 Rabtriever，它能够独立地编码查询和文档，同时提供与重排序器相当的跨查询–文档理解能力。我们的起点是训练一个基于大型语言模型（LLM）的生成式重排序器，它先输入文档，再输入查询，并通过对数概率生成相关性分数。随后，我们将其作为教师模型，在一个在线策略蒸馏框架中以 Rabtriever 作为学生模型，用于重建教师的具备上下文感知的查询嵌入。为实现这一目标，Rabtriever 首先从教师模型初始化，参数保持冻结。之后引入联合嵌入预测架构（Joint-Embedding Predictive Architecture, JEPA）范式，在 LLM 层与头之间集成轻量的可训练预测器，将查询嵌入投射到新的隐空间，以文档嵌入作为潜在向量。JEPA 最小化投射嵌入与教师嵌入之间的分布差异。为了提高在线策略蒸馏的采样效率，我们还在 LLM logits 的反向 KL 上添加了辅助损失，以重塑学生模型的 logit 分布。Rabtriever 将教师模型在文档长度上的二次复杂度优化为线性，在理论和实证上都得到了验证。实验结果表明，Rabtriever 在多种基于推理的任务中（包括共情对话和机器人操作）超越了不同的检索基线，其准确率相较重排序器仅有轻微下降。Rabtriever 在传统检索基准（如 MS MARCO 和 BEIR）上也展现了良好的泛化能力，表现与最优的检索器基线相当。

## Abstract
Unlike traditional fact-based retrieval, rationale-based retrieval typically necessitates cross-encoding of query-document pairs using large language models, incurring substantial computational costs. To address this limitation, we propose Rabtriever, which independently encodes queries and documents, while providing comparable cross query-document comprehension capabilities to rerankers. We start from training a LLM-based generative reranker, which puts the document prior to the query and prompts the LLM to generate the relevance score by log probabilities. We then employ it as the teacher of an on-policy distillation framework, with Rabtriever as the student to reconstruct the teacher's contextual-aware query embedding. To achieve this effect, Rabtriever is first initialized from the teacher, with parameters frozen. The Joint-Embedding Predictive Architecture (JEPA) paradigm is then adopted, which integrates a lightweight, trainable predictor between LLM layers and heads, projecting the query embedding into a new hidden space, with the document embedding as the latent vector. JEPA then minimizes the distribution difference between this projected embedding and the teacher embedding. To strengthen the sampling efficiency of on-policy distillation, we also add an auxiliary loss on the reverse KL of LLM logits, to reshape the student's logit distribution. Rabtriever optimizes the teacher's quadratic complexity on the document length to linear, verified both theoretically and empirically. Experiments show that Rabtriever outperforms different retriever baselines across diverse rationale-based tasks, including empathetic conversations and robotic manipulations, with minor accuracy degradation from the reranker. Rabtriever also generalizes well on traditional retrieval benchmarks such as MS MARCO and BEIR, with comparable performance to the best retriever baseline.