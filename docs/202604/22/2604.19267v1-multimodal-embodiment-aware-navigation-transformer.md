---
title: Multimodal embodiment-aware navigation transformer
title_zh: 多模态具身感知导航 Transformer
authors: "Louis Dezons, Quentin Picard, Rémi Marsal, François Goulette, David Filliat"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19267v1"
tags: ["query:navwm"]
score: 9.0
evidence: 用于目标条件导航的多模态具身感知导航Transformer
tldr: 本文提出了ViLiNT，一种多模态、具身感知的导航Transformer模型，通过融合RGB图像、3D激光点云、目标嵌入和机器人特征，实现更稳健的目标导航。模型结合Transformer与扩散模型生成轨迹，并通过路径清 clearance预测提升避障能力，显著提高了在不同环境下的导航成功率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19267v1/fig-001.webp\", \"caption\": \"Fig. 1: Overall architecture of our navigation model. Highlighted blocks in red indicate our main contributions. See text for details.\", \"page\": 1, \"index\": 1, \"width\": 429, \"height\": 385}, {\"url\": \"assets/figures/arxiv/2604.19267v1/fig-002.webp\", \"caption\": \"Fig. 6: Zero-shot deployment of ViLiNT (green curves) and NoMaD-FT (Red curves). We compare the capability of both solutions to prevent collision occurrences while navigating our robot towards 2D goals in environments and obstacles unknown from our training data. Goals are set to be 30 m away from the initial robot’s position.\", \"page\": 7, \"index\": 2, \"width\": 1022, \"height\": 295}, {\"url\": \"assets/figures/arxiv/2604.19267v1/fig-003.webp\", \"caption\": \"Fig. 7: Simulated experience showing how changing the robot’s size input influences the navigation behavior.\", \"page\": 7, \"index\": 3, \"width\": 488, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2604.19267v1/fig-004.webp\", \"caption\": \"Fig. 2: Global model architecture. See text for details.\", \"page\": 3, \"index\": 4, \"width\": 1076, \"height\": 391}, {\"url\": \"assets/figures/arxiv/2604.19267v1/fig-005.webp\", \"caption\": \"Fig. 3: Illustration of our LiDAR tokenization approach: PointTransformerV3 features are aggregated in a cylindrical voxel grid using Generalized Mean Pooling to produce tokens. Empty voxel tokens are represented by a learned feature vector.\", \"page\": 4, \"index\": 5, \"width\": 434, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2604.19267v1/fig-006.webp\", \"caption\": \"Fig. 4: Example clearance computation from Scand [23]. It is defined for each segment linking two consecutive waypoints as the shortest distance to an obstacle.\", \"page\": 5, \"index\": 6, \"width\": 434, \"height\": 375}]"
motivation: 现有基于监督学习的导航模型在环境或传感器变化下避障能力下降，需提升鲁棒性。
method: 模型融合视觉和激光雷达信息，用Transformer生成条件轨迹并通过扩散模型与路径评分机制优化导航。
result: "ViLiNT在三种模拟环境中平均成功率提升166%，并在真实场地验证了性能优势。"
conclusion: 多模态融合和具身感知机制有效提升了机器人在复杂环境中的导航鲁棒性。
---

## 摘要
通过监督学习训练的地面机器人目标条件导航模型在零样本迁移方面表现出良好潜力，但其避障能力在分布偏移（即环境、机器人或传感器配置变化）下仍会退化。我们提出 ViLiNT，一种用于目标导航的多模态注意力机制策略，在来自多个平台和环境的异质数据上进行训练，并通过两个关键特性提升鲁棒性。首先，我们将 RGB 图像、3D LiDAR 点云、目标嵌入向量以及机器人体态描述符在 Transformer 架构中进行融合，以捕捉几何与外观的互补特征。Transformer 的输出用于调节扩散模型（diffusion model），生成可导航的轨迹。其次，利用自动生成的离线标签，我们训练一个路径清晰度预测头，用于对扩散模型生成的轨迹进行评分与排序。扩散调节以及轨迹排序头均依赖于机器人体态标识符，使模型能够根据机器人体型生成并选择合适的轨迹。在三个仿真环境中，ViLiNT 将成功率（Success Rate）平均提升了 166%，相较于等效的视觉-only 先进基线模型（NoMaD）。这一性能提升亦通过实际部署于障碍场中的探测车导航实验得到验证。这些结果表明，将多模态融合与碰撞预测机制结合，可以显著提升越野导航的鲁棒性。

## Abstract
Goal-conditioned navigation models for ground robots trained using supervised learning show promising zero-shot transfer, but their collision-avoidance capability nevertheless degrades under distribution shift, i.e. environmental, robot or sensor configuration changes. We propose ViLiNT a multimodal, attention-based policy for goal navigation, trained on heterogeneous data from multiple platforms and environments, which improves robustness with two key features. First, we fuse RGB images, 3D LiDAR point clouds, a goal embedding and a robot's embodiment descriptor with a transformer architecture to capture complementary geometry and appearance cues. The transformer's output is used to condition a diffusion model that generates navigable trajectories. Second, using automatically generated offline labels, we train a path clearance prediction head for scoring and ranking trajectories produced by the diffusion model. The diffusion conditioning as well as the trajectory ranking head depend on a robot's embodiment token that allows our model to generate and select trajectories with respect to the robot's dimensions. Across three simulated environments, ViLiNT improves Success Rate on average by 166\% over equivalent state-of-the-art vision-only baseline (NoMaD). This increase in performance is confirmed through real-world deployments of a rover navigating in obstacle fields. These results highlight that combining multimodal fusion with our collision prediction mechanism leads to improved off-road navigation robustness.

---

## 论文详细总结（自动生成）

# 多模态具身感知导航 Transformer（ViLiNT）论文总结

---

## 1. 核心问题与研究动机

- **研究背景**：  
  当前地面机器人在复杂、非结构化（尤其是越野）环境中的自主导航仍面临巨大挑战。传统的基于可行性地图和局部规划器的导航框架虽然具备可解释性和一定的鲁棒性，但在换平台或传感器配置变化时常需要大量重新标定或再训练。

- **问题痛点**：  
  基于监督学习的目标条件导航模型（Goal-conditioned navigation）在零样本迁移中表现良好，但在遇到环境或机器人配置分布偏移时，其避障能力明显退化。视觉单模态模型在感知可靠性和几何推理方面存在局限。

- **研究目标**：  
  本文旨在构建一种多模态（融合视觉和激光雷达）且具身感知（考虑不同机器人尺寸与形态）的通用导航策略，兼顾**鲁棒性**与**安全性**，能够在未知环境下实现零样本导航与稳健避障。

---

## 2. 方法论与核心技术

### 核心思想
提出 **ViLiNT (Vision-LiDAR Navigation Transformer)**，一种基于 Transformer 的多模态融合与扩散生成框架，包含：
1. 多模态 Token 融合（RGB 图像、LiDAR 点云、目标坐标、机器人体态描述符），
2. 条件扩散模型用于生成可行导航轨迹，
3. 依赖机器人尺寸的轨迹清晰度（Clearance）预测与排序。

### 核心结构
- **输入**：RGB 图像序列 + LiDAR 点云 + 目标坐标 + 机器人尺寸（宽度、长度）。
- **Fusion Transformer**：  
  通过类型、时间、位置嵌入将多模态数据统一为 token 序列，使用自注意力层进行全局特征融合。
- **扩散轨迹生成（Diffusion Policy）**：  
  模仿运动轨迹分布，利用条件扩散过程生成多条候选轨迹。
  - 训练目标：最小化噪声恢复误差 \( L_{diff} \)
- **清晰度预测头（Clearance Head）**：  
  基于轨迹 Waypoints 与 LiDAR token 进行跨注意力计算，预测每个路径点与障碍物的最小距离。
  - 使用自监督生成标签，通过几何计算最邻近点距离 \( d(p_m, S_j) \)
  - 采用**不对称 Huber Loss**强化保守预测（避免过高估计安全距离）
- **推理选择机制**：  
  从扩散生成的多条轨迹中选取：
  - 若安全轨迹集合 \( B_{safe} \) 非空，则选取距目标最近的一条；
  - 若全部不安全，则重新生成不依赖目标的探索轨迹，以重获可行路径。

### 训练目标
综合多项子目标：
\[
L = \lambda_{diff}L_{diff} + \lambda_{clear}L_{clear} + \lambda_{aux}L_{aux}
\]
其中 \(L_{aux}\) 为预测到目标的距离辅助项，增强轨迹稳定性。

### 技术亮点
- PointTransformerV3 用于高效点云编码；
- 基于**极坐标分桶（polar grid tokenization）**的点云特征整合；
- 自动化 Clearance 标签生成，无需人工标注；
- 在推理阶段通过“goal masking”机制实现从避障模式到目标模式的动态切换。

---

## 3. 实验设计

### 数据集与场景
组合使用多个公开和自建数据集，总计约 **98小时数据**：
| 数据集 | 模态 | 场景 | 时长 |
|---------|-------|--------|--------|
| RELLIS-3D | RGB+LiDAR | 越野 | 35 min |
| TartanDrive 2.0 | RGB+LiDAR | 越野 | 7h |
| SCAND | RGB+LiDAR | 人行道 | 9h |
| Husky Off-Road (自采) | RGB+LiDAR | 人行道 | 30 min |
| Isaac-Sim (仿真数据) | RGB+LiDAR | 越野 | 30 min |
| Huron | RGB+2D扫描 | 人行道 | 75h |
| GrandTour | RGB+LiDAR | 多场景 | 6h |

### 实验环境
- **真实场景**：Clearpath Husky 平台，配备 Zed2i 摄像头与 Ouster OS1 LiDAR。
- **仿真场景**：Isaac-Sim 三种环境（不同障碍密度与开放空间）。

### Benchmark 对比方法
- **学习型基线**：
  - NoMaD-FT（视觉扩散导航模型，微调版）
  - NoMaD-Col（视觉扩散+碰撞预测）
  - ViLiNT-LiDAR-Mask / Image-Mask（消融测试）
- **传统基线**：
  - TEB-Elev（基于弹性带和局部高程图的规划器）

### 评价指标
- **SR（Success Rate）**：导航成功率（到达目标）
- **CR（Collision Rate）**：碰撞率（安全性）

---

## 4. 资源与算力

- 文中仅说明在 **Nvidia AGX Orin** 嵌入计算平台上进行部署与推理；
- 未详细给出训练算力配置（GPU型号、数量、训练时间等）；  
  因此无法量化训练成本，仅可推测为中等规模单GPU训练。

---

## 5. 实验数量与充分性

- 实验覆盖：
  - 三个仿真环境 × 多个基线 × 两种模态遮罩消融；
  - 真实场地实验；
  - 不同机器人尺寸输入的具身性能分析。
- 实验结果统计：
  - 每个环境100次随机目标采样（定量统计SR和CR）。
- **充分性**：
  - 数据来源多样（6+个数据集）；
  - 消融与对比全面（视觉-only、LiDAR-only、双模态）；
  - 同时包含仿真与实机验证；
  整体实验设计较为系统、公平。

---

## 6. 主要结论与发现

- **性能提升**：
  - 平均成功率比 NoMaD 提高 **166%**；
  - 平均碰撞率减少 **62%**；
  - 在真实环境中成功率达 **85% 对比 15%（NoMaD）**。
- **多模态融合优势**：
  - LiDAR 提供的几何深度显著提高安全性；
  - RGB 视觉补充环境语义与外观特征，联合使用效果最优。
- **具身感知效果**：
  - 不同机器人尺寸输入可改变路径选择，避免不适合尺寸的通道；
  - 体现模型的“尺寸适应性”。
- **传统方法对比**：
  - TEB-Elev具有零碰撞率，但高度依赖参数调优；
  - ViLiNT实现无需重新标定的跨平台鲁棒导航。

---

## 7. 优点与创新点

- **方法层面的亮点**：
  - 首次在目标导航中综合使用 Transformer + Diffusion 政策；
  - 通过具身token显式考虑机器人尺寸，实现跨平台适配；
  - 使用自动清晰度标注，避免人工成本；
  - goal masking 实现探索状态与导航状态之间的动态切换。

- **系统性与泛化能力**：
  - 多模态数据源融合增强了鲁棒性；
  - 零样本现实测试显示强迁移能力；
  - 模型兼容现有低层控制器（无需替换运动学模块）。

---

## 8. 不足与局限

- **方法局限**
  - 轨迹生成与选择分离，未实现联合优化；
  - 当前探索策略依赖阈值切换机制，可能产生保守或绕路行为。
- **实验覆盖**
  - 虽覆盖多场景，但大规模真实越野测试仍有限；
  - 无深度对比RL或MPC式自学习方案。
- **算力与效率**
  - 扩散生成在嵌入平台推理速度有限，未报告实时性能指标；
  - 训练资源缺乏具体说明，复现性略受限制。

---

**总体评价**：  
ViLiNT 在结构设计上实现了视觉–几何–具身融合的创新导航策略，显著提升了端到端越野导航的安全性与鲁棒性，是多模态生成式导航方向的重要进展。虽存在推理效率和规划融合度的局限，但为后续在真实部署中实现具身泛化提供了坚实基础。

---

（完）
