---
title: "OLLM: Options-based Large Language Models"
title_zh: OLLM：基于选项的大型语言模型
authors: "Shashank Sharma, Janina Hoffmann, Vinay Namboodiri"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19087v1"
tags: ["query:navwm"]
score: 7.0
evidence: 用于建模选项变化的离散隐变量
tldr: 本文提出OLLM方法，通过引入离散潜变量索引的候选选项集来替代单一的下一个词预测，使生成多样性显式可控。该方法通过少量额外参数即可改造预训练LLM，在数学推理任务中性能显著提升，并在潜变量空间实现高效的策略优化与对齐。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19087v1/fig-001.webp\", \"caption\": \"Figure 7: Example 2 showing the deterministic and ambiguous tokens.\", \"page\": 10, \"index\": 1, \"width\": 654, \"height\": 977}, {\"url\": \"assets/figures/arxiv/2604.19087v1/fig-002.webp\", \"caption\": \"Figure 2: Training and inference architectures for our method. The LLM backbone and the lm_head remain fixed. (a) During training, the encoder predicts the appropriate latent by looking at the next token. (b) During training, a policy network predicts the appropriate latent. Policy is trained using SFT for behavioral cloning.\", \"page\": 3, \"index\": 2, \"width\": 762, \"height\": 191}, {\"url\": \"assets/figures/arxiv/2604.19087v1/fig-003.webp\", \"caption\": \"Figure 5: Histogram of the token entropies in predicted text for the OmniMath dataset. Two modes can be observed: one for near-deterministic tokens and another for tokens with multiple possible options.\", \"page\": 8, \"index\": 3, \"width\": 787, \"height\": 508}, {\"url\": \"assets/figures/arxiv/2604.19087v1/fig-004.webp\", \"caption\": \"Figure 4: Evaluation curves comparing OLLM and state-of-the-art LORA modules.\", \"page\": 4, \"index\": 4, \"width\": 779, \"height\": 333}, {\"url\": \"assets/figures/arxiv/2604.19087v1/fig-005.webp\", \"caption\": \"Figure 6: Example 1 showing the deterministic and ambiguous tokens. Tokens with probabilities > .85 are marked as deterministic (dark) and ambiguous (light) otherwise. Note how the start of phrases, sentences, or math expressions is ambiguous, but the endings are mostly deterministic.\", \"page\": 9, \"index\": 5, \"width\": 652, \"height\": 975}]"
motivation: 现有LLM依赖温度或采样策略来引入多样性，但缺乏显式的可控生成机制。
method: 在LLM输出层前加入编码器和解码器以学习离散潜变量选项集，并在其上训练策略网络实现控制。
result: "在数学推理任务上，OLLM提升了正确率至约70%，显著超越传统LoRA基线模型。"
conclusion: OLLM结构性地增强了LLM的生成可控性和推理鲁棒性，为潜变量空间中的强化学习提供了新方向。
---

## 摘要
我们提出了 Options LLM（OLLM），这是一种简单而通用的方法，用一组被离散潜变量索引的、可学习的下一个 token 选项，来替代标准大型语言模型（LLM）的单一下一 token 预测。与依赖温度或采样启发式方法来产生多样性不同，OLLM 通过显式建模变异性：一个小型潜空间参数化多个合理的下一 token 选项，可由下游策略选择或搜索。在架构上，OLLM 是一个轻量级“插件”，在输出头之前插入编码器和解码器两层，使几乎任何预训练的 LLM 都能以极少新增参数被转换。我们将 OLLM 应用于一个拥有 17 亿参数的主干模型（仅有 1.56% 的参数可训练），该模型在 OpenMathReasoning 上训练，在 OmniMath 上进行评估。当前基于 LoRA 调整的最优基线在最终答案正确率上达到 51%，而 OLLM 的选项集合在最佳潜变量选择下可达约 70%。随后我们在潜空间中训练了一个紧凑的策略，用于生成控制潜变量。在低维选项空间中运行使得奖励优化的样本效率显著提高，同时显著减少了常见的不匹配现象（例如语言切换或退化推理），因为策略被限制在 SFT 期间学到的选项中。关键的是，这种对齐源于模型结构，而非额外的 KL 或手工设计的对齐损失。我们的结果表明，基于选项的下一 token 建模提升了数学推理中的可控性、鲁棒性和效率，并凸显了潜空间策略学习作为 LLM 强化学习的一条有前景的方向。

## Abstract
We introduce Options LLM (OLLM), a simple, general method that replaces the single next-token prediction of standard LLMs with a \textit{set of learned options} for the next token, indexed by a discrete latent variable. Instead of relying on temperature or sampling heuristics to induce diversity, OLLM models variation explicitly: a small latent space parametrizes multiple plausible next-token options which can be selected or searched by a downstream policy. Architecturally, OLLM is a lightweight "plug-in" that inserts two layers: an encoder and a decoder, before the output head, allowing almost any pretrained LLM to be converted with minimal additional parameters. We apply OLLM to a 1.7B-parameter backbone (only $1.56\%$ of parameters trainable) trained on OpenMathReasoning and evaluated on OmniMath. The SOTA LoRA-adapted baselines peak at $51\%$ final answer correctness, while OLLM's option set allows up to $\sim 70\%$ under optimal latent selection. We then train a compact policy in the latent space that emits latents to control generation. Operating in a low-dimensional option space makes reward optimization far more sample-efficient and substantially reduces common misalignments (e.g., language switching or degenerate reasoning), as the policy is constrained to options learned during SFT. Crucially, this alignment arises from model structure rather than additional KL or handcrafted alignment losses. Our results demonstrate that optionized next-token modeling enhances controllability, robustness, and efficiency in math reasoning, and highlight latent-space policy learning as a promising direction for reinforcement learning in LLMs.