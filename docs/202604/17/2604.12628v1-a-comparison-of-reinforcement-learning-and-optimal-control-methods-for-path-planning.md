---
title: A Comparison of Reinforcement Learning and Optimal Control Methods for Path Planning
title_zh: 强化学习与最优控制方法在路径规划中的比较研究
authors: "Qiang Le, Yaguang Yang, Isaac E. Weintraub"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12628v1"
tags: ["query:navwm"]
score: 8.0
evidence: 复杂威胁环境下的强化学习路径规划
tldr: 本文研究自主车辆在威胁环境中的路径规划问题，提出基于深度确定性策略梯度（DDPG）的学习方法，相比伪谱最优控制方法能更快生成有效安全路径，显示其在实时任务中的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12628v1/fig-001.webp\", \"caption\": \"Figure 4: (a) Obtained path by further trained DDPG agent with low standard deviation in the action noise, the resulting travel time 22 × 0.25 = 5.5 (b) Instant Reward (c) Action over time steps\", \"page\": 5, \"index\": 1, \"width\": 508, \"height\": 1177}, {\"url\": \"assets/figures/arxiv/2604.12628v1/fig-002.webp\", \"caption\": \"Figure 3: (a) Obtained path by DDPG with the standard deviation 5 degrees in the action noise, the resulting travel time 23 × 0.25 = 5.75 (b) Instant Reward (c) Action over time steps\", \"page\": 5, \"index\": 2, \"width\": 506, \"height\": 1144}, {\"url\": \"assets/figures/arxiv/2604.12628v1/fig-003.webp\", \"caption\": \"Figure 1: (a) Actor network (b) Critic network\", \"page\": 3, \"index\": 3, \"width\": 494, \"height\": 371}, {\"url\": \"assets/figures/arxiv/2604.12628v1/fig-004.webp\", \"caption\": \"Figure 2: Heat map of the reward function where w3=0\", \"page\": 3, \"index\": 4, \"width\": 466, \"height\": 350}, {\"url\": \"assets/figures/arxiv/2604.12628v1/fig-005.webp\", \"caption\": \"Figure 5: Feasible area of the specialized agent who is trained using [400, 400]⊤ as the starting point\", \"page\": 6, \"index\": 5, \"width\": 466, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2604.12628v1/fig-006.webp\", \"caption\": \"Figure 8: Testing examples of the general DDPG agent\", \"page\": 6, \"index\": 6, \"width\": 466, \"height\": 404}, {\"url\": \"assets/figures/arxiv/2604.12628v1/fig-007.webp\", \"caption\": \"Figure 6: Feasible area of the general agent who is trained using random initial points\", \"page\": 6, \"index\": 7, \"width\": 466, \"height\": 400}, {\"url\": \"assets/figures/arxiv/2604.12628v1/fig-008.webp\", \"caption\": \"Figure 7: Testing examples of the specialized DDPG agent\", \"page\": 6, \"index\": 8, \"width\": 466, \"height\": 406}, {\"url\": \"assets/figures/arxiv/2604.12628v1/fig-009.webp\", \"caption\": \"Figure 9: Obtained path by PS, Elapsed time ≈ 8 seconds (a) constraining speed |V | = 200, (b) no speed constraint\", \"page\": 6, \"index\": 9, \"width\": 466, \"height\": 350}]"
motivation: 针对自主车辆在威胁环境中的路径规划问题，传统最优控制方法计算耗时且难以实时应用。
method: 采用深度确定性策略梯度（DDPG）算法进行路径规划，并将威胁区建模为圆形禁区。
result: DDPG方法能生成有效路径且计算速度显著快于传统最优控制方法。
conclusion: 虽然DDPG在部分情形下无法找到最优或可行路径，但其实时性优于传统方法，未来将改进奖励函数与可行集分析。
---

## 摘要
在充满威胁的环境中进行自主车辆路径规划是一项基本挑战。传统的最优控制方法虽然能够找到理想路径，但其计算时间往往过长，难以满足实时决策的需求。为解决这一问题，本文提出了一种基于深度确定性策略梯度（Deep Deterministic Policy Gradient，DDPG）的方法，并将威胁建模为一个简单的圆形“禁行区”。如果车辆在任意时刻进入此“禁行区”或未能到达目标附近，则任务被视为失败。DDPG智能体被训练以学习从当前状态（位置和速度）到一系列可行动作的直接映射，从而引导其安全到达目标。本文设计了奖励函数，并通过两个神经网络（评价网络与策略网络）来描述环境并指导控制行为。DDPG训练智能体以找到最大可能的起始点集合（即“可行集”），在该集合中可以保证存在一条通往目标的安全路径。这为任务规划提供了关键信息，使得在任务执行前即可判断从给定起点出发任务是否可行，从而辅助任务前规划活动。本文通过仿真验证了该方法，并将DDPG方法与传统的最优控制（伪谱）方法进行了比较。结果表明，基于学习的智能体能够生成有效路径，同时计算速度显著加快，更适合实时应用。然而，也存在DDPG智能体无法找到通往目标路径的区域（“不可行集”），且可行集中的路径并非最优。这些初步结果为后续研究提供了指导方向：（1）改进奖励函数以扩大DDPG可行集；（2）分析伪谱方法得到的可行集；（3）研究弧搜索内点法（arc-search IPM）在路径规划问题中的应用。

## Abstract
Path-planning for autonomous vehicles in threat-laden environments is a fundamental challenge. While traditional optimal control methods can find ideal paths, the computational time is often too slow for real-time decision-making. To solve this challenge, we propose a method based on Deep Deterministic Policy Gradient (DDPG) and model the threat as a simple, circular `no-go' zone. A mission failure is claimed if the vehicle enters this `no-go' zone at any time or does not reach a neighborhood of the destination. The DDPG agent is trained to learn a direct mapping from its current state (position and velocity) to a series of feasible actions that guide the agent to safely reach its goal. A reward function and two neural networks, critic and actor, are used to describe the environment and guide the control efforts. The DDPG trains the agent to find the largest possible set of starting points (``feasible set'') wherein a safe path to the goal is guaranteed. This provides critical information for mission planning, showing beforehand whether a task is achievable from a given starting point, assisting pre-mission planning activities. The approach is validated in simulation. A comparison between the DDPG method and a traditional optimal control (pseudo-spectral) method is carried out. The results show that the learning-based agent may produce effective paths while being significantly faster, making it a better fit for real-time applications. However, there are areas (``infeasible set'') where the DDPG agent cannot find paths to the destination, and the paths in the feasible set may not be optimal. These preliminary results guide our future research: (1) improve the reward function to enlarge the DDPG feasible set, (2) examine the feasible set obtained by the pseudo-spectral method, and (3) investigate the arc-search IPM method for the path planning problem.

---

## 论文详细总结（自动生成）

# 强化学习与最优控制方法在路径规划中的比较研究 — 中文结构化总结

---

## 一、研究背景与核心问题

- **研究动机**  
  在自主车辆、无人机和机器人等系统中，路径规划是核心任务之一。其目标是在存在障碍或威胁区（如禁行区、敌方区域）的环境中，寻找一条安全且时间最优的路径，以实现任务到达。
  
- **传统方法的局限**  
  传统的最优控制方法（如伪谱法）能精确求解最优路径，但计算代价高，难以支撑实时决策。
  
- **核心研究问题**  
  本文探讨两种路径规划范式的对比：  
  1. 基于**深度强化学习（DDPG）**的智能规划方法；  
  2. 基于**最优控制（伪谱法）**的数值优化算法。  
  研究重点是从性能、计算效率与可行性三个维度比较两者的优缺点，为未来混合优化算法提供方向。

---

## 二、研究方法与技术路线

### 1. 方法框架
- **总体思路**：  
  使用 **Deep Deterministic Policy Gradient (DDPG)** 算法训练智能体，使其从连续状态（位置与朝向）出发，输出连续控制动作（转向角变化），在不触碰禁行区的前提下达到目标点。
  
- **问题建模**：  
  - 环境：包含圆形“禁行区”；若智能体进入禁区或未能到达目标则任务失败。  
  - 状态：\( S_t = [X_t, Y_t, \phi_t]^T \)（2D坐标与航向）。  
  - 动作：航向改变量 \( A_t = \Delta \phi_t \)。  
  - 时间步长：\( T_s = 0.25 \)。  
  - 速度恒定：\(|V| = 200\)。

### 2. DDPG 算法结构
- **Actor–Critic 架构**：
  - **Actor 网络**输出动作 \( A = \pi(S; \theta_A) \)。  
  - **Critic 网络**输出状态–动作价值 \( Q(S,A;\theta_C) \)。  
  - 使用目标网络稳定训练。
  
- **更新公式**：
  - 目标值：
    \[
    y = R + \gamma Q'(S', \pi'(S'; \theta_A'); \theta_C')
    \]
  - Critic 损失：
    \[
    L = \frac{1}{N}\sum_i (y_i - Q(S_i, A_i; \theta_C))^2
    \]
  
- **探索机制**：  
  向动作加入高斯或 Ornstein–Uhlenbeck 噪声 \( N \)，并在训练后期减小噪声以平衡探索与利用。

### 3. 奖励函数设计
- 基于**人工势场 (Artificial Potential Field)** 的三部分奖励项：
  1. 吸引项：鼓励靠近目标点 \( P_f \)；  
  2. 排斥项：惩罚接近禁区中心 \( O \)；  
  3. 控制代价项：惩罚大转向行为以压缩路径长度。
  
  公式：
  \[
  R(P,A) = w_1 N(P,P_f,\sigma) - w_2 N(P,O,\sigma) - w_3|A|^2
  \]
  （其中 \( N(P,μ,σ) \) 为归一化二维高斯分布，\( w_1,w_2,w_3 \) 为权重参数。）

### 4. 对比方法——伪谱（Pseudo-spectral）法
- 将路径规划转化为约束最优控制问题：
  \[
  \min_{S,U,t_f} \int_{t_0}^{t_f} L(S,U,t)\,dt + M(t_f,S(t_f),U(t_f))
  \]
  约束包括动力学方程、路径约束（禁区）与边界条件。
- 采用 **Legendre-Gauss-Lobatto (LGL)** 配点法，分段求解连续控制最优路径。

---

## 三、实验设计与设置

### 1. 仿真环境
- **二维空间**，目标点为 \((-200, -400)\)，起点为 \((400, 400)\)。  
- **禁行区**：圆心在原点 \((0,0)\)，半径 240。  
- **速度固定**：200（单位未说明）。  
- **时间步长**：0.25，最多 24 步。  

### 2. 实验任务
- **DDPG 训练阶段**：
  - 专用智能体：固定起点训练。  
  - 通用智能体：随机起点训练。  
  - 奖励达阈值（12,000～18,000）后停止训练。  

- **测试阶段**：
  - 使用 1000×1000 网格（步长 20）生成“可行区域图”。  
  - 判断哪些起点可安全达到目标（黄色区域表示可行）。
  - 对比 DDPG 与伪谱法生成的路径与计算时间。

### 3. 对比方法
- **强化学习方法**：两种 DDPG 智能体（专用与通用）。  
- **最优控制方法**：伪谱法（Pseudo-spectral Method）。  

### 4. 基准指标
- 是否能到达目标（可行性）；  
- 路径长度或时间（最优性）；  
- 计算时间（实时性）。

---

## 四、资源与算力说明
- 论文未提及 GPU 或并行加速。  
- 实验在 **Matlab** 环境下运行。
- 明确提到伪谱法运行于 **Dell Precision 5560（i7 2.4GHz, 32GB 内存）**，一次路径求解约 **8 秒**。  
- DDPG 训练耗时“数分钟”，但推理阶段比伪谱法快 **约两个数量级**。

---

## 五、实验数量与充分性分析

- **实验组别**：
  - 主要包括两种 DDPG 智能体、一个伪谱法实例；
  - 每种智能体进行了路径可行性与区域分析；
  - 没有多组参数消融或不同地图环境测试。
  
- **实验充分性**：
  - 仅展示单个禁区单目标案例，验证具有初步性质；
  - 可行性图表明模型捕捉到了部分合理区域，但未系统比较多障碍、多起点复杂场景；
  - 缺乏统计量和标准 benchmark 比较，因此结果具参考性但不完全充分。

---

## 六、主要发现与结论

1. **计算速度优势**：  
   DDPG 经过训练后，可在毫秒级生成路径，比伪谱法（数秒级）快 1–2 个数量级，适合实时应用。

2. **可行性不完备**：  
   DDPG 存在“不可行集”，即部分起点无法找到有效路径；伪谱法在数值上更稳定，但计算代价大。

3. **最优性差异**：  
   伪谱法产生的路径在时间上略优（如 5.54 秒 vs DDPG 5.5–5.75 秒），而 DDPG 路径虽趋近最优但不保证最优。

4. **理论启示**：  
   建立了基于 DDPG 的“可行区域”框架，为预任务规划提供了可评估的任务起点集合。

---

## 七、优势与亮点

- **连续动作空间建模**：  
  DDPG 可同时应对连续状态与动作，较传统 DQN 更适合运动控制场景。  

- **奖励函数融合潜力场思想**：  
  结合吸引与排斥势场，实现了自然的避障策略。  

- **创新性指标**：提出“可行区域（feasible set）”概念，将强化学习结果映射为空间任务可行性分析。  

- **工程可行性高**：  
  DDPG 推理速度极快，具备实时路径生成潜力。

---

## 八、不足与局限

- **场景简化**：仅考虑单圆形静态障碍，忽略了动态障碍、多目标及环境噪声。  
- **训练样本偏窄**：专用智能体产生过拟合现象，可行区域局限于训练起点附近。  
- **路径最优性不保证**：学习结果仅保障可行，但未强制最优。  
- **缺乏泛化验证**：未在不同地图、不同奖励权重或不同结构上进行消融实验。  
- **算力未说明**：无法客观比较算法效率在大规模部署下的表现。  

---

**（完）**
