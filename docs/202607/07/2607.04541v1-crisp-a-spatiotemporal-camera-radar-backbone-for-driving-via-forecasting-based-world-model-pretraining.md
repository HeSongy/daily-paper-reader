---
title: "CRISP: A Spatiotemporal Camera-Radar Backbone for Driving via Forecasting-Based World-Model Pretraining"
title_zh: CRISP：一种面向自动驾驶、基于预测式世界模型预训练的时空摄像头-雷达主干网络
authors: "Jingyu Song, Yi Liu, Katherine A. Skinner"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04541v1"
tags: ["query:navwm"]
score: 10.0
evidence: 基于预测的世界模型预训练用于自动驾驶
tldr: CRISP提出一种面向自动驾驶的时空相机-雷达统一骨干网络，通过利用历史多视角图像和雷达数据预测未来LiDAR点云，实现基于预测的世界模型预训练。方法引入增强雷达编码、雷达增强时序注意力与模态创新门控机制，以强化BEV时空融合与跨模态信息利用。在nuScenes上，该方法不仅提升长时域点云预测能力，还能有效迁移到检测、跟踪、建图、运动预测和规划等多项任务。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04541v1/fig-001.webp\", \"caption\": \"Fig. 1. Overview of the motivation of CRISP. We use low-cost camera-radar inputs for inference, while leveraging LiDAR as privileged supervision during pretraining to learn a transferable multimodal driving backbone.\", \"page\": 1, \"index\": 1, \"width\": 529, \"height\": 375}, {\"url\": \"assets/figures/arxiv/2607.04541v1/fig-002.webp\", \"caption\": \"Fig. 3. Radar-enhanced temporal self-attention. The calibrated radar BEV feature F rad t primes the input BEV query Q (l−1)\", \"page\": 6, \"index\": 2, \"width\": 485, \"height\": 544}, {\"url\": \"assets/figures/arxiv/2607.04541v1/fig-003.webp\", \"caption\": \"Fig. 5. Label-efficiency of CRISP pretraining on nuScenes 3D detection. We train the same BEVFormer-CRISP detection model for 12 epochs using 1/4, 1/2, and the full nuScenes training split. The scratch baseline uses the same FCOS3D-pretrained camera encoder but does not use CRISP backbone pretraining. CRISP improves both NDS and mAP across all data scales, with clear gains in the low-label regime.\", \"page\": 11, \"index\": 3, \"width\": 517, \"height\": 367}, {\"url\": \"assets/figures/arxiv/2607.04541v1/fig-004.webp\", \"caption\": \"Fig. 8. Failure case analysis for future world-geometry prediction. We show the front camera views at the current frame (0s) and future timestamps for visualization only; future images are not provided to the model. On the right, predicted point clouds are compared with ground truth. This scene requires reasoning about a traffic light that is small and difficult to observe from the input images: the highlighted vehicle remains stopped for the red light, while the road ahead is visually open. CRISP predicts the vehicle geometry more accurately than the CO ViDAR baseline at early horizons, but its prediction begins to drift after 2s. This suggests that, when high-level traffic-signal intent is ambiguous, the forecasting model may rely more on learned motion priors from pretraining than on the underlying traffic-control state.\", \"page\": 15, \"index\": 4, \"width\": 1076, \"height\": 621}, {\"url\": \"assets/figures/arxiv/2607.04541v1/fig-005.webp\", \"caption\": \"Fig. 4. Multimodal feature rendering. Camera and radar cross-attention branches produce residual innovations, which are selectively admitted by the Modality Innovation Gate (MIG). The pre-attention BEV query is preserved through the residual path.\", \"page\": 7, \"index\": 5, \"width\": 512, \"height\": 301}, {\"url\": \"assets/figures/arxiv/2607.04541v1/fig-006.webp\", \"caption\": \"Fig. 7. Qualitative comparison between CRISP and ViDAR for future world-geometry prediction. We show the front camera views at the current frame (0s) and future timestamps for visualization only; future images are not provided to the model. On the right, predicted point clouds are compared with ground truth, where CRISP better preserves the scene geometry over time. In this example, the CO ViDAR baseline propagates an erroneous structure from a billboard region, while CRISP maintains more reliable geometry.\", \"page\": 14, \"index\": 6, \"width\": 1076, \"height\": 621}, {\"url\": \"assets/figures/arxiv/2607.04541v1/fig-007.webp\", \"caption\": \"Fig. 6. Qualitative comparison between CRISP and ViDAR for future world-geometry prediction in a low-light scene. We show the front camera views at the current frame (0s) and future timestamps for visualization only; future images are not provided to the model. On the right, predicted point clouds are compared with ground truth. In this example, the CO ViDAR baseline loses the forward vehicle over time and exhibits large spatial drift, while CRISP maintains a more accurate prediction of the vehicle trajectory. This illustrates the benefit of radar motion cues for forecasting dynamic objects under challenging illumination, which is important for downstream trajectory evaluation and safe planning around leading vehicles.\", \"page\": 13, \"index\": 7, \"width\": 1076, \"height\": 621}, {\"url\": \"assets/figures/arxiv/2607.04541v1/fig-008.webp\", \"caption\": \"TABLE I. Point cloud forecasting on nuScenes following the ViDAR protocol [23]. We report Chamfer Distance (CD) for future timestamps from 0.5s to 3.0s. The best result is shown in bold and the second-best result is underlined. † indicates a language-augmented world model [50] with an LLMbased component and additional text supervision, which is different from the sensor-only pretraining setting of the other listed methods.\", \"page\": 9, \"index\": 8, \"width\": 525, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2607.04541v1/fig-009.webp\", \"caption\": \"Fig. 2. Overview of CRISP. Historical multi-view images and aggregated radar observations are encoded into a shared CR BEV backbone. Radar first passes through ego-aware radar encoding, then enters the backbone through radar-enhanced temporal propagation and multimodal feature rendering. During pretraining, a future occupancy decoder is supervised by future LiDAR; during downstream finetuning, only the pretrained CR backbone is retained.\", \"page\": 5, \"index\": 9, \"width\": 1076, \"height\": 577}]"
motivation: 现有相机-雷达融合模型大多依赖任务特定监督，缺乏可复用的通用表示学习能力，因此需要一种适用于实际传感器配置的统一预训练框架。
method: 模型利用历史多视角图像与雷达序列，通过预测未来LiDAR点云进行BEV时空表示预训练，并设计增强雷达编码器、雷达增强时序自注意力和模态创新门控机制强化融合。
result: 在nuScenes数据集上，CRISP显著提升了长时域点云预测性能，并在3D检测、跟踪、在线建图、运动预测、未来占据预测和规划等下游任务中取得有效迁移增益。
conclusion: CRISP表明基于未来预测的相机-雷达联合预训练能够学习可迁移的驾驶表示，在仅依赖相机和雷达部署的条件下提升多任务自动驾驶能力。
---

## 摘要
摄像头-雷达（CR）融合是一种面向自动驾驶的实用传感配置，但现有模型通常依赖特定任务的监督进行训练，从而限制了可复用表征的学习。我们提出了 CRISP，一种通过基于预测的表征学习进行预训练的时空摄像头-雷达主干网络。给定历史多视角图像与雷达扫描数据，CRISP 通过预测未来 LiDAR 点云来学习统一的鸟瞰图（BEV）表征。LiDAR 仅在预训练阶段作为特权监督使用；部署后的模型仅需摄像头与雷达输入。为了使基于预测的预训练能够有效适用于 CR 融合，CRISP 引入了增强型雷达编码器、雷达增强的时序自注意力机制，以及带有模态创新门控的多模态特征渲染。这些组件将雷达的距离与多普勒信息注入 BEV 的时序传播过程，并使 BEV token 能够有选择地融合摄像头与雷达证据。在 nuScenes 数据集上的实验表明，CRISP 不仅提升了长时域点云预测能力，还能有效迁移到下游任务，包括 3D 检测、目标跟踪、在线地图构建、运动预测、未来占用预测以及规划任务。这表明，在实用传感器配置下，基于预测的 CR 预训练是实现可扩展驾驶表征的一条有前景的路径。项目主页：https://umfieldrobotics.github.io/CRISP。

## Abstract
Camera-radar (CR) fusion is a practical sensing configuration for autonomous driving, but existing models are typically trained with task-specific supervision, limiting reusable representation learning. We present CRISP, a spatiotemporal CR backbone pretrained through forecasting-based representation learning. Given historical multi-view images and radar sweeps, CRISP learns a unified bird's-eye-view (BEV) representation by predicting future LiDAR point clouds. LiDAR is used only as privileged supervision during pretraining; the deployed model requires only camera and radar. To make forecasting-based pretraining effective for CR fusion, CRISP introduces an enhanced radar encoder, radar-enhanced temporal self-attention, and multimodal feature rendering with modality innovation gating. These components inject radar range and Doppler cues into BEV temporal propagation and allow BEV tokens to selectively incorporate camera and radar evidence. Experiments on nuScenes show that CRISP improves long-horizon point cloud forecasting and transfers effectively to downstream tasks, including 3D detection, tracking, online mapping, motion forecasting, future occupancy prediction, and planning, suggesting that predictive CR pretraining is a promising path toward scalable driving representations under practical sensor configurations. The project website is https://umfieldrobotics.github.io/CRISP.

---

## 论文详细总结（自动生成）

# CRISP 论文详细总结

## 1. 核心问题与整体含义（研究动机与背景）

### 自动驾驶中的现实问题
当前自动驾驶系统越来越依赖多模态传感器融合，其中：

- 摄像头（Camera）提供丰富语义与纹理；
- 雷达（Radar）提供距离、速度（Doppler）与恶劣天气鲁棒性；
- LiDAR 虽然精度高，但成本高、部署受限。

工业界越来越倾向于“Camera + Radar”这一低成本、可量产配置。

然而，现有 Camera-Radar（CR）方法存在几个核心问题：

- 大多数模型是“任务专用训练”：
  - 检测模型只学检测；
  - 跟踪模型只学跟踪；
  - 缺乏统一可迁移表征。
- 缺少类似视觉领域 foundation model 的预训练体系。
- Camera-Radar 融合比 Camera-LiDAR 更困难：
  - 雷达点稀疏；
  - 噪声大；
  - 与图像模态分布差异明显。
- 现有世界模型（world model）大多依赖：
  - LiDAR-only；
  - Camera-only；
  - 或训练/推理都依赖 LiDAR；
  不适合真实低成本部署。

### 论文核心目标
CRISP 的目标是：

> 用 Camera + Radar 历史序列，通过“预测未来 LiDAR 世界几何”进行世界模型式预训练，从而学习可迁移的统一驾驶表征。

核心思想：

- 训练时：
  - 使用未来 LiDAR 点云作为“特权监督（privileged supervision）”。
- 推理部署时：
  - 仅使用 Camera + Radar。
- 本质上属于：
  - Forecasting-based world-model pretraining（基于预测的世界模型预训练）。

论文强调：

> “预测未来世界”比单纯做监督任务更能学习通用驾驶表示。

---

# 2. 方法论：CRISP 的核心思想与关键技术

## 2.1 总体框架

CRISP 的输入：

- 历史多视角图像序列
- 历史 radar sweeps

输出：

- 未来 LiDAR occupancy / point cloud

整体结构：

```text
历史 Camera + Radar
        ↓
统一 CR-BEV Backbone
        ↓
时空 BEV 表征
        ↓
未来 Occupancy Decoder
        ↓
未来 LiDAR 点云预测
```

训练后：

- 仅保留 pretrained CR backbone；
- Decoder 被丢弃；
- Backbone 可迁移到下游任务。

---

# 2.2 核心思想：Forecasting-based Representation Learning

与传统检测监督不同：

传统：
```text
输入 → 检测框
```

CRISP：
```text
输入历史 → 预测未来世界
```

论文认为：

- 要预测未来，
- 模型必须理解：
  - 物体运动；
  - 场景几何；
  - 时序动态；
  - 交通行为；
  - 可通行区域。

因此会自然学习更通用表示。

---

# 2.3 CRISP 的三个关键技术

---

## （1）增强型 Radar Encoder（Ego-aware Radar Encoding）

### 动机
原始 radar：

- 非常稀疏；
- 噪声大；
- 坐标随 ego motion 变化。

因此作者设计：

- ego-aware radar encoding。

### 核心做法

编码：

- range（距离）
- azimuth（方位）
- Doppler velocity（速度）
- RCS（反射强度）

并进行：

- ego-motion compensation（自车运动补偿）。

目标：

- 让 radar 更稳定地进入 BEV 空间。

---

## （2）Radar-enhanced Temporal Self-Attention

对应 Fig.3。

### 问题
传统 BEV 时序传播：

- 多依赖视觉 token；
- 对动态物体运动建模不足。

### 方法
作者将 radar feature 注入 temporal attention：

```text
Radar BEV feature
    ↓
校准后作为 temporal query 的增强项
```

作用：

- 用 radar Doppler 信息帮助时序传播；
- 增强动态目标预测；
- 提升低光照场景鲁棒性。

论文实验中特别强调：

- 夜间场景；
- 运动车辆；
- 长时域预测；

提升明显。

---

## （3）Multimodal Feature Rendering + MIG

对应 Fig.4。

这是论文最有特色的模块之一。

### 问题
简单 Camera-Radar fusion：

- 容易互相污染；
- radar 噪声可能破坏视觉表示。

### 方法

模型建立：

- camera cross-attention branch
- radar cross-attention branch

分别产生：

```text
Residual innovations
```

然后通过：

```text
Modality Innovation Gate (MIG)
```

动态决定：

- 是否接纳该模态的新信息。

本质类似：

- selective residual update。

### 优势

相比直接融合：

- 更稳定；
- 更鲁棒；
- 避免雷达噪声传播。

---

# 2.4 预测目标

训练目标：

- future occupancy prediction
- future LiDAR point cloud forecasting

使用：

- Chamfer Distance（CD）
- occupancy supervision

预测时间范围：

- 0.5s ～ 3.0s

属于长时域未来建模。

---

# 3. 实验设计

# 3.1 数据集

## nuScenes

论文主要基于：

- nuScenes benchmark

包含：

- 多摄像头
- radar
- LiDAR
- 动态城市驾驶场景

这是当前自动驾驶多模态标准 benchmark。

---

# 3.2 预训练任务

### 世界模型预训练
任务：

- 未来点云预测（forecasting）

输入：

- 历史 Camera + Radar

监督：

- future LiDAR

---

# 3.3 下游任务

论文迁移到多个任务：

- 3D Detection
- Tracking
- Online Mapping
- Motion Forecasting
- Future Occupancy Prediction
- Planning

这是论文的重要亮点：

> 一个 backbone 迁移多个任务。

---

# 3.4 Benchmark 与指标

## 点云预测

遵循：

- ViDAR protocol

指标：

- Chamfer Distance（CD）

预测：

- 0.5s ～ 3.0s

---

## 检测任务

指标：

- NDS
- mAP

并测试：

- label efficiency

即：

- 1/4 数据
- 1/2 数据
- full data

---

# 3.5 对比方法

论文主要对比：

- ViDAR
- CO-ViDAR
- Camera-only world models
- 各类 forecasting baselines

部分还对比：

- language-augmented world models

即：

- 带 LLM 与文本监督的方法。

---

# 3.6 消融实验

论文进行了较充分消融：

包括：

- 去掉 radar-enhanced temporal attention
- 去掉 MIG
- 不使用 radar
- 不使用 forecasting pretraining
- 不同 label scale

属于比较完整的模块验证。

---

# 4. 资源与算力

论文提供的信息有限。

从给出的内容中：

- 未明确说明：
  - GPU 型号
  - GPU 数量
  - 总训练时长
  - FLOPs
  - 参数量

因此：

- 算力成本不可准确评估；
- 这是论文信息披露上的一个缺失。

考虑其：

- 时序 BEV backbone
- 多模态输入
- 长时域 occupancy forecasting

推测训练成本应较高。

---

# 5. 实验数量与充分性

## 实验覆盖较广

论文实验包括：

### （1）主任务实验
- 点云未来预测

### （2）迁移实验
- 检测
- 跟踪
- 地图
- motion forecasting
- planning

### （3）Label efficiency
- 少样本训练

### （4）定性分析
- 夜间
- 动态目标
- 长时域漂移

### （5）消融实验
- 模块贡献验证

---

## 客观性与公平性

总体较客观：

- 使用统一 benchmark；
- 对比主流方法；
- 保持相同 backbone；
- 有 scratch baseline。

但也存在限制：

- 主要集中于 nuScenes；
- 缺少跨数据集泛化；
- 没有 Waymo/OpenDrive 等验证。

因此：

- 泛化性证据仍有限。

---

# 6. 主要结论与发现

## 核心结论

### （1）Forecasting pretraining 有效
预测未来世界：

- 能学习更强驾驶表示；
- 比任务专用监督更通用。

---

### （2）Radar 对长期预测非常重要

尤其：

- 夜间；
- 低光；
- 动态目标；
- 长时域预测。

Doppler motion cue 明显提升未来轨迹稳定性。

---

### （3）Camera-Radar 可以学习接近 LiDAR world model 的能力

虽然推理不使用 LiDAR：

- 但通过 LiDAR supervision，
- 可以获得强世界理解能力。

---

### （4）预训练具有明显迁移性

CRISP backbone：

- 能迁移到多个任务；
- 并提升低标签场景表现。

这说明：

- 学到的是通用 driving representation。

---

# 7. 优点与亮点

## 方法层面

### 1. 非常符合工业实际
部署只需：

- Camera + Radar

成本低于 LiDAR-heavy 方案。

---

### 2. 世界模型思路先进
将：

- forecasting
- occupancy prediction
- BEV representation

统一到预训练框架中。

符合当前 autonomous driving foundation model 趋势。

---

### 3. Radar 融合设计细致
不是简单 concat：

而是：

- temporal injection
- modality gating
- innovation residual

设计成熟。

---

### 4. 多任务迁移验证充分
不是只做 detection。

覆盖：

- planning
- forecasting
- occupancy

更能证明 backbone 通用性。

---

### 5. Label efficiency 很有价值
少标签仍有效提升：

对自动驾驶非常重要。

---

# 8. 不足与局限

## 1. 数据集单一

主要只在：

- nuScenes

验证。

缺少：

- Waymo
- Argoverse
- 长尾天气场景

泛化能力尚不充分。

---

## 2. 长时域预测仍会漂移

论文 failure case 显示：

- 超过 2s 后开始 drift；
- 尤其涉及交通灯等高层语义时。

说明：

当前模型更依赖 motion prior，
而非真正 traffic reasoning。

---

## 3. 缺少高层决策建模

虽然做 planning transfer：

但 backbone 本身仍偏：

- 几何预测；
- occupancy forecasting；

缺少：

- intent understanding
- interaction reasoning
- symbolic traffic reasoning

---

## 4. 算力与效率未充分分析

缺少：

- 推理延迟
- 实时性
- 参数量
- GPU 成本

对于车端部署评估不足。

---

## 5. LiDAR supervision 仍然依赖昂贵数据

虽然部署不用 LiDAR：

但预训练仍需：

- 大规模 future LiDAR labels。

因此：

- 数据采集成本依然较高。

---

# 总体评价

CRISP 的贡献可以概括为：

> 将“未来世界预测”与“Camera-Radar 多模态 BEV 表征学习”结合，构建了一个具有 foundation-model 潜力的自动驾驶预训练 backbone。

其价值主要体现在：

- 现实传感器配置；
- 世界模型预训练；
- 多任务迁移；
- radar-aware 时空建模。

这是当前 autonomous driving foundation model 方向中：

“低成本传感器世界模型”的代表性工作之一。

（完）
