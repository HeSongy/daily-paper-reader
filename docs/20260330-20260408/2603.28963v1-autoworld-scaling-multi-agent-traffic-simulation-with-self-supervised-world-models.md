---
title: "AutoWorld: Scaling Multi-Agent Traffic Simulation with Self-Supervised World Models"
title_zh: AutoWorld：通过自监督世界模型扩展多智能体交通仿真
authors: "Mozhgan Pourkeshavatz, Tianran Liu, Nicholas Rhinehart"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28963v1"
tags: ["query:navwm"]
score: 9.0
evidence: 从无标签占据表示中学习的世界模型
tldr: 本研究提出AutoWorld框架，通过在无标签LiDAR占据表示数据上训练自监督世界模型，实现多智能体交通仿真的高效扩展。该方法结合粗到细的场景预测与基于行列式点过程的多样性采样机制，并引入运动感知潜监督目标，在无额外标注的情况下显著提升仿真逼真度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28963v1/fig-001.webp\", \"caption\": \"Fig. 5: Qualitative analysis of multimodal behavior coverage. The ground-truth ego trajectory, candidate SDC paths from WOMD, and 32 rollouts generated by AutoWorld are visualized (left to right). The model produces diverse behaviors within a single scenario, including alternative turning directions and lane changes, with diversity that also reflects different kinematic profiles.\", \"page\": 21, \"index\": 1, \"width\": 664, \"height\": 221}, {\"url\": \"assets/figures/arxiv/2603.28963v1/fig-002.webp\", \"caption\": \"Table 1: Results on the WOSAC leaderboard. RMM (Realism Meta Metric) is the primary ranking metric. The “†” denotes technical reports for the Waymo challenge.\", \"page\": 10, \"index\": 2, \"width\": 725, \"height\": 215}, {\"url\": \"assets/figures/arxiv/2603.28963v1/fig-003.webp\", \"caption\": \"Fig. 2: Overview of AutoWorld. A LiDAR-based world model is first trained on unlabeled sequences to learn latent scene dynamics. At simulation time, the trained model predicts future occupancies from the observed LiDAR history, which condition the motion generation module. At inference, we sample from both the world model and the motion generator using a training-free cascaded latent diversity strategy.\", \"page\": 4, \"index\": 3, \"width\": 614, \"height\": 440}, {\"url\": \"assets/figures/arxiv/2603.28963v1/fig-004.webp\", \"caption\": \"Table 2: Ablation study on WOSAC 2% validation split. The “†” denotes the model we used to submit on the leaderbord.\", \"page\": 12, \"index\": 4, \"width\": 725, \"height\": 296}]"
motivation: 现有交通仿真依赖有标签数据，难以扩展，而无标签传感数据尚未被充分利用。
method: 方法基于自监督世界模型和行列式点过程采样构建多智能体运动生成框架。
result: 在WOSAC基准上，AutoWorld在真实性指标上排名第一，并通过消融实验验证各模块有效性。
conclusion: AutoWorld证明了利用无标签数据可有效提升交通仿真真实感。
---

## 摘要
多智能体交通仿真在自动驾驶系统的开发与测试中起着核心作用。近年来的数据驱动仿真器取得了令人鼓舞的成果，但它们高度依赖于带标签轨迹或语义注释的监督学习，从而导致其性能扩展成本高昂。与此同时，大量无标签的传感器数据可以轻松采集，却在现有交通仿真框架中基本未被利用。这引出了一个关键问题：如何利用无标签数据来提升交通仿真的性能？在本研究中，我们提出了 AutoWorld——一个基于世界模型的交通仿真框架，该模型通过学习无标签的 LiDAR 数据占据表示而建立。利用世界模型的样本，AutoWorld 构建从粗到细的预测场景上下文，作为多智能体运动生成模型的输入。为促进样本多样性，AutoWorld 采用级联的行列式点过程（Determinantal Point Process, DPP）框架来引导世界模型与运动模型的采样过程。此外，我们设计了一种对运动感知的潜在监督目标，以增强 AutoWorld 对场景动态的表征。基于 WOSAC 基准的实验结果表明，AutoWorld 在主要的 Realism Meta Metric (RMM) 指标上排名第一。进一步的研究显示，随着无标签 LiDAR 数据的加入，仿真性能持续提升。我们还通过消融实验分析了各组件的有效性。该方法为在无需额外标注的情况下扩展交通仿真的真实感铺平了道路。我们的项目页面包含更多可视化结果与开源代码。

## Abstract
Multi-agent traffic simulation is central to developing and testing autonomous driving systems. Recent data-driven simulators have achieved promising results, but rely heavily on supervised learning from labeled trajectories or semantic annotations, making it costly to scale their performance. Meanwhile, large amounts of unlabeled sensor data can be collected at scale but remain largely unused by existing traffic simulation frameworks. This raises a key question: How can a method harness unlabeled data to improve traffic simulation performance? In this work, we propose AutoWorld, a traffic simulation framework that employs a world model learned from unlabeled occupancy representations of LiDAR data. Given world model samples, AutoWorld constructs a coarse-to-fine predictive scene context as input to a multi-agent motion generation model. To promote sample diversity, AutoWorld uses a cascaded Determinantal Point Process framework to guide the sampling processes of both the world model and the motion model. Furthermore, we designed a motion-aware latent supervision objective that enhances AutoWorld's representation of scene dynamics. Experiments on the WOSAC benchmark show that AutoWorld ranks first on the leaderboard according to the primary Realism Meta Metric (RMM). We further show that simulation performance consistently improves with the inclusion of unlabeled LiDAR data, and study the efficacy of each component with ablations. Our method paves the way for scaling traffic simulation realism without additional labeling. Our project page contains additional visualizations and released code.

---

## 论文详细总结（自动生成）

# AutoWorld：通过自监督世界模型扩展多智能体交通仿真 — 深度总结

---

## 一、研究背景与核心问题

- **背景动机**：  
  多智能体交通仿真是自动驾驶系统开发与验证的重要环节，要求模拟真实而多样的车辆行为。现有数据驱动方法虽取得进步（如基于 Diffusion、Transformer、CVAEs 的交通行为生成），但严重依赖**大量带标签的轨迹和语义标注数据**。这些数据的采集与标注成本极高，制约了仿真系统的可扩展性。

- **核心问题**：  
  如何**利用无标签传感数据（尤其是 LiDAR 点云）**提升交通仿真性能？  
  AutoWorld 的目标是打破监督学习依赖，通过**自监督世界模型**学习场景动态，实现大规模、低成本的高真实度交通模拟。

---

## 二、方法论解析

### 1. 核心思想
AutoWorld 采用“**世界模型驱动的多智能体仿真框架**”，即：
- 先从**无标签 LiDAR 数据**中学习“世界模型”，捕捉场景的时空动态；
- 再将预测的**未来占据潜变量**作为条件，生成多智能体的动作与轨迹；
- 在推理阶段，通过**行列式点过程（DPP）级联采样**提升场景与行为的多样性。

这种方式使得交通行为生成以“场景动态预测”为基础，不再只是简单的轨迹复现。

### 2. 关键技术模块与流程

#### （1）运动感知潜监督世界模型（Motion-Aware World Model）
- 使用 **VAE 编码** 将 LiDAR 数据体素化并转为潜空间占据表示；
- 基于**Rectified Flow Matching**方法训练潜空间的占据预测模型；
- 引入“**运动感知权重图**”，根据时序占据变化增强动态区域的学习：
  \[
  W_t(i,j) = 1 + \lambda \overline{C_{t,\Delta}}(i,j)
  \]
  加权后损失更关注动态物体而非静态背景。

#### （2）世界模型条件的行为生成（World-model-conditioned Diffusion Policy）
- 将预测的未来潜变量序列构建为“**预测场景上下文**”（Predictive Scene Context）：
  - 使用多头交叉注意（MHCA）汇聚长时序信息；
  - 结合历史状态，通过扩散模型生成多智能体未来动作。
- 表征分为两级：**局部潜变量预测 + 全局场景上下文聚合**，形成粗到细的生成结构。

#### （3）级联潜变量多样性（Cascaded Latent Diversity）
- 使用 **DPP（行列式点过程）** 衡量样本间相似度与质量：
  - 全局多样性在世界模型采样阶段；
  - 局部多样性在行为生成阶段；
- 两级采样过程独立于训练，**推理时无额外训练成本**。
- 该机制促使样本在潜空间中保持“多样但合理”，提升模拟的丰富度与真实感。

#### （4）整体算法流程
1. 使用无标签 LiDAR 序列训练世界模型；
2. 利用世界模型预测未来潜变量；
3. 聚合潜变量形成预测场景上下文；
4. 通过扩散模型生成多智能体轨迹；
5. 使用 DPP 引导采样，实现级联多样性。

---

## 三、实验设计

### 1. 数据集与场景
- 使用 **Waymo Open Dataset (WOD)**：训练世界模型（无标签 LiDAR 数据）。  
- 使用 **Waymo Open Motion Dataset (WOMD)**：训练运动生成模块（带轨迹标签）。  
- 基于 **WOSAC Benchmark（Waymo Open Sim Agents Challenge）** 进行最终评估。

### 2. Benchmark 指标
- **Realism Meta Metric (RMM)**：综合衡量运动的物理合理性、交互质量、地图约束。
- 其他指标包括：
  - **minADE**（最小平均偏移误差）
  - **IoU**（占据预测精度）
  - **FVD**（Fréchet Video Distance，用于时序真实性评估）

### 3. 对比方法
- 与多种先进基线对比，包括：
  - InfGen、SMART、UniMotion、TrajTok、SMART-R1、DecompGAIL、RLFTSim 等。
- AutoWorld 在 **WOSAC 排行榜上 RMM = 0.7865，为当前最高分**。

### 4. 消融实验设计
共进行四类核心试验：
1. **世界模型作用**：有无世界模型的对比。
2. **多样性采样机制**：逐层取消或保留 DPP 组件（场景层、行为层）。
3. **运动感知监督机制**：与语义监督、均匀监督对比。
4. **无标签数据量扩展实验**：从 1× 到 5× 数据规模，验证性能随数据增长的可扩展性。

---

## 四、资源与算力

- **作者未明确报告算力配置**（例如 GPU 型号、训练时长、批次大小、硬件数量只部分出现）。  
  世界模型训练部分提到：
  - 批量大小：8～16；
  - 训练轮数：世界模型 150 epochs，运动生成 100 epochs。  
  但**未说明具体硬件或训练耗时**。

---

## 五、实验数量与充分性评估

- 论文共进行：
  - **1 个主线上基准测试（WOSAC Leaderboard 排名）**；
  - **4 组系统化消融实验**（世界模型、采样多样性、运动感知监督、数据规模）；
  - **附加实验与可视化分析**（多模态覆盖度、场景上下文有效性等）。
- 每组实验包含多种配置（如采样预算 1×32、4×8、8×4 等）。
- 实验**覆盖设计全面，评估维度多样（真实性、交互、地图约束）**，并提供清晰的统计结果。
- 评价标准**来自公开挑战平台（WOSAC）**，具备**公平性与可重复性**。

---

## 六、主要结论与发现

1. **AutoWorld 在 WOSAC 指标中排名第一**（RMM=0.7865），实现当前最真实交通仿真结果。  
2. **世界模型的引入有效改善了交互与物理一致性**，不依赖奖励或策略微调。  
3. **运动感知监督**显著提升动态场景预测能力，接近语义监督的效果。  
4. **无标签数据量越多，现实度指标越高**，验证了自监督学习的可扩展性。  
5. **级联多样性策略有效提升行为多样性与整体真实感**，且无需重新训练。
6. 整体上实现了“**从无标签数据到真实交通行为**”的端到端逻辑。

---

## 七、方法优点与创新亮点

- **自监督世界模型**：首次将无标签 LiDAR 的占据预测用于行为生成。
- **运动感知潜监督目标**：在无语义标签条件下仍能捕捉动态要素。
- **级联多样性采样 (Cascaded DPP)**：推理时增强多样性且无额外训练开销。
- **粗到细的场景上下文结构**：通过 MHCA 聚合未来潜变量提升长期一致性。
- **可扩展性验证完善**：性能随无标签数据增多而线性提升，证明可大规模扩展。

---

## 八、不足与局限

- **算力与资源信息不透明**：未给出 GPU 型号或训练时间，难以评估训练效率。
- **动态场景精度仍略低于语义监督模型**（IoU 动态略有差距）。
- **多样性评估主要依赖潜空间指标**，缺乏真实交通流或安全性评估。
- **实验集中于 Waymo 数据集**，跨地域或不同传感类型的泛化性未验证。
- **推理阶段需多次采样（N×M）**，可能带来实际应用中的计算开销。

---

（完）
