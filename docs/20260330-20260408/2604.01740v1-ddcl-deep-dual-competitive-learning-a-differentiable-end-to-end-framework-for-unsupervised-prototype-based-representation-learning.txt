Title: DDCL: Deep Dual Competitive Learning: A Differentiable End-to-End Framework for Unsupervised Prototype-Based Representation Learning

URL Source: https://arxiv.org/pdf/2604.01740v1

Published Time: Fri, 03 Apr 2026 00:36:56 GMT

Number of Pages: 50

Markdown Content:
# DDCL: Deep Dual Competitive Learning: A Differentiable End-to-End Framework for Unsupervised Prototype-Based Representation Learning 

Giansalvo Cirrincione 

> Lab. LTI, Université de Picardie Jules Verne, Amiens, France

Abstract 

A persistent structural weakness in deep clustering is the disconnect between feature learning and cluster assignment. Most architectures invoke an exter-nal clustering step, typically k-means, to produce pseudo-labels that guide training, preventing the backbone from directly optimising for cluster quality. This paper introduces Deep Dual Competitive Learning (DDCL), the first fully differentiable end-to-end framework for unsupervised prototype-based representation learning. The core contribution is architectural: the external k-means is replaced by an internal Dual Competitive Layer (DCL) that generates prototypes as native differentiable outputs of the network. This single inversion makes the complete pipeline, from backbone feature extraction through prototype generation to soft cluster assignment, trainable by backpropagation through a single unified loss, with no Lloyd iterations, no pseudo-label discretisation, and no external clustering step. To ground the framework theoretically, the paper derives an exact alge-braic decomposition of the soft quantisation loss into a simplex-constrained reconstruction error and a non-negative weighted prototype variance term. This identity reveals a self-regulating mechanism built into the loss geome-try: the gradient of the variance term acts as an implicit separation force that resists prototype collapse without any auxiliary objective, and leads to a global Lyapunov stability theorem for the reduced frozen-encoder system. Six blocks of controlled experiments validate each structural prediction. The decomposition identity holds with zero violations across more than 

10 5 training epochs; the negative feedback cycle is confirmed with Pear-1

> arXiv:2604.01740v1 [cs.LG] 2 Apr 2026

son r = −0.98 ; with a jointly trained backbone, DDCL outperforms its non-differentiable ablation by +65% in clustering accuracy and DeepCluster end-to-end by +122% .

Keywords: competitive learning, deep clustering, differentiable clustering, dual competitive layer, Lyapunov stability, prototype learning 

1. Introduction 

Unsupervised representation learning is a central challenge in machine learning: given a large unlabeled dataset, the goal is to learn feature repre-sentations that capture the intrinsic structure of the data without relying on manual annotations [21]. Clustering-based approaches offer a natural frame-work for this task, grouping similar samples together and using the resulting structure to guide feature learning [8]. Convolutional neural networks trained on large supervised datasets [22] have become the dominant paradigm for visual representation learning. Yet the requirement for labeled data is a fundamental bottleneck: annotations are expensive, domain-specific, and unavailable in many scientific settings such as genomics [23] and medical imaging. This motivates the development of unsupervised methods capable of learning rich representations from raw data alone. 

1.1. Motivation: the disconnect problem 

A fundamental limitation shared by many deep clustering methods is the disconnect between representation learning and cluster assignment. Methods such as DeepCluster [1], DEC [3], and Deep k-Means [6] perform feature extraction and clustering in separate stages, preventing the two objectives from being jointly optimized through backpropagation. In DeepCluster [1], the most prominent example of this paradigm, a CNN backbone extracts features which are then clustered by k-means; the cluster assignments are converted to discrete pseudo-labels and used to train the backbone in a subsequent supervised step. This two-stage procedure has been shown to yield strong unsupervised representations, but its structural disconnect prevents the backbone from directly optimizing for cluster quality:   

> Email address: exin@u-picardie.fr (Giansalvo Cirrincione)

it can only predict whatever labels the current k-means iteration happens to produce. 

1.2. Contribution: DDCL 

This paper introduces Deep Dual Competitive Learning (DDCL), the first fully differentiable end-to-end framework for unsupervised prototype-based deep clustering. The core contribution is architectural: the external k-means is replaced by an internal Dual Competitive Layer (DCL) [2], which feeds the transposed feature matrix to the competitive layer so that prototypes emerge as native network outputs rather than internal weight vectors. This single inversion makes the complete backbone–prototype–assignment pipeline train-able by backpropagation through a single unified loss. The theoretical analy-sis that follows, including the loss decomposition, collapse analysis, feedback stability, and global Lyapunov theorem, constitutes a rigorous foundation for understanding why this architectural choice works. The main contributions are: 1. The DDCL framework : a unified differentiable architecture replac-ing external k-means with an internal DCL module, in both batch and incremental formulations. 2. Loss decomposition theorem : a proof that Lq = LOLS + V where 

V ≥ 0 is the weighted prototype variance under the soft assignment distribution. 3. Gradient analysis : complete characterization of gradients with re-spect to assignments, prototypes, and backbone parameters, including the role of stop-gradient. 4. Collapse analysis : proof that Lq makes prototype collapse a first-order locally unstable saddle via the implicit separation force ∇P V =2P Σqn , where P ∈ Rd×k is the prototype matrix, Σqn = diag( qn) −

qnq⊤ 

> n

∈ Rk×k is the soft-assignment covariance matrix, and qn ∈ ∆k−1

is the soft assignment vector of sample n (all formally defined in Sec-tion 5); this force is absent in LOLS .5. Feedback stability : identification and linearized analysis of a nega-tive feedback loop between prototype separation and assignment con-centration, with explicit stability conditions. 6. Global Lyapunov stability : a rigorous global stability theorem for the reduced (frozen-encoder) DDCL flow, establishing that the regu-larized energy E(P, Q ), where P ∈ Rd×k is the prototype matrix and 3Q = {qn}Nn=1 is the collection of soft assignment vectors, is a Lya-punov function, all trajectories remain bounded, and every trajectory converges to the KKT stationary set. 7. Extensions : principled extension to CNNs beyond AlexNet, recurrent architectures, and Transformers. It is important to emphasize the scope of the theoretical results. The decomposition theorem and gradient analysis are exact algebraic identities. The collapse analysis establishes a first-order local instability of the col-lapsed configuration under Lq, not a global convergence guarantee for the full nonconvex deep system. The feedback stability analysis is a linearized characterization of the reduced prototype–assignment dynamics, providing structural insight and a practical design rule rather than a full nonlinear convergence proof. The global Lyapunov theorem (Section 5.6) is a genuine global result, but applies to the reduced system with frozen encoder features; global asymptotic stability of the full end-to-end DDCL system remains an open problem (Section 5.7). The goal of this paper is not to establish large-scale state-of-the-art benchmark performance, but to provide the theoretical foundation and controlled validation of a differen-tiable prototype-based deep clustering framework. The numerical experiments in Section 7 are designed to verify these structural predictions; large-scale GPU validation on standard visual benchmarks is identified as the primary direction for future work. 

1.3. Paper organization 

Section 2 reviews related work. Section 3 recalls DeepCluster and DCL. Section 4 introduces the DDCL framework. Section 5 develops the theoret-ical analysis, culminating in the global Lyapunov stability result. Section 6 discusses architectural extensions. Section 7 presents numerical validation. Section 8 provides a practical discussion. Section 9 concludes. Proofs are collected in the Appendices. 

2. Related Work 

2.1. Deep clustering 

Deep clustering methods [8] combine representation learning with clus-tering objectives. DEC [3] minimizes the KL divergence between soft assign-ments and a sharpened target distribution, jointly training an autoencoder 4and cluster centers; its improved variant IDEC [25] preserves local structure through a reconstruction term. JULE [4] alternates between agglomerative clustering and network updates in a recurrent framework. Deep Adaptive Image Clustering [5] learns mappings that bring augmented pairs close in feature space. Deep k-Means [6] embeds k-means directly into the network loss via a differentiable relaxation, but without a dedicated prototype gen-eration module. Deep Subspace Clustering [7] uses self-expressive layers to capture subspace structure. Two recent methods pursue end-to-end differentiable clustering through a different route. ClAM [34] (ICML 2023) proposes a continuous relaxation of the discrete assignment problem using Associative Memory dynamics, en-abling differentiable clustering via SGD; however, it operates in the ambient data space and does not learn latent representations, limiting its applicability to high-dimensional data. DCAM [35] (NeurIPS 2024) extends this idea to latent space by using Associative Memory attractor dynamics as an induc-tive bias inside an autoencoder, making the encoder, decoder, and cluster prototypes jointly differentiable. DDCL differs from both in three funda-mental respects: (i) prototypes are native outputs of the backbone graph via the DCL module, not latent attractors of a recurrent memory system; (ii) no decoder is required, so the framework is composable with any backbone archi-tecture; (iii) the competitive learning formulation admits a rigorous algebraic decomposition and Lyapunov analysis that have no analogue in associative memory methods. More recent methods exploit self-supervised pretraining. SCAN [26] de-couples representation learning from clustering assignment in two explicit phases: a contrastive pretraining step followed by a neighborhood-based clus-ter assignment. DINO [27] shows that Vision Transformer features trained with self-distillation contain rich semantic structure directly exploitable for clustering without further training. CC [28] combines instance-level and cluster-level contrastive objectives in a single end-to-end framework. Pro-totypical contrastive learning (PCL, [29]) explicitly maintains cluster proto-types and uses them as additional contrastive anchors, arriving at a similar motivation to DDCL but from a contrastive rather than competitive learning perspective, and without the theoretical decomposition Lq = LOLS + V .The key structural difference between DDCL and all these methods is summarized as follows. SwAV [15], which combines contrastive learning with online clustering by assigning augmented views to shared prototypes, and PCL use prototypes as contrastive anchors within an augmentation-based 5objective: prototypes are cluster centroids maintained outside the backbone graph and updated via an EM or momentum step, not as differentiable out-puts. SCAN separates pretraining from clustering in two distinct phases with no joint gradient signal. DDCL differs in three specific ways: (i) pro-totypes are native outputs of the backbone graph (DCL module), not exter-nal centroids; (ii) the entire system is optimized by a single unified loss Lq

with continuous gradient flow; (iii) the decomposition Lq = LOLS + V and its consequences (collapse resistance via ∇P V , the feedback cycle, and the Lyapunov theorem) have no analogue in contrastive or EM-based prototype methods. The present paper focuses on the theoretical analysis of this in-tegration; empirical comparison with SCAN, DINO, and PCL on large-scale visual benchmarks is left for future work. Table 1 provides a structured comparison of DDCL against the most rel-evant existing methods along five dimensions: whether prototypes are native outputs of the backbone graph, whether the framework requires a recon-struction decoder, whether training uses a single differentiable loss with no alternating steps, whether data augmentation is needed, and whether a rig-orous theoretical analysis of the loss geometry accompanies the method. The table covers the full spectrum from classical deep clustering (DEC, DeepClus-ter, Deep k-Means) through modern self-supervised methods (SwAV, PCL) to the two most closely related differentiable clustering approaches (ClAM, DCAM).      

> Table 1: Structural comparison of deep clustering methods ( ✓= satisfied, ×= not satis-fied). †: ambient data space only, no latent representation. ‡: requires encoder + decoder.

Method Native protos No decoder Unified loss No augmentation Theory DEC [3] × × ✓ ✓ ×

DeepCluster [1] × ✓ × ✓ ×

Deep k-Means [6] × ✓ ✓ ✓ ×

SwAV [15] × ✓ ✓ × ×

PCL [29] × ✓ ✓ × ×

ClAM † [34] × ✓ ✓ ✓ ×

DCAM ‡ [35] × × ✓ ✓ ×

DDCL (proposed) ✓ ✓ ✓ ✓ ✓

The key observation from Table 1 is that DDCL is the only method that 6simultaneously satisfies all five criteria. The columns denote: Native protos 

— prototypes are differentiable outputs of the backbone graph, not external centroids; No decoder — no reconstruction decoder is required; Unified loss 

— a single differentiable objective with continuous gradient flow; No aug-mentation — no data augmentation or contrastive views; Theory — rigorous analysis of loss geometry and dynamics. The most discriminating column is 

Native protos : no prior method generates prototypes as differentiable outputs of the backbone computational graph. DEC and DeepCluster maintain clus-ter centres as external parameters updated outside the gradient flow. SwAV and PCL update prototypes via Sinkhorn-Knopp or EM steps that are decou-pled from the backbone. ClAM and DCAM achieve differentiable clustering but through associative memory dynamics rather than competitive learn-ing: ClAM operates only in the ambient data space without representation learning, while DCAM requires a full autoencoder and uses memory attrac-tors as prototypes rather than native network outputs. The Theory column reflects that, unlike all of these methods, DDCL comes with a rigorous anal-ysis: an exact loss decomposition, a characterisation of collapse resistance, a linearised feedback stability result, and a global Lyapunov theorem for the reduced system. Self-supervised methods [9, 10] avoid explicit clustering by designing pretext tasks. More recently, contrastive methods such as SimCLR [11] and MoCo [12] learn representations by contrasting augmented views of the same image. BYOL [13] and SimSiam [14] operate without negative pairs. SwAV [15] bridges contrastive and clustering objectives (discussed in Sec-tion 2 in relation to DDCL). 

2.2. Competitive learning 

Competitive learning [16] is a biologically inspired paradigm where neu-rons compete to represent input patterns. The Self-Organizing Map (SOM) [17] organizes prototypes on a fixed low-dimensional grid, combining competi-tion with topology preservation. Growing Neural Gas (GNG) [18] relaxes the fixed topology by incrementally adding units where the quantization error is highest. Both methods learn prototype positions through Competitive Heb-bian Learning (CHL) [19], which encodes the first- and second-winner struc-ture in a topology graph. The GH-EXIN network [20] extends these ideas to hierarchical clustering by nesting competitive layers at multiple granularity levels. 7A fundamental limitation shared by all these methods is that prototypes are weight vectors internal to a single layer: they are not accessible as dif-ferentiable outputs and cannot be backpropagated through from a down-stream loss. The DCL [2] resolves this by transposing the input matrix before the competitive layer, so that prototypes appear as explicit, differen-tiable columns of the output matrix (formally defined in Section 3.2). This architectural inversion makes prototype generation fully differentiable and composable with any downstream objective. It also enables the gradient boundedness property (DCL gradients live in the n-dimensional data sub-space regardless of d, the feature dimension), which is central to the high-dimensional robustness of DDCL. The regression interpretation of DCL, con-necting competitive learning to OLS and its variants (TLS, weighted LS), is developed in [2] and further grounded in the neural data-fitting framework of [30]. 

3. Background 

3.1. DeepCluster 

Let {xn}Nn=1 be N unlabeled input samples. A backbone CNN fθ with parameters θ extracts features zn = fθ(xn) ∈ Rd, collected in the feature ma-trix Fθ = [ z1, . . . , z N ] ∈ Rd×N , where d is the feature dimension. Throughout the paper, k denotes the number of prototypes (clusters), specified a priori .DeepCluster [1] proceeds as follows: 1. Feature preprocessing : PCA to D ≪ d dimensions, ℓ2-normalization, whitening. 2. External clustering : run k-means on {zn} to obtain centroids C ∈

Rd×k and hard assignments yn ∈ { 0, 1}k, y⊤ 

> n

1k = 1 .3. Pseudo-label training : train fθ and a linear classifier head gϕ (weights 

ϕ) to predict the pseudo-labels. The classifier output is: 

ˆsn = softmax( gϕ(zn)) ∈ Rk, (1) where ˆsn,j is the predicted probability of sample n belonging to cluster 

j. The cross-entropy loss is: LDC = − 1

> N

P 

> n

log ˆ sn,y n , where yn ∈{1, . . . , k } is the integer cluster index assigned by k-means (i.e., the component of yn that equals 1 in the one-hot encoding). 4. Repeat until convergence. 8The k-means step solves: 

min 

> C, {yn}

1

N

X

> n

∥zn − Cy n∥2 s.t. yn ∈ { 0, 1}k, y ⊤ 

> n

1k = 1 . (2) DeepCluster includes two mechanisms to prevent trivial solutions: empty cluster reassignment and uniform cluster sampling [1]. The fundamental limitation is structural: k-means is external to the com-putational graph. Gradients do not flow through the assignment step, so the backbone cannot directly optimize for cluster quality. 

3.2. The Dual Competitive Layer (DCL) 

The DCL was introduced in [2] as a gradient-based competitive learning module that generates prototypes as differentiable network outputs by oper-ating on the transposed feature matrix. In what follows, X ∈ Rn×d denotes a generic input matrix of n samples (which in the DDCL context corresponds to the current batch, n ≤ N ); d is the feature dimension and k the number of prototypes, as defined in Section 3. The construction and the two properties most relevant to DDCL are recalled below. 

Vanilla Competitive Layer (VCL). The VCL is the classical competitive learning unit [16, 17] as it appears in the neural network literature. Given an input matrix X ∈ Rn×d (n samples, each with d features, arranged as rows), a VCL with k neurons (where k is specified a priori and may not equal the true number of clusters in the data) has weight matrix W1 ∈ Rk×d,where each row w⊤ 

> j

∈ Rd is a prototype vector. For each sample xi ∈ Rd, the winning neuron is j∗ = arg min j ∥xi − wj ∥2, and the loss is the quantization error: 

Q =

> n

X

> i=1

min  

> j

∥xi − wj ∥2. (3) Prototypes are stored internally as weight vectors and are not directly acces-sible as network outputs; they cannot be connected to a downstream differ-entiable loss. 

Dual Competitive Layer (DCL). The key insight of [2] is to feed the trans-posed matrix X⊤ ∈ Rd×n to the competitive layer. With n input neurons and k output neurons, the DCL weight matrix is W2 ∈ Rn×k, whose j-th 9column w2,j ∈ Rn contains the weights connecting all n input neurons to the 

j-th output neuron. The output layer activation matrix is: 

P = Y2 = X⊤W2 ∈ Rd×k, (4) where Y2 denotes the output activation matrix of the DCL (i.e., the result of multiplying the transposed input X⊤ by the weight matrix W2), whose 

j-th column pj = X⊤w2,j ∈ Rd is the j-th prototype, expressed as a linear combination of the input data columns weighted by w2,j . Prototypes are now explicit network outputs: gradients from any downstream loss flow through 

P back into W2 and, via X⊤, into the backbone that produced X.The DCL loss augments the quantization error with a topology regular-ization term: 

LDCL = Q + λ∥E∥2, (5) where E ∈ Rk×k is the Competitive Hebbian Learning (CHL) adjacency matrix [19], encoding the first- and second-winner co-activation structure across the training samples. Minimizing ∥E∥2 penalizes dense connectivity between prototypes, enforcing a sparse topology : only pairs of prototypes that are mutually nearest neighbours are linked, producing a graph whose connected components tend to reflect the true cluster structure of the data. This is particularly valuable in practice, since k is set a priori and may overestimate the actual number of clusters; the sparse topology provides a visual and algorithmic means of identifying the true cluster count without rerunning the model. 

Duality and equivalence. Reference [2] proves (Theorems 2.1–2.4) that VCL and DCL produce the same prototype geometry under the assumption that input samples are uncorrelated with unit variance, a condition practically achieved by batch normalization. The two architectures are therefore equiv-alent in representation power while DCL is strictly richer in connectivity: the same competitive dynamics, but with prototypes promoted to explicit, differentiable output tensors that participate fully in the computation graph. 

Gradient subspace property. Theorems 3.4–3.5 of [2] establish that the DCL gradient ∇W2 LDCL lies in the column space of X⊤, an n-dimensional sub-space of Rd. This result has a deep practical consequence that is central to DDCL’s behavior in high-dimensional settings. In the ambient space Rd,most directions are uninformative: when d ≫ n, only n linearly independent 10 directions are spanned by the data, and the remaining d − n directions con-tain only noise. The gradient subspace property means that DCL updates are 

automatically confined to the n-dimensional data subspace , a geometric tun-nel aligned with the actual information content of the batch, and completely immune to the noise that contaminates the d − n orthogonal directions. This is not a regularization imposed from outside but a structural property of the dual architecture: because the prototype pj = X⊤w2,j is formed by taking a linear combination of the n data vectors, its gradient can never escape their span. The practical effect is striking: as d grows beyond n, meth-ods that operate in the full ambient space (including standard k-means and DeepCluster) see their signal-to-noise ratio collapse, while DDCL maintains meaningful gradient updates through this subspace tunnel. Block 3 (Sec-tion 7.3) validates this property experimentally across six dimensionalities from d = 10 to d = 5000 , showing that DDCL( Lq) degrades gracefully at 

d > n while all ambient-space methods converge to chance level. 

4. Deep Dual Competitive Learning (DDCL) 

4.1. Conceptual shift 

DDCL replaces the external k-means in DeepCluster with a DCL block. The transpose of the feature matrix Fθ ∈ Rd×N is passed to DCL: 

F ⊤

> θ
> DCL

−−→ P = [ p1, . . . , p k] ∈ Rd×k. (6) DCL outputs the prototype matrix P as network outputs, differentiable end-to-end. The full pipeline is: 

xn → fθ → zn, F ⊤ 

> θ

→ DCL → P, (zn, P ) → qn → L . (7) Fig. 1 illustrates the batch DDCL pipeline with CNN backbone. 

4.2. Batch DDCL with cross-entropy 

The simplest DDCL variant retains the pseudo-label logic of DeepCluster. From P , define soft assignments with temperature T > 0:

qnj = exp( −∥ zn − pj ∥2/T )

P 

> ℓ

exp( −∥ zn − pℓ∥2/T ), (8) 11 Figure 1: Batch DDCL pipeline with CNN backbone (see Section 4). Blue: forward pass; red: backpropagation. 

and train a linear classifier head with weight matrix Wc ∈ Rk×d and bias 

b ∈ Rk, with output probability vector 

ˆsn = softmax( Wczn + b) ∈ Rk, (9) via the soft cross-entropy loss: 

LsoftCE = − 1

N

X

> n

X

> j

qnj log ˆ snj . (10) Here ˆsnj is the predicted probability that sample n belongs to cluster j,and qnj provides the soft target in place of the hard pseudo-labels used by DeepCluster. 

4.3. Unified batch DDCL 

In the unified formulation, the separate classifier head is eliminated. The soft quantization loss is: 

Lq = 1

N

> N

X

> n=1
> k

X

> j=1

qnj ∥zn − pj ∥2, (11) 12 with qnj from (8) denoting the j-th component of the soft assignment vector 

qn ∈ ∆k−1. This single loss jointly trains the backbone fθ and the DCL module, requiring no Lloyd iterations, no hard pseudo-labels, and no classifier head. To prevent trivial solutions, the total loss augments Lq:

Ltot = Lq + βL bal + γL ent + ηL sep + λ 

> 2

∥P ∥2 

> F

, (12) where β, γ, η, λ ≥ 0 are non-negative weights, ¯q = 1

> N

P 

> n

qn ∈ ∆k−1 is the mean soft assignment across the batch, and: 

Lbal = KL 



¯q 1 

> k

1k



, (13) 

Lent = 1

N

X

> n

H(qn), H(qn) = − X

> j

qnj log qnj , (14) 

Lsep = − X

> i<j

∥pi − pj ∥2. (15) Each term addresses a specific failure mode. Lbal is the KL divergence be-tween the mean assignment ¯q and the uniform distribution 1 

> k

1k: minimizing it encourages all k clusters to be used roughly equally, preventing the model from collapsing to a solution where most samples are assigned to a single prototype. Lent is the mean Shannon entropy H(qn) = − P 

> j

qnj log qnj of the per-sample soft assignments: maximizing it (i.e., penalizing its nega-tive) keeps assignments soft and spread, avoiding premature hard conver-gence before meaningful prototype positions have been established. Lsep =

− P 

> i<j

∥pi − pj ∥2 directly penalizes pairs of prototypes that are too close, providing an explicit push-apart force that complements the implicit sepa-ration already provided by ∇P V (Section 5.4). The quadratic term λ 

> 2

∥P ∥2

> F

prevents prototypes from drifting to infinity under the repulsion of Lsep ; its role in ensuring coercivity and hence the global Lyapunov result is discussed in Remark 1. 

Remark 1 (Role of the quadratic term (λ/ 2) ∥P ∥2 

> F

). The explicit separa-tion term Lsep is unbounded below as ∥pi − pj ∥ → ∞ , so it cannot by itself guarantee that prototype trajectories remain bounded. The quadratic regular-izer (λ/ 2) ∥P ∥2 

> F

(λ > 0) introduces a restoring force towards the origin that, together with Lsep , confines the prototypes to a bounded region: Lsep pushes prototypes apart, while (λ/ 2) ∥P ∥2 

> F

prevents them from drifting to infinity. 

13 This coercivity is essential for the global Lyapunov result of Section 5.6. In practice λ is chosen small (e.g. 10 −4–10 −3) so that its effect on prototype geometry is negligible. 4.4. Constrained OLS interpretation 

The name LOLS reflects the classical Ordinary Least Squares problem from linear regression. Given a feature vector zn ∈ Rd and a prototype matrix P = [ p1, . . . , p k] ∈ Rd×k, the goal is to find an assignment vector 

qn ∈ Rk such that the linear combination P q n approximates zn as closely as possible. This is precisely the OLS problem min qn ∥zn − P q n∥2, which in unconstrained form has the closed-form solution qn = ( P ⊤P )−1P ⊤zn. The constraint qn ∈ ∆k−1, i.e., the entries of qn are non-negative and sum to one, turns it into a simplex-constrained OLS problem, which no longer has a closed form but admits efficient projected-gradient solvers. Relaxing the hard one-hot constraint in (11) to the probability simplex ∆k−1 = {q ≥ 0 : 1⊤q = 1 }

gives the simplex-constrained OLS loss :

LOLS = ∥zn − P q n∥2 = ∥zn − ¯pn∥2, ¯pn = P q n. (16) Hard DDCL ( T → 0) recovers a discrete one-hot OLS problem equivalent to 

k-means. Soft DDCL is a continuous simplex-constrained OLS problem. 

Remark 2 (Beyond OLS: noise assumptions and regression variants) . OLS assumes that observation errors lie entirely in the feature space zn (errors-in-response model). When both observations zn and prototype coordinates P

are subject to noise, Total Least Squares (TLS) provides a more appropri-ate formulation, minimizing orthogonal distances to the prototype subspace rather than vertical residuals. More generally, the choice of regression cri-terion (OLS, TLS, weighted LS, or robust variants) should be guided by the noise structure of the representation space; a systematic treatment of these regression problems and their neural implementations is given in [30]. 4.5. Incremental DDCL 

In the incremental formulation, prototype rows are generated sequen-tially: the DCL produces one row r(t)⊤ ∈ Rk per step. After t steps, the par-tial prototype matrix is P1: t ∈ Rt×k. Defining truncated features zn, 1: t ∈ Rt,the incremental assignment update is: 

q(t) 

> n

= arg min 

> q∈∆k−1

∥zn, 1: t − P1: tq∥2. (17) 14 This is solved online via Widrow–Hoff with simplex projection: 

e(t) 

> n

= znt − r(t)⊤q(t−1)  

> n

, (18) 

˜q(t) 

> n

= q(t−1)  

> n

+ μ e (t) 

> n

r(t), (19) 

q(t) 

> n

= Π k−1∆

 ˜q(t)

> n

 , (20) where e(t) 

> n

∈ R is the scalar prediction error at step t, μ > 0 is the Widrow– Hoff step size, and Πk−1∆ is the Euclidean projection onto ∆k−1, computed via the efficient algorithm of [2]. 

Simplex projection. The Euclidean projection Πk−1∆ (v) maps any vector v ∈

Rk to the nearest point in the probability simplex ∆k−1 = {q ∈ Rk : q ≥

0, 1⊤q = 1 }. It can be computed exactly in O(k log k) time via the following sorting-based algorithm [2]: 1. Sort v in descending order: v(1) ≥ v(2) ≥ · · · ≥ v(k).2. Find the largest ρ ∈ { 1, . . . , k } such that v(ρ) − 1

> ρ

Pρj=1 v(j) − 1



> 0.3. Set the threshold θ∗ = 1

> ρ

Pρj=1 v(j) − 1



.4. Return Πk−1∆ (v) = max( v − θ∗1k, 0) .This operation replaces the unconstrained update ˜q(t) 

> n

with the nearest fea-sible assignment vector, ensuring q(t) 

> n

∈ ∆k−1 at every step with no iterative inner loop. A complete pseudocode of the incremental loop is given in Ap-pendix H. The total incremental objective is: 

Linc =

> d

X

> t=1

αtL(t), L(t) = 1

N

X

> n

∥zn, 1: t − P1: tq(t) 

> n

∥2, (21) with non-negative weights αt typically increasing with t.

5. Theoretical Analysis 

The core theoretical results are developed below. Throughout this section a single sample zn ∈ Rd, prototypes P = [ p1, . . . , p k] ∈ Rd×k, and a soft assignment qn ∈ ∆k−1 are fixed. The notation is: ¯pn = P q n (prototype mixture), dn = ( ∥zn − p1∥2, . . . , ∥zn − pk∥2)⊤ ∈ Rk (distance vector). All proofs are in the Appendices. 15 5.1. Loss decomposition 

Theorem 1 (Loss decomposition) . For any zn ∈ Rd, P ∈ Rd×k, qn ∈ ∆k−1:

Lq = LOLS + V (P, q n), (22) 

where the variance term 

V (P, q n) = X

> j

qnj ∥pj − ¯pn∥2 ≥ 0 (23) 

is the weighted variance of the prototypes under qn. Equality holds iff qn is a vertex of ∆k−1 or all active prototypes coincide. 

Corollary 1 (Upper bound) . 0 ≤ V (P, q n) ≤ 14 diam( P )2, where diam( P ) = max i̸=j ∥pi − pj ∥ is the prototype diameter. 

The decomposition (22) reveals that Lq always overestimates LOLS by the prototype variance. The gap vanishes only in the hard-assignment limit and grows with prototype dispersion. 

5.2. Temperature and entropic structure 

Since Lq = d⊤ 

> n

qn is linear in qn, its unconstrained minimizer over ∆k−1 is always a vertex (hard assignment, equivalent to k-means). Soft assignments arise solely from entropic regularization: 

Proposition 1 (Entropic regularization) . The soft assignments in (8) , qnj (T ) ∝

exp( −dnj /T ), are the unique minimizer of: 

min 

> qn∈∆k−1

d⊤ 

> n

qn − T · H(qn) , (24) 

with lim T →0 qn(T ) = ej∗ (k-means hard assignment, where ej∗ ∈ { 0, 1}k is the standard basis vector for the nearest prototype j∗ = arg min j dnj ) and 

lim T →∞ qn(T ) = 1 

> k

1k (uniform). 

Corollary 2 (Monotonicity of V ). V (P, q n(T )) is monotone non-decreasing in T , with V |T →0 = 0 and V |T →∞ = Var( P ) ≜ 1

> k

P 

> j

∥pj ∥2 − ∥ 1

> k

P 

> j

pj ∥2 (the unweighted prototype variance). 

Temperature T is therefore the master parameter of DDCL: it controls the softness of assignments, the magnitude of V , the backbone learning signal, and the implicit separation force (Section 5.4). 16 5.3. Gradient analysis 

Define the assignment covariance matrix: 

Σqn = diag( qn) − qnq⊤ 

> n

∈ Rk×k. (25) 

Proposition 2 (Gradients w.r.t. qn).

∇qn LOLS = 2 P ⊤(¯ pn − zn), (26) 

∇qn Lq = dn (constant in qn), (27) 

∇qn V = ( ∥pj ∥2)j − 2P ⊤ ¯pn. (28) 

Proposition 3 (Gradients w.r.t. P ).

∇P LOLS = 2(¯ pn − zn)q⊤ 

> n

, (29) 

∇P Lq = 2( P − zn1⊤ 

> k

)diag( qn), (30) 

∇P V = 2 P Σqn . (31) 

Proposition 4 (Gradients w.r.t. zn). Under stop-gradient on qn:

∇zn Lq sg = ∇zn LOLS sg = 2( zn − ¯pn). (32) 

Under full gradient, both losses acquire a correction O(T −1) proportional to 

Σqn , and ∇zn Lq − ∇ zn LOLS = ∇zn V .

Corollary 3 (Stop-gradient as design choice) . With stop-gradient on qn, Lq

and LOLS give identical backbone updates. The difference between the two losses is felt exclusively in the DCL module gradients ∇P and the assignment gradients ∇qn . Without stop-gradient, both losses enrich the backbone signal by a term ∝ T −1Σqn , coupling backbone and assignment dynamics. Properties of Σqn : Σqn ⪰ 0; rank(Σ qn ) ≤ k − 1 with qn ∈ ker(Σ qn ); tr(Σ qn ) = 1 − ∥ qn∥2 ∈ [0 , (k − 1) /k ]; Σqn → 0 as T → 0; Σqn → 1 

> k

Ik − 1 

> k2

11 ⊤ as T → ∞ .

5.4. Prototype collapse 

Prototype collapse (multiple prototypes converging to the same point) is one of the most dangerous failure modes in deep clustering. 

Proposition 5 (LOLS : collapse is a stable fixed point) . If pi = pj = p∗, then 

∂L OLS /∂p i and ∂L OLS /∂p j point in the same direction. No differential force acts to separate them; collapse is a stable attractor of LOLS .

17 Proposition 6 (Lq: collapse is an unstable saddle) . Consider a perturbation 

pi = p∗ + εu , pj = p∗ − εu , ∥u∥ = 1 . Under gradient descent on Lq, the net force along u separating pi from pj is: 

Fsep = 2 ε(qni + qnj ) > 0, (33) 

absent under LOLS at first order. Hence collapse is an unstable saddle of Lq.

The source of this implicit separation is ∇P V = 2 P Σqn . To see why, recall from the decomposition Lq = LOLS + V that minimizing Lq simultaneously minimizes the reconstruction error and the prototype variance V . The gra-dient ∇P V = 2 P Σqn points in the direction that increases V , so the gradient 

descent step on Lq pushes the prototypes in the direction that decreases V

— that is, it increases the spread of the prototype distribution. Since the assignment covariance Σqn = diag( qn) − qnq⊤ 

> n

is positive semidefinite with eigenvalues that grow when assignments are more uniform, the force P Σqn is strongest when the prototypes are close and the assignments are undecided — precisely the regime of incipient collapse. This is why ∇P V acts as a self-regulating separation force: it is automatically largest where it is most needed. 

Proposition 7 (Separation intensity vs. T ). ∥∇ P V ∥F = 2 ∥P Σqn ∥F is mono-tone non-decreasing in T , vanishing as T → 0. At T → ∞ :

∥∇ P V ∥F → 2 P   1 

> k

Ik − 1 

> k2

11 ⊤ 

> F

. (34) 

Corollary 4 (Role of Lsep ). The explicit term Lsep in (15) acts as a backup mechanism, most necessary when T is small and the implicit force ∇P V is weak. The weight η should be chosen inversely proportional to T · tr(Σ qn (T )) .5.5. Negative feedback and stability 

To analyze the self-regulating dynamics of DDCL, three scalar quanti-ties that capture the essential state of the system at each training step. Intuitively, the training process involves two competing tendencies: proto-types want to separate (to cover the data better), while assignments want to sharpen (to commit to a single prototype per sample). The three quantities below make this competition precise. 18 The feedback quantities are defined as: 

S(P ) = 1

(k

> 2

)

X

> i<j

∥pi − pj ∥2 (separation) , (35) 

K(qn) = ∥qn∥2 (concentration, ∈ [1 /k, 1] ), (36) 

I(Σ qn ) = tr(Σ qn ) = 1 − K (separation force) . (37) 

S(P ) measures how spread out the prototypes are on average: it is zero when all prototypes coincide (total collapse) and grows as they move apart. 

K(qn) = ∥qn∥2 measures how concentrated the soft assignment of sample 

n is: it equals 1 when qn is a one-hot vector (hard assignment to a single prototype) and equals 1/k when qn = 1 

> k

1k (maximally uncertain, uniform over all prototypes). I(Σ qn ) = 1 − K is the complementary measure of assignment uncertainty, and — as established by ∇P V = 2 P Σqn above — it directly controls the magnitude of the implicit separation force. The cycle reads: 

S ↑ ⇒ K ↑ ⇒ I ↓ ⇒ ∥∇ P V ∥F ↓ ⇒ S ↓ . (38) This chain of implications describes a self-correcting loop, readable as follows. When prototypes are well separated ( S large), each sample clearly belongs to one prototype: assignments sharpen ( K increases). Sharp assignments mean 

Σqn shrinks toward zero ( I falls), which weakens the implicit separation force 

∥∇ P V ∥F . With less force pushing prototypes apart, separation S naturally decreases — closing the loop. The same logic runs in reverse when proto-types are too close: uniform assignments maximize I, which maximizes the separation force and pushes prototypes apart. The practical consequence is that the system self-corrects in both directions: it resists prototype collapse 

and resists excessive dispersion, without any external intervention. 

Theorem 2 (Negative feedback) . The cycle is negative: it resists both col-lapse ( S → 0) and excessive dispersion ( S → ∞ ), and there exists at least one equilibrium (S∗, K∗).

Linearizing around the equilibrium with perturbations (s, κ ) = ( S − S∗, K − K ∗) gives a 2 × 2 linear dynamical system. Each quantity in the cycle is expanded to first order around (S∗, K∗). The rate of change of separation 

˙s depends on two competing effects: (i) a self-damping term proportional to 

b · s, which captures the direct effect of the backbone pulling features toward 19 prototypes (tending to reduce separation at rate αDCL ) while the backbone also spreads them (at rate αbb ); and (ii) a cross-coupling term −aκ capturing how an increase in assignment concentration (more committed assignments) reduces the separation force. The rate of change of concentration ˙κ is driven entirely by the change in separation +c·s: when prototypes are farther apart, assignments sharpen (Boltzmann softmax becomes more peaked). This gives: 

 ˙s

˙κ



=

b −ac 0

| {z }

> J

sκ



, a, c > 0, b = αbb − αDCL , (39) where αbb , α DCL are effective learning rates of backbone and DCL, respec-tively. The Jacobian J has a clear physical reading: the off-diagonal entry 

−a < 0 encodes the inhibitory effect of sharp assignments on separation (more commitment ⇒ less separation force); the entry +c > 0 encodes the excitatory effect of separation on sharpness (farther prototypes ⇒ clearer winner); the diagonal entry b is the net self-feedback on separation, positive when the backbone dominates and negative when the DCL dominates. 

Theorem 3 (Local stability) . The equilibrium is stable iff b < 0; oscillatory stable (spiral) iff additionally b2 < 4ac ; unstable if b > 0 dominates. 

Corollary 5 (Practical stability condition) .

αbb 

αDCL 

< 1 + 4∥P ∥F

T · ∥∇ zn fθ∥F

. (40) 

The backbone learning rate must not exceed the DCL learning rate by too large a margin. 

The qualitative message of Corollary 5 is important for practitioners. The right-hand side grows with ∥P ∥F (larger prototype spread allows a more dominant backbone) and shrinks with T (lower temperature means sharper assignments, so the system becomes more sensitive to imbalance between 

αbb and αDCL ). In practice this means: keep the backbone learning rate no larger than the DCL learning rate , especially at low temperatures. Aratio αbb /α DCL ∈ [1 /10 , 1/3] is a safe starting point; the right-hand side of the inequality then provides a temperature-dependent bound that can be monitored during training. When the condition is violated, the backbone evolves too fast relative to the prototypes, the negative feedback loop breaks 20 down, and the system can enter an unstable regime in which prototypes fail to track the evolving feature space. The oscillatory-stable regime (when b < 0 and b2 < 4ac ) is worth under-standing in concrete terms. The characteristic polynomial of J has complex conjugate roots with negative real part, which means the solution to (39) is a 

damped spiral : both S(P ) and K(qn) oscillate around their equilibrium values with decreasing amplitude. In practice this manifests as visible oscillations in the clustering accuracy curve during training — the accuracy rises, then dips slightly, rises again, and so on, with each swing smaller than the last. This is not a sign of instability or divergence; it is the natural signature of a sys-tem with negative feedback and inertia, analogous to a mass-spring-damper settling toward equilibrium. Recognizing this pattern allows the practitioner to distinguish healthy convergent oscillations from pathological divergence, and to avoid premature stopping or unnecessary hyperparameter tuning in response to what is, in fact, correct system behavior. 

5.6. Global Lyapunov stability of the reduced DDCL flow 

The feedback stability analysis of Sections 5.5 and Theorems 2–3 is local: it characterizes behavior near the equilibrium via linearization. We now establish a global stability result for the reduced system in which the encoder features are frozen. We work with the following setup. Assume that the encoder features 

zn ∈ Rd, n = 1 , . . . , N , are fixed. Let Q = {qn}Nn=1 with each qn ∈ ∆k−1.Define the regularized reduced DDCL energy: 

E(P, Q ) = 

> N

X

> n=1
> k

X

> j=1

qnj ∥zn − pj ∥2 + βL bal (Q) + γL ent (Q) + ηL sep (P ) + λ 

> 2

∥P ∥2 

> F

.

(41) The term (λ/ 2) ∥P ∥2 

> F

(λ > 0) ensures coercivity in P . Coercivity means, intuitively, that the energy E grows without bound whenever the prototypes drift far from the origin: E(P, Q ) → +∞ as ∥P ∥F → ∞ . This matters because Lsep = − P 

> i<j

∥pi − pj ∥2 is itself unbounded below as prototypes move apart: without a counterweight, the optimization landscape would have no lower bound and trajectories could escape to infinity. The quadratic term (λ/ 2) ∥P ∥2 

> F

acts as a soft barrier: it grows as ∥P ∥2 

> F

, which eventually dominates the −∥ P ∥2 

> F

growth of Lsep and pulls the energy back up. The net effect is that the combined term ηL sep + λ 

> 2

∥P ∥2 

> F

has a bounded minimizer: 21 prototypes are pushed apart by Lsep , but cannot drift arbitrarily far because 

(λ/ 2) ∥P ∥2 

> F

grows faster. Every sublevel set {(P, Q ) : E(P, Q ) ≤ M } is therefore compact in P , which is the key technical condition required to invoke LaSalle’s invariance principle in the proof of Theorem 4. The continuous-time reduced DDCL dynamics are given by the projected gradient flow: 

˙P = −∇ P E(P, Q ), (42) 

˙qn = Π Tqn ∆(−∇ qn E(P, Q )) , (43) where Tqn ∆ denotes the tangent cone of the simplex at qn, and ΠTqn ∆ is the Euclidean projection onto that cone. These dynamics are the continuous-time counterpart of gradient descent on P and projected gradient descent on the simplex-constrained assignment vectors qn.

Theorem 4 (Global Lyapunov stability) . Assume that: 

1. the feature vectors zn are fixed; 

2. E(P, Q ) is continuously differentiable; 

3. Lbal and Lent are bounded below (both are non-negative); 

4. λ > 0.Then every trajectory of the projected gradient flow (42) –(43) is bounded, the energy E(P (t), Q (t)) is non-increasing, and every trajectory converges to the set of KKT stationary points of the constrained problem. Proof. See Appendix F. 

Corollary 6 (Uniqueness implies global asymptotic stability) . If E(P, Q )

has a unique KKT stationary point (P ⋆, Q ⋆), then this equilibrium is globally asymptotically stable. Interpretation.. Theorem 4 establishes a genuine global stability statement for the reduced DDCL flow: 

• all trajectories remain bounded; 

• the total reduced DDCL energy decreases monotonically; 

• every trajectory converges to the set of KKT stationary points. 22 This does not imply convergence to a unique equilibrium, because the energy may still possess several stationary configurations due to prototype permu-tation symmetry, clustering degeneracies, or nonconvexity of the regulariz-ers (Corollary 6 gives the condition under which uniqueness implies global asymptotic stability). 

5.7. Limits of the global result and the full DDCL system 

Theorem 4 is global, but only for the reduced system with frozen encoder features. In the full model, zn = fθ(xn), so the backbone parameters θ also evolve: 

˙θ = −ε∇θE(θ, P, Q ), ˙P = −∇ P E(θ, P, Q ), ˙qn = Π Tqn ∆(−∇ qn E(θ, P, Q )) .

(44) This system is harder to analyze because: it is nonconvex in θ; multiple sta-tionary points are generally present; temperature annealing makes the dy-namics non-autonomous; and the coupled backbone–prototype–assignment flow requires a two-timescale analysis. Global asymptotic stability of the full end-to-end DDCL system therefore remains an open problem. A plausible route toward a stronger result is to assume that the back-bone evolves on a slower timescale ( 0 < ε ≪ 1). If, for each fixed θ, the fast subsystem (P, Q ) admits a unique globally asymptotically stable equi-librium (P ⋆(θ), Q ⋆(θ)) , and if the reduced energy ¯E(θ) = E(θ, P ⋆(θ), Q ⋆(θ)) 

has a unique globally asymptotically stable minimizer, then it is in principle possible to prove that the full dynamics tracks the slow manifold (P, Q ) ≈

(P ⋆(θ), Q ⋆(θ)) . This would require a singular perturbation or two-timescale stochastic approximation framework and is identified as the primary open theoretical question for future work. 

6. Extensions 

6.1. CNNs beyond AlexNet 

DDCL requires only that the backbone provides zn = fθ(xn) ∈ Rd. Any CNN architecture (VGG [32], ResNet [33], DenseNet, EfficientNet, Con-vNeXt) can serve as backbone, with no modification to the convolutional layers. 23 Attachment points. The DCL module can be attached at three natural po-sitions. (i) After global average pooling : the pooled feature map zn ∈ Rd

is compact and translation-invariant; this is the most common choice and the one used in Block 4 (ResNet-18 on CIFAR-10). (ii) After the penul-timate fully connected layer : the representation is more task-adapted but higher-dimensional; useful when discriminative structure is concentrated in the final layers. (iii) After a dedicated projection head : a small MLP head (e.g., d → 256 → 128 ) can be inserted between backbone and DCL to re-duce dimensionality and decouple prototype geometry from backbone feature statistics, following the practice of SimCLR [11]. 

Multi-scale variants. DCL blocks can be attached at multiple depths simulta-neously, each receiving a different-resolution feature map and learning proto-types at a different granularity. This produces a hierarchical prototype struc-ture without any additional supervision: coarse-scale blocks capture global cluster identity while fine-scale blocks resolve within-cluster sub-structure. The gradient subspace property (Section 3.2) ensures that all DCL blocks remain well-conditioned regardless of the feature dimensionality at each at-tachment point. 

Practical note on batch size. Since prototypes pj = X⊤w2,j are linear com-binations of the current batch, the batch size n directly controls the expres-siveness of the prototype subspace (Theorem 3.4 of [2]: gradients live in the 

n-dimensional data subspace). For large CNN backbones, a batch size n ≥ 2k

is recommended to ensure that the data subspace is rich enough to represent all k prototype directions. 

6.2. Recurrent architectures 

For sequential data xn = ( xn1, . . . , x nT n ), a recurrent encoder (RNN, LSTM, GRU) produces a sequence summary zn ∈ Rd (final hidden state or pooled representation), and the DDCL pipeline applies directly. Incre-mental DDCL is especially natural here: DCL rows can be generated syn-chronously with hidden states, coupling temporal dynamics to prototype formation. Variants include sequence-level, time-step-level, and hierarchical clustering. 

6.3. Transformers 

Transformers provide class-token ( zn ∈ Rd), averaged-token, or struc-tured token embeddings. Three levels of DDCL are possible: sequence-level 

24 (one global embedding per input), token-level (treating each token as a sep-arate sample, so DDCL organizes the internal token cloud into prototype mixtures), and layer-wise (applying DCL at multiple transformer layers to enforce hierarchical prototype organization). The compatibility of DDCL with attention-based architectures and masked modeling pretraining [24] is an especially promising research direction. 

7. Numerical Validation of Theoretical Predictions 

The following experiments are not intended as a competitive benchmark. The goal of the experiments is controlled validation of the derived structural predictions rather than state-of-the-art benchmark performance. They are designed specifically to verify, in controlled settings, each structural predic-tion derived in Section 5. 

Evaluation metrics. All clustering results are reported in terms of three stan-dard unsupervised clustering metrics. Clustering Accuracy (ACC) is com-puted as: 

ACC = max 

> π∈Sk

1

N

> N

X

> n=1

1[yn = π(ˆ yn)] , (45) where yn is the ground-truth label, ˆyn is the predicted cluster index, and the maximum is taken over all permutations π of {1, . . . , k } (solved via the Hungarian algorithm). Normalized Mutual Information (NMI) measures the mutual information between predicted and true label assignments, normal-ized by their geometric mean entropy; NMI ∈ [0 , 1] with 1 denoting perfect agreement. Adjusted Rand Index (ARI) measures the agreement between two clusterings corrected for chance; ARI = 1 for perfect agreement and ARI 

≈ 0 for random assignments. 

Experimental setup. Blocks 1–3 use pure NumPy implementations (no GPU), making every result fully reproducible on a standard CPU. Block 4 uses PyTorch with a pretrained ResNet-18 backbone for feature extraction on CIFAR-10; the prototype dynamics remain a NumPy implementation on the extracted features. Block 5 uses PyTorch end-to-end with a trainable MLP backbone. Block 6 validates the incremental DDCL formulation on streaming data with mini-batch updates. All implementations use standard numerical libraries (NumPy, PyTorch) and are described in sufficient detail for full reproducibility. 25 7.1. Block 1: Verification of structural properties 

Setup: Three synthetic 2-D datasets (Moons, Circles, Spiral; n = 300 ,

k = 2 ) and one Blobs dataset ( n = 400 , k = 4 ) are used. These datasets are deliberately chosen for their geometric simplicity: because the data live in two dimensions, every aspect of the training dynamics (prototype trajecto-ries, assignment softness, and separation evolution) can be visualized directly, making it straightforward to verify whether the theoretical predictions hold or fail. No backbone is used; DDCL operates directly on the 2-D features, isolating the prototype dynamics from any feature-learning effects. The tem-perature is swept over T ∈ { 0.1, 0.5, 1.0} to cover the near-hard ( T → 0), intermediate, and near-uniform ( T → ∞ ) regimes, over 10 independent ran-dom initializations. 

Results: The block confirms four structural predictions simultaneously. 

Loss decomposition (Theorem 1): V ≥ 0 holds at every epoch with zero violations across all datasets and temperatures; V grows monotonically with 

T , confirming the Monotonicity Corollary. The zero-violation result is no-table: it holds without any enforcement mechanism, purely as a consequence of the algebraic identity Lq = LOLS + V and the non-negativity of the vari-ance (the identity E[∥p∥2] − ∥ E[p]∥2 ≥ 0 holds for any probability weights and any vectors). 

Collapse resistance (Propositions 5–7): Fig. 2 directly shows the key contrast between Lq and LOLS across temperatures. In the left panel, Lq

maintains a positive plateau of prototype separation S(P ) at all three tem-peratures, while LOLS at T = 1 .0 sees S(P ) decay to zero (both prototypes converge to the same point and all cluster structure is lost. In the right panel, the collapse rate of LOLS grows with T (reaching 100% at T = 1 .0), while Lq

shows zero collapses across all temperatures and all 10 runs, confirming that collapse is a stable attractor of LOLS and an unstable saddle of Lq.

Negative feedback cycle (Theorem 2): corr( S, K) = −0.67 (Moons, 200 epochs), confirming the negative feedback direction quantitatively; damped oscillations in I(Σ qn ) are consistent with the oscillatory-stable regime pre-dicted by Theorem 3 (the system spirals toward equilibrium rather than converging monotonically. 

Temperature continuum : an optimal T ∗ ∈ [0 .3, 0.5] exists between the 

k-means limit ( T → 0) and the uniform limit ( T → ∞ ); on Moons (10 runs) ACC = 0 .847 , NMI = 0 .382 , ARI = 0 .479 are stable across all three temperatures, while V grows from 0.010 to 0.206 without affecting accuracy 26 (confirming that V acts as a structural regularizer, not as a clustering objec-tive: it can vary widely without degrading clustering performance, because what matters for accuracy is the relative geometry of the prototypes, not the absolute value of V . Table 2 summarizes the main structural measurements across datasets and temperatures.        

> Table 2: Block 1 — Structural verification on synthetic datasets ( n∈ { 300 ,400 }, 10 runs).
> S: prototype separation at convergence; corr: Pearson r(S,K); viol.: V < 0violations.

Dataset T ACC ( Lq) S(Lq) S(LOLS ) r(S, K) viol. Moons 0.1 0.847 ± 0.000 > 0 (stable) > 0 (stable) −0.67 0Moons 0.5 0.847 ± 0.000 > 0 (stable) > 0 (stable) −0.67 0Moons 1.0 0.847 ± 0.000 > 0 (stable) → 0 (collapsed) −0.67 0Circles 0.5 > 0.85 (stable) > 0 (stable) → 0 (collapsed) < 0 0Blobs 0.5 > 0.95 (stable) > 0 (stable) > 0 (stable) < 0 0                     

> Figure 2: Block 1 — Prototype collapse on Moons ( n= 300 ,k= 2 ). Left: prototype sep-aration S(P)at convergence vs. temperature T:Lq(solid) remains stable; LOLS (dashed) diverges at high T.Right: collapse rate (% of runs): zero for Lq, rising for LOLS .

7.2. Block 2: Low-dimensional benchmark 

Setup: The sklearn load_digits dataset is used: n = 1797 images, d =64 features ( 8 × 8 pixels), k = 10 classes [31]. Input features are preprocessed with PCA to 20 components (reducing d = 64 to 20 principal dimensions) followed by standardization (the same preprocessing step used internally by DeepCluster’s k-means stage (ensuring a fair comparison. DDCL uses a direct DCL prototype module on these features with temperature annealing 27 T (t) = T0 exp( −t/τ ), T0 = 2 , Tmin = 0 .5, τ = 80 . DeepCluster uses a faithful reimplementation: 3-layer MLP backbone with BatchNorm, PCA whitening + ℓ2-normalization of extracted features, k-means with ninit = 20 and empty-cluster reassignment, uniform cluster sampling for CE training. All results are mean ±std over 5 independent runs. 

Exp 2.1 — Comparison: Table 3 reports results. All methods reach ACC ≈ 0.60 –0.64 , consistent with literature on this benchmark [3, 25]. The key observations are: (i) DDCL( Lq) and DDCL( LOLS ) achieve comparable accuracy on PCA features, consistent with the theoretical prediction that backbone gradients are identical under stop-gradient and the difference be-tween the two losses manifests primarily in collapse-resistance properties rather than in final accuracy when features are fixed; (ii) DeepCluster ex-hibits significantly higher variance ( ±0.091 vs. ±0.053 for DDCL), reflecting the instability of its alternating k-means/pseudo-label cycle (a structural limitation that DDCL eliminates by design). 

> Table 3: Block 2 — MNIST Digits (5 runs, mean ±std).

Method ACC NMI ARI DDCL ( Lq) .602 ± .053 .602 ± .046 .457 ± .051 

DDCL ( LOLS ) .604 ± .055 .596 ± .047 .455 ± .052 

DeepCluster .616 ± .091 .540 ± .085 .426 ± .091 

k-means .590 ± .002 .627 ± .003 .468 ± .003 

k-means+PCA .639 ± .041 .647 ± .038 .502 ± .046 

Exp 2.2 — Ablation. Temperature annealing ( T0 = 2 → Tmin = 0 .5)consistently outperforms fixed T (ACC = 0 .576 vs. 0.563 for fixed T = 0 .5), confirming the practical value of the temperature schedule derived from the entropic regularization analysis (Proposition 1). Fixed T = 0 .1 and T = 2 .0

both underperform, corresponding to the degenerate k-means and uniform limits respectively. The Lq + Lsep variant does not improve over Lq alone (ACC = 0 .549 ), suggesting that the implicit separation force ∇P V is already sufficient on this dataset. Table 4 summarizes the ablation results. 

Exp 2.3 — Dynamics and prototype visualization. The feedback cycle is sharply confirmed: corr( S, K) = −0.98 across training. The phase portrait traces a smooth trajectory from the initial high-K / low-S regime toward the equilibrium predicted by Theorem 2. The variance term satisfies 28 Table 4: Block 2 — Ablation on MNIST Digits (5 runs, mean ±std). 

Configuration ACC NMI ARI 

Lq + annealing ( T0=2 , Tmin =0 .5) .576 ± .048 .578 ± .041 .434 ± .047 

Lq + fixed T = 0 .5 .563 ± .051 .561 ± .044 .421 ± .049 

Lq + fixed T = 0 .1 .544 ± .039 .543 ± .035 .407 ± .038 

Lq + fixed T = 2 .0 .531 ± .058 .527 ± .053 .388 ± .061 

Lq + Lsep + annealing .549 ± .052 .551 ± .046 .412 ± .051 

V ≥ 0 in every epoch (zero violations). Fig. 3 shows the full training dy-namics (top half) and the learned prototypes (bottom half). In the top half, the four panels show: (left to right) the loss decomposition Lq = LOLS + V

over training epochs; the feedback scatter between S(P ) and K(qn) (Pearson 

r = −0.98 ); the ACC curves; and the phase portrait tracing the trajec-tory (S(t), K(t)) converging toward equilibrium. In the bottom half, the 10 learned DDCL prototypes are shown as 8 × 8 images with Hungarian-matched digit labels: each prototype converges to a visually distinct digit, confirming semantic coherence without any supervised signal. The dynamics measurements are collected in Table 5.      

> Table 5: Block 2 — Dynamics summary (MNIST Digits, 5 runs). †:V≡0by definition; feedback not defined.

Method corr( S, K) S∗ Vmin Vmax viol. DDCL ( Lq) −0.98 stable 0.000 0.206 0DDCL ( LOLS ) † stable 0† 0† 0†

7.3. Block 3: High-dimensional validation 

Setup: A MADELON-style dataset ( n = 100 , k = 2 ) is used across six di-mensionalities d ∈ { 10 , 50 , 200 , 500 , 1000 , 5000 } with five methods: DDCL( Lq), DDCL( LOLS ), DeepCluster, k-means+PCA, k-means (raw). Fig. 4 shows ACC and NMI on a log axis; Table 6 reports selected d.

Results: At d = 10 all methods achieve ACC ≈ 1.0. The first divergence appears at d = 50 : DeepCluster and k-means (raw) drop to ACC ≈ 0.70 as noise begins to overwhelm the informative directions. At d = 200 (d > n ,transition) a three-way split emerges: k-means+PCA leads (ACC = 0 .884 ±

29 Figure 3: Block 2 — DDCL dynamics on MNIST Digits (run 1). Top: loss decomposition, feedback scatter ( r = −0.98 ), ACC curves, phase portrait (S(t), K(t)) . Bottom: 10 learned prototypes with Hungarian-matched digit labels. 

30 0.104 ) via explicit noise suppression; DDCL( Lq) degrades gracefully (ACC 

= 0 .807 ± 0.124 ), confirming the DCL gradient subspace property; LOLS , k-means (raw), and DeepCluster all collapse toward chance (ACC ≈ 0.5). At 

d = 5000 all methods converge to chance level. Three structural findings hold across all d: (i) DDCL( Lq) ≥ DDCL( LOLS ) at every dimensionality; (ii) the performance transition is anchored at d/n = 1 , as predicted by DCL theory; (iii) DeepCluster and k-means (raw) are the most fragile methods at every 

d, while k-means+PCA is strongest in the transition regime but collapses at 

d ≫ n.      

> Table 6: Block 3 — MADELON ( n= 100 ,k= 2 ). Selected d.Mean ±std over 5 runs. Best ACC in bold.

d Method ACC NMI ARI 

d = 10 

(d < n )DDCL ( Lq) 0.994 ± 0.014 0.965 ± 0.046 0.975 ± 0.059 

DDCL ( LOLS ) 0.989 ± 0.024 0.951 ± 0.069 0.961 ± 0.093 

DeepCluster 0.707 ± 0.198 0.208 ± 0.289 0.199 ± 0.295 

k-means+PCA 0.987 ± 0.026 0.941 ± 0.073 0.949 ± 0.099 

k-means (raw) 0.700 ± 0.210 0.195 ± 0.280 0.185 ± 0.271 

d = 200 

(d > n ,transition) DDCL ( Lq) 0.807 ± 0.124 0.423 ± 0.265 0.430 ± 0.293 

DDCL ( LOLS ) 0.546 ± 0.088 0.026 ± 0.042 0.019 ± 0.032 

DeepCluster 0.503 ± 0.008 0.001 ± 0.003 0.001 ± 0.002 

k-means+PCA 0.884 ± 0.104 0.552 ± 0.290 0.594 ± 0.330 

k-means (raw) 0.511 ± 0.015 0.002 ± 0.004 0.001 ± 0.003 

d = 5000 

(d ≫ n,extreme) DDCL ( Lq) 0.511 ± 0.022 0.001 ± 0.003 0.001 ± 0.003 

DDCL ( LOLS ) 0.504 ± 0.009 0.000 ± 0.000 0.000 ± 0.000 

DeepCluster 0.501 ± 0.005 0.000 ± 0.000 0.000 ± 0.000 

k-means+PCA 0.572 ± 0.110 0.037 ± 0.069 0.028 ± 0.060 

k-means (raw) 0.502 ± 0.006 0.000 ± 0.000 0.000 ± 0.000 

7.4. Block 4: Structural verification at scale (CIFAR-10) 

Setup: Block 4 uses CIFAR-10 ( N = 50 ,000 , k = 10 ) with frozen ResNet-18 ( d = 512 , ℓ2-normalised) to verify that structural predictions hold at large scale and in high-dimensional feature space, without backbone adaptation. All results: mean ±std over 5 runs. 

Results: Table 7 and Fig. 5 confirm three predictions. (1) Stop-gradient equivalence (Proposition 4): DDCL( Lq) and DDCL( LOLS ) produce identical 31 Figure 4: Block 3 — ACC (left) and NMI (right) vs. d on log axis ( n = 100 , k = 2 , 5 runs). Dotted line: d = n.

metrics to three decimal places across all runs (the sharpest confirmation obtained across all blocks. (2) Loss decomposition : V ≥ 0 at every epoch with zero violations; V grows monotonically as temperature decreases, con-firming the Monotonicity Corollary. (3) Feedback cycle : Pearson r = +0 .98 

between S and K; the positive sign (vs. r = −0.98 in Block 2) reflects the frozen-encoder regime where backbone modulation is absent and both quan-tities grow monotonically together, consistent with Theorem 4. k-means dominates in absolute ACC ( 0.644 ) because frozen ImageNet features are already discriminative; DDCL’s advantage over k-means requires end-to-end backbone adaptation (Block 5).     

> Table 7: Block 4 — CIFAR-10 (ResNet-18 frozen, N= 50 ,000 ,k= 10 , 5 runs).

Method ACC NMI ARI DDCL ( Lq) 0.265 ± 0.031 0.208 ± 0.033 0.085 ± 0.028 

DDCL ( LOLS ) 0.265 ± 0.031 0.208 ± 0.033 0.085 ± 0.028 

k-means 0.644 ± 0.000 0.536 ± 0.000 0.425 ± 0.000 

DeepCluster 0.154 ± 0.025 0.053 ± 0.024 0.013 ± 0.011 

32 Figure 5: Block 4 — DDCL dynamics on CIFAR-10 (ResNet-18 frozen, run 1). Top: loss decomposition and feedback scatter (S, K). Bottom: accuracy curves and phase portrait. 

33 7.5. Block 5: End-to-end DDCL with trainable backbone 

Setup: Block 5 tests whether the Lq advantage over LOLS persists when the backbone is jointly optimised end-to-end. MNIST Digits ( n = 1 ,797 ,

d = 64 , k = 10 ) is used with a three-layer MLP backbone ( 64 → 256 →

128 → 32 , BatchNorm+ReLU). Training: (1) 50-epoch SimCLR+AE warm-up on Gaussian-augmented views ( λAE = 0 .5); (2) 300-epoch end-to-end joint optimisation with separation weight η growing from 0 to 0.05 over the first 100 epochs. Comparisons: DDCL( Lq) vs. DDCL( LOLS ) vs. k-means+PCA (fixed-feature baseline) vs. DeepCluster e2e (same backbone). All results: mean ±std over 3 runs. 

Results: Table 8 and Fig. 6 show the key finding: with trainable back-bone, Lq achieves ACC 0.378 ± 0.085 vs. 0.229 ± 0.041 for LOLS (+65% rel-ative) (the largest Lq–LOLS gap across all six blocks. This contrasts sharply with Blocks 2 and 4, where frozen features yield identical results for both losses (Proposition 4). The divergence confirms the theoretical prediction: 

∇P V becomes the decisive factor precisely when backbone and prototypes co-adapt. DDCL( Lq) also substantially outperforms DeepCluster e2e (ACC 

0.378 vs. 0.170 , +122% ), confirming that continuous gradient flow is su-perior to the alternating pseudo-label cycle under identical backbone and budget. k-means+PCA retains the highest absolute ACC ( 0.595 ) (expected on a small dataset where classical feature engineering dominates; the relevant comparison for DDCL remains the Lq–LOLS gap under identical end-to-end conditions. 

> Table 8: Block 5 — MNIST Digits, end-to-end DDCL, MLP backbone (3 runs).

Method ACC NMI ARI DDCL ( Lq, e2e) 0.378 ± 0.085 0.448 ± 0.125 0.242 ± 0.090 

DDCL ( LOLS , e2e) 0.229 ± 0.041 0.320 ± 0.064 0.122 ± 0.017 

k-means+PCA (fixed) 0.595 ± 0.011 0.617 ± 0.007 0.465 ± 0.006 

DeepCluster (e2e) 0.170 ± 0.042 0.124 ± 0.076 0.027 ± 0.024 

7.6. Block 6: Incremental DDCL (streaming validation) 

Setup: Block 6 validates the incremental DDCL formulation (Section 4.5) on a streaming version of MNIST Digits. Data arrive in mini-batches of 

B = 50 samples in a single pass; no sample is revisited. Prototypes are 34 Figure 6: Block 5 — MNIST Digits end-to-end DDCL (MLP backbone, run 1). Left: ACC curves during Phase 2. Right: final ACC bar chart (3 runs). Table 9: Block 6 — Incremental DDCL, MNIST Digits ( n = 1797 , d = 20 , k = 10 , B = 50 ,5 runs). Batch DDCL is the Block 2 ceiling (full-data training). 

Method ACC NMI ARI DDCL (incr., Lq) 0.535 ± 0.063 0.520 ± 0.049 0.346 ± 0.046 

MiniBatchKMeans 0.587 ± 0.077 0.608 ± 0.043 0.455 ± 0.060 

DDCL (batch, Block 2) 0.602 ± 0.053 0.617 ± 0.034 0.465 ± 0.046 

updated online via a gradient step on each arriving batch, with temperature 

T (t) = max( T0e−t/τ , T min ), T0 = 2 , τ = 30 , Tmin = 0 .3. Comparisons: in-cremental DDCL( Lq) vs. MiniBatchKMeans (sklearn, same batch size) vs. batch DDCL (Block 2 reference ceiling, ACC = 0 .602 ± 0.053 ). All results: mean ±std over 5 runs. Fig. 7 shows ACC and prototype separation S(P ) as functions of samples seen. 

Results: Table 9 reports final metrics after a single streaming pass. In-cremental DDCL achieves ACC = 0 .535 ± 0.063 , within the error margin of MiniBatchKMeans ( 0.587 ± 0.077 ); the difference is not statistically sig-nificant given the overlap of standard deviations. Both streaming methods fall below the batch DDCL ceiling ( 0.602 ), as expected from the single-pass constraint. Fig. 7 (left) shows that incremental DDCL converges smoothly as samples accumulate, without the oscillations characteristic of hard-assignment streaming methods. Fig. 7 (right) shows that prototype separation S(P )

grows monotonically and stabilises, confirming that the implicit separation force ∇P V remains active in the incremental regime. 35 Figure 7: Block 6 — Incremental DDCL on MNIST Digits (run 3, seed=56). Left: ACC vs. samples seen. Right: prototype separation S(P ).

8. Discussion 

8.1. Comparison with DeepCluster 

Table 10 summarizes the structural differences between DeepCluster and DDCL. Six experimental blocks add quantitative confirmation: (i) variance reduced 1.7× (Block 2); (ii) at d/n = 2 , Lq ACC = 0 .807 vs. DeepCluster 

0.503 (Block 3), with the gap growing monotonically in d; (iii) corr( S, K) = 

−0.98 and −0.67 confirm the negative feedback loop that DeepCluster lacks (Blocks 1–2); (iv) Lq never collapses across all settings; LOLS collapses at high T (Block 1); (v) on CIFAR-10 frozen features (Block 4), DeepCluster achieves ACC = 0 .154 , worst across all methods; (vi) with trainable back-bone (Block 5), DDCL( Lq) outperforms DeepCluster e2e by +122% (ACC 

0.378 vs. 0.170 ) and LOLS by +65% (the largest Lq–LOLS gap observed, con-firming that ∇P V matters most when the backbone co-adapts; (vii) in the incremental streaming regime (Block 6), the implicit separation force ∇P V

remains active in a single-pass setting, with prototype separation growing monotonically across the data stream. 

8.2. Theoretical predictions vs. experimental outcomes 

All five major structural predictions are confirmed across all six blocks. (1) V ≥ 0 always: zero violations in > 10 5 epochs (Blocks 1–3), confirmed at scale in Block 4 ( N = 50 ,000 , d = 512 ). (2) V monotone in T : V

grows from 0.010 to 0.206 while ACC remains unchanged (Block 1 temper-ature sweep, Block 2 ablation); confirmed in Block 4 where V grows mono-tonically as temperature decreases. (3) Collapse resistance: Lq maintains 

S(P ) > 0 at all temperatures while LOLS collapses at high T (Blocks 1 36 Table 10: DeepCluster vs. DDCL: structural and empirical comparison (structural posi-tioning across all methods in Table 1). 

Aspect DeepCluster DDCL Clustering External k-means Internal DCL Assignments Hard, discrete Soft ( T -scaled) Backprop Pseudo-labels only End-to-end Collapse Empty-cluster fix Implicit ∇P V

Feedback None Negative, self-reg. Formulation Two-stage alt. Single unified loss Variance High ( ±0.091 ) Low ( ±0.053 )High-d Degrades at d > n Stable ( d ≤ n subspace) Stability Not analyzed Global (frozen enc.) and 3). (4) Negative feedback cycle: corr (S, K) = −0.67 (Block 1), −0.98 

(Block 2), +0 .98 (Block 4). The sign reversal in Block 4 is theoretically co-herent: the frozen-encoder regime suppresses the backbone modulation that drives self-regulating oscillations, so S and K grow monotonically rather than forming a negative loop. (5) T → 0 gives k-means, T → ∞ gives uniform: confirmed by the temperature ablation in Block 2. The Lq advantage over LOLS depends critically on whether the backbone is frozen or trainable: on fixed features (Blocks 2 and 4) both losses are statistically indistinguishable (Proposition 4), while with trainable backbone (Block 5) Lq outperforms LOLS by +65% in ACC, confirming that ∇P V

propagates into the backbone gradient only through full end-to-end coupling. 

8.3. Initialization sensitivity 

Both frameworks are bootstrapping methods that must self-organize from random initialization, but their failure modes differ qualitatively. DeepClus-ter’s hard assignment pipeline creates a brittle feedback loop: poor initial features produce unreliable pseudo-labels, which reinforce the deficiency; the high variance ±0.091 (Block 2) and near-zero NMI in Block 5 are direct em-pirical signatures. DDCL shifts the problem from a hard bootstrap loop to a coupled dynamical stability problem (nearly uniform assignments, prema-ture sharpening, or excessive backbone speed (all addressed by the rate-ratio condition of Corollary 5 and the temperature schedule of Section 5.5. In 37 summary: DDCL turns a brittle hard bootstrap into a softer and more con-trollable one. 

8.4. Practical training guidelines 

The stability conditions and experimental evidence motivate a four-phase training strategy. A complete pseudocode summary of the batch training loop is given in Appendix G. 

Phase 1 — Soft warm-up: Start with high temperature T ≈ 1–2 and learning-rate ratio αDCL /α bb ≈ 3–10 (satisfying Corollary 5). Optionally apply stop-gradient on assignments. Balancing ( Lbal ) and entropy ( Lent )regularization prevent trivial imbalance. 

Phase 2 — Stabilization: Reduce the learning-rate asymmetry grad-ually. Monitor prototype separation S(P ) and cluster usage. Damped oscil-lations in K(qn) and S(P ) are expected and are not a failure mode. 

Phase 3 — Sharpening: Begin temperature annealing T (t) = T0 exp( −t/τ ).Remove stop-gradient once S has stabilized. The phase portrait of (S, K)

(Fig. 3) provides a direct visual diagnostic. 

Phase 4 — Structured refinement: Reduce αDCL to avoid overshoot-ing equilibrium. Target T ∈ [0 .1, 0.3] and K(qn) → 1.

8.5. Open problems 

Several theoretical questions remain open: (i) global asymptotic stability of the joint backbone–DCL dynamics under full (non-stop-gradient) training, including the two-timescale slow–fast extension sketched in Section 5.7; (ii) identifiability of prototype configurations under Lq; (iii) exact relationship between Lq and maximum-likelihood estimation under a Gaussian mixture model; (iv) large-scale end-to-end GPU validation on CIFAR-10 and STL-10 with a jointly trained backbone (Block 4 addressed the frozen-encoder regime; end-to-end adaptation with AlexNet or ResNet-18 at full scale remains future work). 

9. Conclusion 

This paper has introduced DDCL, the first fully differentiable end-to-end framework for unsupervised prototype-based deep clustering. The cen-tral architectural contribution is the replacement of the external k-means of DeepCluster [1] with an internal DCL module [2] whose key insight is to 38 feed the transposed feature matrix to the competitive layer, so that proto-types become native network outputs and the complete backbone–prototype– assignment pipeline is trainable by backpropagation through a single unified loss. This resolves, by design, the structural disconnect that affects all two-stage deep clustering methods. The theoretical analysis that grounds this framework centres on the alge-braic decomposition Lq = LOLS + V . At first glance this is a simple identity, but it has far-reaching consequences. It reveals that the soft quantization loss carries an implicit geometric structure absent from the reconstruction objective alone: the variance term V grows with prototype dispersion and vanishes only when assignments are hard. Its gradient ∇P V = 2 P Σqn acts as a continuous separation force, preventing prototype collapse without any explicit penalty (a form of self-regulation that is structurally guaranteed by the loss geometry rather than engineered as an add-on. The same decompo-sition explains why Lq and LOLS appear equivalent under stop-gradient (the backbone gradient is identical for both) yet diverge dramatically when the backbone co-adapts with the prototypes: it is precisely through the extra term ∇zn V that Lq injects richer learning signal into the backbone. Beyond the local analysis of the negative feedback loop between prototype separation and assignment concentration, Theorem 4 establishes a genuine global result: all trajectories of the reduced frozen-encoder DDCL flow re-main bounded, the regularized energy decreases monotonically along every trajectory, and convergence to the KKT stationary set is guaranteed. This provides a solid theoretical floor for the system (while honestly acknowledging that the full end-to-end stability, where the backbone itself evolves, remains an open problem requiring a two-timescale framework. The experimental program is designed to make this theory visible. Rather than optimizing for benchmark leaderboard performance, the six blocks of experiments each target a specific theoretical prediction: the V ≥ 0 identity, the monotonicity of V in temperature, the collapse resistance of Lq versus 

LOLS , the negative feedback cycle, the stop-gradient equivalence at scale, and the survival of the implicit separation force in the incremental streaming regime. In each case the prediction is confirmed. The most striking quanti-tative finding is in Block 5: with a jointly trained backbone, Lq outperforms 

LOLS by +65% in clustering accuracy and DeepCluster by +122% (precisely the conditions under which the theory predicts the largest effect of ∇P V .The primary direction for future work is large-scale end-to-end GPU vali-dation with jointly trained backbones on standard visual benchmarks, which 39 will test whether the structural advantages demonstrated here at controlled scale translate to the CIFAR-10 and STL-10 settings where contrastive and self-supervised methods currently set the state of the art. 

Appendix A. Proof of Theorem 1 (Loss Decomposition) 

Proof. Expand Lq:

Lq = X

> j

qnj 

 ∥zn∥2 − 2z⊤ 

> n

pj + ∥pj ∥2 = ∥zn∥2 − 2z⊤ 

> n

¯pn + X

> j

qnj ∥pj ∥2.

Expand LOLS :

LOLS = ∥zn∥2 − 2z⊤ 

> n

¯pn + ∥¯pn∥2.

Hence Lq − LOLS = P 

> j

qnj ∥pj ∥2 − ∥ ¯pn∥2. Expanding V = P 

> j

qnj ∥pj − ¯pn∥2:

V = X

> j

qnj 

 ∥pj ∥2 − 2p⊤ 

> j

¯pn + ∥¯pn∥2 = X

> j

qnj ∥pj ∥2 − ∥ ¯pn∥2.

Hence Lq −LOLS = V . Non-negativity of V follows directly from the convexity of the squared norm: since ∥ · ∥ 2 is convex and qn ∈ ∆k−1 (i.e., qnj ≥ 0,P 

> j

qnj = 1 ), the variance formula gives: 

V = X

> j

qnj ∥pj ∥2 − X

> j

qnj pj

> 2

≥ 0,

which is the standard identity E[∥p∥2]−∥ E[p]∥2 ≥ 0 (variance is non-negative), valid for any probability distribution qn and any vectors pj ∈ Rd. Equality holds iff qn is a vertex of ∆k−1 (hard assignment, so ¯pn = pj∗ and variance vanishes) or all active prototypes coincide ( pi = pj for all i, j with qnj > 0). 

□

Proof of Corollary 1 (Upper Bound).. Since qn ∈ ∆k−1, it follows that qnj ∈

[0 , 1] for all j. Therefore: 

V = X

> j

qnj ∥pj − ¯pn∥2 ≤ max  

> j

∥pj − ¯pn∥2 ≤ max  

> i̸=j

∥pi − pj ∥2 = diam( P )2.

The tighter bound V ≤ 14 diam( P )2 follows from the fact that ¯pn is a convex combination of the pj : for any j, ∥pj − ¯pn∥ ≤ 12 diam( P ), and hence ∥pj −

¯pn∥2 ≤ 14 diam( P )2, where diam( P ) = max i̸=j ∥pi − pj ∥. □

40 Appendix B. Proof of Proposition 1 

Proof. The Lagrangian for min qn≥0{d⊤ 

> n

qn − T H (qn)} subject to 1⊤qn = 1 

is: 

L = d⊤ 

> n

qn + T X

> j

qnj log qnj − λ(1⊤qn − 1) .

Stationarity: dnj + T (log qnj + 1) = λ, so qnj = exp(( λ − 1) /T ) exp( −dnj /T ).Normalization gives the softmax (8). As T → 0, mass concentrates on j∗ =arg min j dnj . As T → ∞ , qnj → 1/k for all j. □

Proof of Corollary 2 (Monotonicity of V ).. From the proof above, tr(Σ qn(T )) = 1 − ∥ qn(T )∥2 is non-decreasing in T . Since V (P, q n) = tr( P ⊤P Σqn ) (an equivalent representation following from expanding V ), and P ⊤P ⪰ 0, V is monotone non-decreasing in T by the monotone trace inequality. At T → 0:

Σqn → 0, hence V → 0. At T → ∞ : qn → 1 

> k

1k, Σqn → 1 

> k

Ik − 1 

> k2

11 ⊤, and 

V → 1

> k

P 

> j

∥pj ∥2 − ∥ 1

> k

P 

> j

pj ∥2 = Var( P ). □

Appendix C. Proof of Propositions 2–4 

Proof of Proposition 2. For LOLS = ∥zn − P q n∥2: ∇qn LOLS = 2 P ⊤(P q n −

zn) = 2 P ⊤(¯ pn − zn). For Lq = d⊤ 

> n

qn, since dn is independent of qn: ∇qn Lq =

dn. For V = P 

> j

qnj ∥pj ∥2 − ∥ P q n∥2: ∇qn V = ( ∥pj ∥2)j − 2P ⊤ ¯pn. □

Proof of Proposition 3. ∇P LOLS = 2( P q n − zn)q⊤ 

> n

= 2(¯ pn − zn)q⊤ 

> n

. Dif-ferentiating Lq = P 

> j

qnj ∥zn − pj ∥2: ∇P Lq = 2( P − zn1⊤ 

> k

)diag( qn). For V :

∇P V = 2 P diag( qn) − 2P q nq⊤ 

> n

= 2 P Σqn . □

Proof of Proposition 4. With stop-gradient on qn: ∇zn LOLS |sg = 2( zn −

¯pn); ∇zn Lq|sg = P 

> j

qnj · 2( zn − pj ) = 2( zn − ¯pn). Both are identical. With full gradient, ∂q nj /∂z n contributes a term ∝ T −1Σqn . □

Proof of Corollary 3 (Stop-Gradient).. The result is an immediate conse-quence of Proposition 4: under stop-gradient, ∇zn Lq = ∇zn LOLS , so the backbone parameter update ∆θ ∝ −∇ θL = −(∇zn L) · ∇ θfθ is identical for both losses. The DCL gradients ∇P Lq̸ = ∇P LOLS differ (Proposition 3), so the prototype dynamics remain distinct. Without stop-gradient, the back-bone gradient acquires the extra term ∇zn V · ∇ θfθ ∝ T −1Σqn · ∇ θfθ, which couples backbone and assignment dynamics and constitutes the mechanism by which Lq outperforms LOLS in the end-to-end regime. □

41 Appendix D. Proof of Propositions 5–7 

Proof of Proposition 5. If pi = pj = p∗, then ∂L OLS /∂p i = 2 qni (¯ pn − zn)

and ∂L OLS /∂p j = 2 qnj (¯ pn − zn) point in the same direction. No differential force exists to break symmetry. □

Proof of Proposition 6. With pi = p∗ + εu , pj = p∗ − εu : ∂Lq/∂p i =2qni (p∗ + εu − zn); ∂Lq/∂p j = 2 qnj (p∗ − εu − zn). The separating force along 

u is Fsep = 2 ε(qni + qnj ) > 0, absent under LOLS at first order. □

Proof of Proposition 7. tr(Σ qn ) = 1 − ∥ qn(T )∥2 is non-decreasing in T

(more uniform ⇒ larger variance), hence ∥Σqn ∥F and ∥P Σqn ∥F are non-decreasing. □

Proof of Corollary 4 (Role of Lsep ).. From Proposition 7, ∥∇ P V ∥F = 2 ∥P Σqn ∥F

is monotone non-decreasing in T and vanishes as T → 0. In the hard-assignment regime ( T → 0), qn → ej∗ , Σqn → 0, so ∇P V → 0 and the implicit separation force becomes negligible. The explicit term Lsep = − P 

> i<j

∥pi −

pj ∥2 then acts as a necessary backup, maintaining separation in the absence of entropic regularization. As T grows, Σqn increases and the implicit force dominates; Lsep becomes redundant, consistent with the experimental finding (Exp 2.2) that Lq + Lsep does not outperform Lq alone at moderate T . The suggested scaling η ∝ (T · tr(Σ qn (T ))) −1 ensures that the two forces remain balanced across the temperature schedule. □

Appendix E. Proof of Theorems 2 and 3 

Proof of Theorem 2. When S → 0: prototype gaps vanish, K → 1/k , I → 

(k − 1) /k (maximum), so ∥∇ P V ∥F is maximal (the system is pushed away from collapse. When S → ∞ : K → 1, I → 0, ∥∇ P V ∥F → 0; meanwhile attraction toward zn remains. By continuity, an equilibrium exists. □

Proof of Theorem 3. The linearized system (39) has characteristic poly-nomial λ2 − bλ + ac = 0 , with Re( λ1,2) = b/ 2. Stable iff b < 0; oscillatory stable iff additionally b2 < 4ac . □

Proof of Corollary 5 (Practical Stability Condition).. In the linearized sys-tem, b = αbb − αDCL . Stability requires b < 0, i.e., αbb < α DCL . The full condition including the oscillatory regime gives b < 2√ac , and eval-uating the linearization coefficients a, c from the DDCL dynamics yields the inequality in Corollary 5. The derivation uses a ∝ ∥∇ P V ∥F /S and 

c ∝ αDCL · T −1∥∇ zn fθ∥F ; see the linearization in Section 5.5. □

42 Proof of Corollary 6 (Uniqueness implies GAS).. If E(P, Q ) has a unique KKT stationary point (P ⋆, Q ⋆), then the invariant set {(P, Q ) : ddt E = 0 }

consists of the single point (P ⋆, Q ⋆). By Theorem 4, every trajectory con-verges to this set. Since E is non-increasing and bounded below, and the limit set is unique, every trajectory converges to (P ⋆, Q ⋆), which is therefore globally asymptotically stable. □

Appendix F. Proof of Theorem 4 (Global Lyapunov Stability) 

Proof. We show that E(P, Q ) defined in (41) is a Lyapunov function for the projected gradient flow (42)–(43). 

Step 1: ˙E ≤ 0:. Along the flow: 

ddt E(P, Q ) = ⟨∇ P E, ˙P ⟩ +

> N

X

> n=1

⟨∇ qn E, ˙qn⟩. (F.1) Since ˙P = −∇ P E, the first term equals −∥∇ P E∥2 

> F

≤ 0. For the assignment variables, ˙qn = Π Tqn ∆(−∇ qn E). A standard property of Euclidean projection onto a closed convex cone gives: 

⟨∇ qn E, ˙qn⟩ = − ΠTqn ∆(∇qn E) 2 ≤ 0. (F.2) Therefore: 

ddt E(P, Q ) = −∥∇ P E∥2 

> F

−

> N

X

> n=1

ΠTqn ∆(∇qn E) 2 ≤ 0. (F.3) Hence E is non-increasing along every trajectory. 

Step 2: Boundedness. E is bounded below: the quantization term is non-negative, Lbal and Lent are non-negative (KL divergence and entropy terms), and although Lsep = − P 

> i<j

∥pi − pj ∥2 is not bounded below on its own, the quadratic term (λ/ 2) ∥P ∥2 

> F

dominates for large ∥P ∥F :

ηL sep (P ) + λ 

> 2

∥P ∥2 

> F

≥ − η

k

2



∥P ∥2 

> F

+ λ 

> 2

∥P ∥2 

> F

≥ − C0∥P ∥2

> F

for a constant C0 < λ/ 2 (choosing η < λ/ (2  k

> 2

)), so the combined term is bounded below by a negative quadratic which is dominated by (λ/ 2) ∥P ∥2

> F

43 after rearrangement. More precisely, for λ > 2η k

> 2

, the energy satisfies 

E(P, Q ) ≥ c1∥P ∥2 

> F

− c2 for positive constants c1, c 2, and hence every sublevel set {E ≤ M } is bounded in P .Since E is non-increasing and bounded below, E(t) converges as t → ∞ 

and the trajectory stays in the sublevel set {E ≤ E(P (0) , Q (0)) }, which is bounded in P . Each qn belongs to the compact simplex ∆k−1, so the full trajectory (P (t), Q (t)) remains bounded. 

Step 3: Convergence to stationary set. LaSalle’s invariance principle for pro-jected dynamical systems implies that the trajectory converges to the largest invariant set contained in {(P, Q ) : ddt E(P, Q ) = 0 }. From (F.3), this set is characterized by: 

∇P E(P, Q ) = 0 and ΠTqn ∆(∇qn E(P, Q )) = 0 , n = 1 , . . . , N. 

These are exactly the KKT stationarity conditions for the simplex-constrained reduced DDCL problem. Therefore every trajectory converges to the station-ary set. □

Remark 3 (Condition on λ and η). The coercivity argument in Step 2 re-quires λ > 2η k

> 2

. In practice, since η is typically small (the explicit separa-tion term is a backup mechanism, cf. Corollary 4) and λ can be chosen inde-pendently, this condition is easily satisfied. A practical guideline is λ ≥ 10 η

with η ≪ 1.

Appendix G. DDCL Training Algorithm (Batch Formulation) 

The following algorithm summarises the complete batch DDCL training loop in simplified pseudocode. The three quantities S(P ), K(qn), and V serve as runtime diagnostics: monitoring them during training provides a direct window onto the theoretical predictions of Sections 5 and 5.5. 44 Algorithm 1 Batch DDCL — simplified training loop 

Require: Dataset {xn}Nn=1 , number of prototypes k, initial temperature T0,minimum temperature Tmin , annealing constant τ , loss weights β, γ, η, λ 

Ensure: Trained backbone fθ, prototype matrix P 

> 1:

Initialise backbone fθ (random or pretrained), DCL weights W2 (ran-dom), temperature T ← T0 

> 2:

for each epoch t = 1 , 2, . . . do  

> 3:

Forward pass  

> 4:

zn ← fθ(xn) // extract features  

> 5:

P ← F ⊤ 

> θ

W2 // DCL: prototypes as network outputs  

> 6:

qnj ← exp( −∥ zn − pj ∥2/T )

P 

> ℓ

exp( −∥ zn − pℓ∥2/T ) // soft assignments  

> 7:

Compute loss  

> 8:

Lq ← 1

N

P

> n

P 

> j

qnj ∥zn − pj ∥2 

> 9:

Ltot ← L q + βL bal + γL ent + ηL sep + λ 

> 2

∥P ∥2 

> F
> 10:

Backward pass // gradients flow through P , W2, and θ 

> 11:

θ, W 2 ← θ − αbb ∇θLtot , W 2 − αDCL ∇W2 Ltot  

> 12:

Diagnostics (optional, no cost)  

> 13:

V ← P 

> j

qnj ∥pj − P q n∥2 // verify V ≥ 0 

> 14:

S(P ) ← 1

(k

> 2

)

P 

> i<j

∥pi − pj ∥2 // prototype separation  

> 15:

K(qn) ← ∥ qn∥2 // assignment concentration  

> 16:

Anneal T ← max( T0e−t/τ , T min ) 

> 17:

end for Key design choices summarised. 

• DCL attachment : P = F ⊤ 

> θ

W2 (line 4) makes prototypes native network outputs (the critical difference from DeepCluster). 

• Stop-gradient option : detaching qnj from the backbone graph (line 5) gives identical backbone updates for Lq and LOLS (Proposition 4); re-moving stop-gradient activates the ∇P V separation force in the back-bone. 

• Learning-rate ratio : keeping αbb /α DCL ∈ [1 /10 , 1/3] satisfies the sta-bility condition of Corollary 5. 45 • Diagnostics : V ≥ 0 should hold with zero violations; corr( S, K) < 0

confirms the negative feedback cycle (Theorem 2). 

Appendix H. DDCL Training Algorithm (Incremental Formulation) 

Algorithm 2 gives the pseudocode for the incremental (streaming) variant of DDCL introduced in Section 4.5. Data arrive one mini-batch at a time; no sample is revisited. Prototype rows are generated sequentially by the DCL, and assignments are updated online via Widrow–Hoff with simplex projection. 

Algorithm 2 Incremental DDCL — simplified streaming loop 

Require: Stream of batches {x(b) 

> n

}, number of prototypes k, step size μ,temperature schedule T (t)

Ensure: Prototype matrix P ∈ Rd×k, assignment vectors {qn} 

> 1:

Initialise q(0)  

> n

← 1 

> k

1k for all n; P ← 0 

> 2:

for each arriving batch b do  

> 3:

zn ← fθ(x(b) 

> n

) // feature extraction  

> 4:

for each feature dimension t = 1 , . . . , d do  

> 5:

r(t) ← DCL output row t ∈ Rk 

> 6:

for each sample n in batch do  

> 7:

e(t) 

> n

← znt − r(t)⊤q(t−1)  

> n

// prediction error  

> 8:

˜qn ← q(t−1)  

> n

+ μ e (t) 

> n

r(t) // Widrow–Hoff update  

> 9:

q(t) 

> n

← Πk−1∆ (˜ qn) // project onto simplex (see Appendix G)  

> 10:

end for  

> 11:

Append r(t)⊤ as row t of P 

> 12:

end for  

> 13:

Anneal T ← T (t); monitor S(P ) = 1

(k

> 2

)

P 

> i<j

∥pi − pj ∥2 

> 14:

end for Key differences from Algorithm 1. 

• Single pass : each sample is seen at most once; no epoch loop. 

• Row-by-row DCL : P grows one row per feature dimension t, not com-puted all at once from the full batch. 46 • Online assignment update : qn is updated incrementally via Widrow– Hoff; simplex projection ensures q(t) 

> n

∈ ∆k−1 at every step without any iterative inner loop. 

• No backbone gradient : in the standard incremental formulation the backbone is frozen; end-to-end streaming training is an open extension. 

Acknowledgements 

Acknowledgements withheld for review. 

Declaration of generative AI use 

During the preparation of this manuscript, the author used Claude (An-thropic) to assist with language editing, LaTeX formatting, and figure prepa-ration. All scientific content, theoretical results, experimental design, and conclusions are entirely the author’s own. The author reviewed and takes full responsibility for all content. 

Declaration of competing interests 

The author declares no competing interests. 

References 

[1] M. Caron, P. Bojanowski, A. Joulin, and M. Douze, “Deep clustering for unsupervised learning of visual features,” in Proc. ECCV , 2018, pp. 139– 156. [2] G. Cirrincione, V. Randazzo, P. Barbiero, G. Ciravegna, and E. Pasero, “Gradient-based competitive learning: Theory,” Cognitive Computation ,vol. 16, pp. 608–623, 2024. [3] J. Xie, R. Girshick, and A. Farhadi, “Unsupervised deep embedding for clustering analysis,” in Proc. ICML , 2016, pp. 478–487. [4] J. Yang, D. Parikh, and D. Batra, “Joint unsupervised learning of deep representations and image clusters,” in Proc. IEEE CVPR , 2016, pp. 5147–5156. 47 [5] J. Chang, L. Wang, G. Meng, S. Xiang, and C. Pan, “Deep adaptive image clustering,” in Proc. IEEE ICCV , 2017, pp. 5879–5887. [6] M. M. Fard, T. Thonet, and E. Gaussier, “Deep k-means: Jointly clus-tering with k-means and learning representations,” Pattern Recognition Letters , vol. 138, pp. 185–192, 2020. [7] X. Peng, J. Feng, J. T. Zhou, Y. Lei, and S. Yan, “Deep subspace clus-tering,” IEEE Trans. Neural Networks and Learning Systems , vol. 31, no. 12, pp. 5509–5521, 2020. [8] E. Min, X. Guo, Q. Liu, G. Zhang, J. Cui, and J. Long, “A survey of clustering with deep learning,” IEEE Access , vol. 6, pp. 39501–39514, 2018. [9] C. Doersch, A. Gupta, and A. A. Efros, “Unsupervised visual repre-sentation learning by context prediction,” in Proc. IEEE ICCV , 2015, pp. 1422–1430. [10] M. Noroozi and P. Favaro, “Unsupervised learning of visual representa-tions by solving jigsaw puzzles,” in Proc. ECCV , 2016, pp. 69–84. [11] T. Chen, S. Kornblith, M. Noroulli, and G. Hinton, “A simple framework for contrastive learning of visual representations,” in Proc. ICML , 2020, pp. 1597–1607. [12] K. He, H. Fan, Y. Wu, S. Xie, and R. Girshick, “Momentum contrast for unsupervised visual representation learning,” in Proc. IEEE CVPR ,2020, pp. 9729–9738. [13] J.-B. Grill, F. Strub, F. Altché, C. Tallec, P. H. Richemond, E. Buchatskaya, C. Doersch, B. A. Pires, Z. D. Guo, M. G. Azar, B. Piot, K. Kavukcuoglu, R. Munos, and M. Valko, “Bootstrap your own latent,” in Proc. NeurIPS , 2020, pp. 21271–21284. [14] X. Chen and K. He, “Exploring simple siamese representation learning,” in Proc. IEEE CVPR , 2021, pp. 15750–15758. [15] M. Caron, I. Misra, J. Mairal, P. Goyal, P. Bojanowski, and A. Joulin, “Unsupervised learning of visual features by contrasting cluster assign-ments,” in Proc. NeurIPS , 2020, pp. 9912–9924. 48 [16] D. E. Rumelhart and D. Zipser, “Feature discovery by competitive learn-ing,” Cognitive Science , vol. 9, no. 1, pp. 75–112, 1985. [17] T. Kohonen, “Self-organized formation of topologically correct feature maps,” Biological Cybernetics , vol. 43, pp. 59–69, 1982. [18] B. Fritzke, “A growing neural gas network learns topologies,” in Advances in Neural Information Processing Systems , 1995, pp. 625–632. [19] T. Martinetz, “Competitive Hebbian learning rule forms perfectly topol-ogy preserving maps,” in Int. Conf. Artificial Neural Networks , 1993, pp. 427–434. [20] G. Cirrincione, G. Ciravegna, P. Barbiero, V. Randazzo, and E. Pasero, “The GH-EXIN neural network for hierarchical clustering,” Neural Net-works , vol. 121, pp. 57–73, 2020. [21] Y. Bengio, A. Courville, and P. Vincent, “Representation learning: A re-view and new perspectives,” IEEE Trans. PAMI , vol. 35, no. 8, pp. 1798– 1828, 2013. [22] A. Krizhevsky, I. Sutskever, and G. E. Hinton, “ImageNet classifica-tion with deep convolutional neural networks,” in Proc. NeurIPS , 2012, pp. 1097–1105. [23] M. Lovino, G. Urgese, E. Macii, S. Di Cataldo, and E. Ficarra, “A deep learning approach to the screening of oncogenic gene fusions in humans,” 

Int. Journal of Molecular Sciences , vol. 20, 2019. [24] K. He, X. Chen, S. Xie, Y. Li, P. Dollár, and R. Girshick, “Masked autoencoders are scalable vision learners,” in Proc. IEEE CVPR , 2022, pp. 16000–16009. [25] X. Guo, L. Gao, X. Liu, and J. Yin, “Improved deep embedded clustering with local structure preservation,” in Proc. IJCAI , 2017, pp. 1753–1759. [26] W. Van Gansbeke, S. Vandeghen, M. Proesmans, and L. Van Gool, “SCAN: Learning to classify images without labels,” in Proc. ECCV ,2020, pp. 650–668. 49 [27] M. Caron, H. Touvron, I. Misra, H. Jégou, J. Mairal, P. Bojanowski, and A. Joulin, “Emerging properties in self-supervised vision transformers,” in Proc. ICCV , 2021, pp. 9650–9660. [28] Y. Li, P. Hu, Z. Liu, D. Peng, J. T. Zhou, and X. Peng, “Contrastive clustering,” in Proc. AAAI , 2021, pp. 8547–8555. [29] J. Li, P. Zhou, C. Xiong, and S. C. H. Hoi, “Prototypical contrastive learning of unsupervised representations,” in Proc. ICLR , 2021. https: //openreview.net/forum?id=KmykpuSrjcq 

[30] G. Cirrincione and M. Cirrincione, Neural-Based Orthogonal Data Fit-ting: The EXIN Neural Networks . Wiley, 2010. [31] D. Dua and C. Graff, “UCI Machine Learning Repository,” University of California, Irvine, 2017. https://archive.ics.uci.edu/ml 

[32] K. Simonyan and A. Zisserman, “Very deep convolutional networks for large-scale image recognition,” in Proc. ICLR , 2015. https://arxiv. org/abs/1409.1556 

[33] K. He, X. Zhang, S. Ren, and J. Sun, “Deep residual learning for image recognition,” in Proc. IEEE CVPR , 2016, pp. 770–778. [34] B. Saha, D. Krotov, M. J. Zaki, and P. Ram, “End-to-end differentiable clustering with associative memories,” in Proc. ICML , 2023, pp. 29649– 29670. [35] B. Saha, D. Krotov, and M. J. Zaki, “Deep clustering with associative memories,” in Proc. NeurIPS , 2024. 50