---
title: "A Frame is Worth One Token: Efficient Generative World Modeling with Delta Tokens"
title_zh: 一帧价值一个 Token：基于 Delta Token 的高效生成式世界建模
authors: "Tommie Kerssies, Gabriele Berton, Ju He, Qihang Yu, Wufei Ma, Daan de Geus, Gijs Dubbelman, Liang-Chieh Chen"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04913v1"
tags: ["query:navwm"]
score: 9.0
evidence: 在特征空间中使用增量令牌进行高效生成式世界建模
tldr: 本文提出了基于DeltaTok的高效生成式世界建模方法DeltaWorld，通过将视觉基础模型特征间的变化编码为delta token，实现了视频时序的极大压缩与高效预测。该方法在保持多样未来预测能力的同时，显著减少了模型参数和计算量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04913v1/fig-001.webp\", \"caption\": \"Figure 1. Outline of DeltaWorld. Unlike large existing generative world models that require many forward passes and represent each frame with many spatial tokens, our small DeltaWorld generates multiple futures in a single forward pass by using a single delta token to encode the difference between consecutive frames.\", \"page\": 1, \"index\": 1, \"width\": 496, \"height\": 249}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-002.webp\", \"caption\": \"Figure 2. Performance comparison. Compared to the generative world model Cosmos [1], our DeltaWorld forecasts futures that better align with real-world outcomes while having over 35× fewer parameters and using 2,000× fewer FLOPs.\", \"page\": 1, \"index\": 2, \"width\": 414, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-003.webp\", \"caption\": \"Figure 4. Overview of DeltaWorld. The predictor operates entirely on delta tokens (Fig. 3) rather than spatial tokens, enabling efficient generation of future hypotheses. Best-of-Many training (top) backpropagates only through the best predicted delta token, so that diverse futures can be sampled in a single forward pass at inference (bottom). Shown with two context frames and two queries for illustration.\", \"page\": 6, \"index\": 3, \"width\": 925, \"height\": 453}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-004.webp\", \"caption\": \"Table 3. Dense forecasting. Reporting short (∼0.2 s, direct) and mid (∼0.6 s, 3-step rollout) prediction horizons. Generative models report best-of-20 evaluation (mean in parentheses). GFLOPs reflect generating all 20 samples for generative models and a single prediction for DINO-world. Using 512× 512 crops. †Our reimplementation. ‡Both variants use another 7B diffusion decoder, dominating FLOPs.\", \"page\": 8, \"index\": 4, \"width\": 1036, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2604.04913v1/fig-005.webp\", \"caption\": \"Figure 3. Overview of DeltaTok. Given two frames encoded by a frozen vision foundation model (VFM) into grids of patch tokens xt−1 and xt, the DeltaTok encoder takes both as input and compresses them into a single delta token zt. The decoder reconstructs x̂t from xt−1 and zt. Both encoder and decoder are Vision Transformers (ViT) [18] trained with a Mean Squared Error (MSE) loss.\", \"page\": 5, \"index\": 5, \"width\": 919, \"height\": 418}]"
motivation: 现有生成式世界模型计算成本高且难以高效预测多种未来状态。
method: 设计DeltaTok编码帧间特征差异为连续token，并用DeltaWorld生成模型进行多假设视频预测。
result: 在密集预测任务中，DeltaWorld以35倍更小模型和2000倍更少计算量获得更贴近现实的预测结果。
conclusion: DeltaWorld在保证多样性和准确性的前提下，大幅提升了生成式世界建模的效率。
---

## 摘要
预判多样的未来状态是视频世界建模中的核心挑战。判别式世界模型生成的是一个确定性的预测，它隐式地对可能的未来进行了平均，而现有的生成式世界模型在计算上仍然十分昂贵。最新研究表明，在视觉基础模型（Vision Foundation Model, VFM）的特征空间中进行未来预测，而非在用于像素重建的潜在空间中进行预测，可以显著减少世界模型所需的参数。然而，大多数这类方法仍然是判别式的。在本研究中，我们提出了 DeltaTok，一种 tokenizer，用于将连续帧之间的 VFM 特征差异编码为一个连续的“delta” token，并基于此构建了 DeltaWorld，一个在这些 token 上运行的生成式世界模型，能够高效地生成多样且合理的未来。Delta token 将视频从三维时空表示压缩为一维时间序列，例如，对于 512x512 帧，可实现 1,024 倍的 token 减少。这种紧凑的表示使得多假设并行训练成为可行，模型在训练时可并行生成多个未来，并仅监督最优的预测。在推理阶段，这一机制使模型能够在一次前向传播中生成多样化的预测。在密集预测任务上的实验表明，DeltaWorld 所预测的未来与真实结果更加一致，同时其参数量比现有生成式世界模型减少 35 倍以上，FLOPs 使用量降低 2,000 倍。代码和权重： https://deltatok.github.io。

## 速览
**TLDR**：论文提出DeltaTok与DeltaWorld框架，用单一delta标记表征连续帧特征差，将视频转化为紧凑时间序列，从而实现高效多假设生成训练，大幅减少参数与计算量，并能在一次前向推理中生成多样化未来视频预测。 \
**Motivation**：现有生成式视频世界模型计算成本高，判别式模型又难以捕捉多样的未来状态。 \
**Method**：提出DeltaTok对连续帧的VFM特征差编码为单一连续“delta”标记，并基于此训练生成模型DeltaWorld预测未来帧。 \
**Result**：在多种密集预测任务中，DeltaWorld以35倍更少参数、2000倍更低FLOPs实现了更贴近真实的未来预测。 \
**Conclusion**：DeltaWorld显著提高了视频未来预测的效率与多样性，在保持预测质量的同时极大降低了计算与参数开销。

---

## Abstract
Anticipating diverse future states is a central challenge in video world modeling. Discriminative world models produce a deterministic prediction that implicitly averages over possible futures, while existing generative world models remain computationally expensive. Recent work demonstrates that predicting the future in the feature space of a vision foundation model (VFM), rather than a latent space optimized for pixel reconstruction, requires significantly fewer world model parameters. However, most such approaches remain discriminative. In this work, we introduce DeltaTok, a tokenizer that encodes the VFM feature difference between consecutive frames into a single continuous "delta" token, and DeltaWorld, a generative world model operating on these tokens to efficiently generate diverse plausible futures. Delta tokens reduce video from a three-dimensional spatio-temporal representation to a one-dimensional temporal sequence, for example yielding a 1,024x token reduction with 512x512 frames. This compact representation enables tractable multi-hypothesis training, where many futures are generated in parallel and only the best is supervised. At inference, this leads to diverse predictions in a single forward pass. Experiments on dense forecasting tasks demonstrate that DeltaWorld forecasts futures that more closely align with real-world outcomes, while having over 35x fewer parameters and using 2,000x fewer FLOPs than existing generative world models. Code and weights: https://deltatok.github.io.

---

## 论文详细总结（自动生成）

# 一帧价值一个 Token：基于 Delta Token 的高效生成式世界建模 — 深度总结

---

## 一、研究问题与背景

- **研究动机**：  
  视频世界建模的核心目标是在给定历史帧的基础上，预测场景的未来发展。对于自动驾驶、机器人等任务，世界模型需能生成多个可能的未来状态。  
  - **判别式世界模型**虽然准确，但输出的是单一确定性预测，会在不确定性较高的场景下退化为平均结果。  
  - **生成式世界模型**可生成多种未来，但往往需要庞大的模型和多轮推理，计算成本极高。  
- **核心问题**：  
  现有生成式模型在视频建模中存在三个关键瓶颈：  
  1. 使用像素级重建空间，计算量高且语义利用率低。  
  2. 每生成一次未来需多步迭代推理。  
  3. 未能利用视频帧间的时空冗余。  
- **论文目标**：  
  提出一个能够在单次前向传播中生成多样未来、同时极大降低计算与参数量的生成式世界模型。

---

## 二、方法论：DeltaTok 与 DeltaWorld

### 1. 核心思想

- 引入 **DeltaTok**：一种视频 tokenizer，用于编码视觉基础模型（VFM）特征空间中**连续帧之间的差异**，形成一个“delta token”。  
  - 该 token 表示从上一帧到下一帧的变化，而非完整帧，有效压缩 temporal 信息。
- 在此基础上构建 **DeltaWorld**：一个运行在 delta token 序列上的生成式世界模型。  
  - 将视频三维表示（空间 × 时间）压缩为一维时间序列。  
  - 使用 **Best-of-Many (BoM)** 训练策略，使模型在训练中生成多个未来，仅监督最接近真实的结果，从而在推理阶段凭随机输入实现多样预测。

### 2. 技术结构

- **特征编码**：  
  - 视频帧通过冻结的 VFM（如 DINOv3）提取特征。  
  - 相邻帧特征差分被 DeltaTok 编码为 delta token。
- **DeltaTok 架构**：  
  - 编码器 `g(xt−1, xt, zinit)` 产生 delta token。  
  - 解码器 `h(xt−1, zt)` 用上一帧特征加 delta token 重构下一帧特征。  
  - 损失函数采用均方误差（MSE）重建损失。
- **DeltaWorld 生成流程**：  
  - 输入历史 delta token 序列 `Z1:t`。  
  - 使用噪声查询 `qk ~ N(μ, Σ)` 生成多个未来候选 delta token `ˆzt+1`。  
  - 使用 BoM 策略选出最优预测并反向传播。  
  - 推理阶段通过单步前向传播即可得到多种可能未来。
- **效率意义**：  
  - 每帧仅对应一个 token，相比传统 的 H×W 空间 grid 实现 **约 1,024 倍的 token 压缩**（512×512 图像）。  
  - 同时消除重复空间建模，专注视频的时间变化。

---

## 三、实验设计

### 1. 数据集与任务

- 使用 **Dense Forecasting Benchmark**（与 DINO-world 一致）：  
  - **VSPW** — 视频场景解析任务（多域语义分割）。  
  - **Cityscapes** — 城市驾驶场景语义分割。  
  - **KITTI** — 自动驾驶深度估计任务。  
- 评估设置：
  - “短期”预测（约 0.2s）直接生成；  
  - “中期”预测（约 0.6s）采用三步自回归滚动。  
- 评估指标：  
  - 分割任务使用 mIoU；  
  - 深度任务使用 RMSE。  

### 2. 对比方法

- 判别式基线：**DINO-world**（作者重实现版本）。  
- 生成式基线：**Cosmos-4B / Cosmos-12B** — 大型世界模型。  
- 边界参考：**Copy-last（下界）** 和 **Present（上界）**。  

### 3. 消融实验

- 三个阶段测试：
  1. 仅使用 BoM（生成式但无压缩）。  
  2. 帧级压缩（单 token 表征整帧）。  
  3. Delta 压缩（提出方法）。  
- 测试维度包括模型参数量、FLOPs、训练时间、GPU 内存占用及性能表现。  
- 额外实验：验证 delta token 在判别式架构中也有效（例如 DINO-Foresight）。

---

## 四、资源与算力

- **硬件信息**：  
  - 使用 8 × NVIDIA H200 GPU 训练节点（BF16 精度，torch.compile）。  
  - 未给出训练总时长，但主模型训练迭代数：
    - Tokenizer：50K 次迭代；  
    - Predictor：300K 次迭代（主结果），或 100K（消融实验）。  
- **计算资源评估**：  
  - DeltaWorld 使用约 **3.1×10⁴ GFLOPs**（生成20样本）；  
  - 对比 Cosmos-12B 需 **6.4×10⁷ GFLOPs**，差距约 **2000×**。  
  - 模型参数量约 **0.3B vs. Cosmos-12B**。

---

## 五、实验数量与充分性

- **覆盖广度**：
  - 三个不同类型数据集（多域分割 + 深度预测）。
  - 完整比较短期与中期两种时间尺度。
  - 消融实验包括多策略与不同 K 值（BoM 样本数量）。  
- **验证充分性**：
  - 涵盖基准对比、消融、扩展到不同架构的测试。  
  - Benchmark 与评价指标与既有研究保持一致。  
  - 重现与公平性：作者重实现了 DINO-world 以统一比较标准。  
  → 整体实验设计充分且客观。

---

## 六、主要结论与发现

1. **DeltaTok 压缩帧间变化，实现视频序列的一维化表示。**  
   - 512×512 视频帧可减少约 1,024× token。  
2. **DeltaWorld 实现高效生成式世界建模。**  
   - 单次前向传播即可生成多样未来，避免多次采样或扩散步骤。  
3. **性能表现**：  
   - 与大型生成模型 Cosmos 相比，**参数减少 35×，FLOPs 减少 2000×**，预测更贴近真实。  
   - 在 Cityscapes、VSPW、KITTI 上的 **最佳预测均优于 Cosmos**，平均性能竞争性强。  
4. **特征空间预测优越性**：在语义级别进行视频建模比像素空间更高效且更符合下游任务需求。

---

## 七、优点与创新亮点

- ✅ **创新表示方式**：首次使用单个连续 delta token 表征帧间差异，将视频时序视为一维序列。  
- ✅ **训练高效**：BoM 结合 delta token，只需单次前向传播产生多个未来样本。  
- ✅ **极高的计算效率**：参数和 FLOPs 数量均比主流生成式世界模型显著降低。  
- ✅ **泛化能力强**：跨数据集（多域场景）保持稳定性能。  
- ✅ **解释性增强**：由于 delta 表示“变化”，自然包含静态保持的时间先验，使平均预测稳定。  
- ✅ **扩展性思路清晰**：框架可拓展至更长上下文或更大模型规模。

---

## 八、不足与局限

- ❗ **分布学习不够严格**：  
  BoM 目标仅保证样本多样性，无显式的分布建模，与扩散模型相比缺乏生成概率意义。  
- ❗ **误差积累问题**：  
  自回归解码过程中，delta token 的误差可能逐步积累，导致特征漂移。  
- ❗ **数据与代码依赖性**：  
  部分基线（DINO-world原版）无公开代码，作者自重实现，可能引入轻微差异。  
- ❗ **任务局限**：  
  实验主要验证语义分割与深度任务，未覆盖运动规划、物体交互等更复杂世界建模应用。  
- ❗ **缺乏大规模可视化分析**：  
  虽有附录展示多样预测，但未系统量化“未来多样性”指标。

---

**（完）**
